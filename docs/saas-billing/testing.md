# Testing

The focused `SaasBilling` suite covers tax calculation, idempotent invoice generation, partial/manual collection, platform permission boundaries, tenant isolation, server-verified test checkout, signed webhook rejection, and schedule generation. No test calls live Razorpay endpoints.
