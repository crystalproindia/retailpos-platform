@extends('layouts.admin')
@section('title','AI Forecast Settings')
@section('page-title','AI Forecast Settings')
@section('content')<form method="POST" action="{{ route('ai.settings.update') }}" class="mx-auto max-w-3xl space-y-6">@csrf @method('PUT')<section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h1 class="text-xl font-semibold">Forecast settings</h1><p class="mt-1 text-sm text-slate-500">These values affect later calculations only. They never change operational records.</p><div class="mt-5 grid gap-4 sm:grid-cols-2">@foreach(['minimum_sales_history_days'=>'Minimum sales-history days','safety_stock_days'=>'Safety-stock days','slow_moving_days'=>'Slow-moving threshold','dead_stock_days'=>'Dead-stock threshold'] as $key=>$label)<label><span class="text-sm font-medium">{{ $label }}</span><input type="number" name="{{ $key }}" value="{{ old($key,$settings[$key]) }}" class="mt-2 w-full rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label>@endforeach</div><label class="mt-5 flex gap-3 text-sm"><input type="checkbox" name="scheduled_generation_enabled" value="1" @checked($settings['scheduled_generation_enabled'])> Enable scheduled advisory generation</label></section><button class="rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Save settings</button></form>@endsection
