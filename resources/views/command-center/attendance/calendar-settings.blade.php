@extends('layouts.admin')
@section('title', 'Holidays and weekly offs') @section('page-title', 'Holidays and weekly offs')
@section('content')<div class="mx-auto max-w-6xl space-y-6"><div><p class="text-sm font-semibold text-teal-700">Scheduling rules</p><h1 class="mt-1 text-2xl font-semibold text-slate-950">Holidays and weekly offs</h1><p class="mt-2 text-sm text-slate-500">These rules support transparent roster and leave calculations. They never rewrite historic attendance automatically.</p></div><div class="grid gap-6 lg:grid-cols-2"><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold">Add holiday</h2><form method="POST" action="{{ route('attendance.holidays.store') }}" class="mt-4 space-y-3">@csrf<input name="name" required placeholder="Holiday name" class="w-full rounded border-slate-300"><input type="date" name="holiday_date" required class="w-full rounded border-slate-300"><select name="outlet_id" class="w-full rounded border-slate-300"><option value="">All outlets</option>@foreach($outlets as $outlet)<option value="{{ $outlet->id }}">{{ $outlet->name }}</option>@endforeach</select><select name="holiday_type" class="w-full rounded border-slate-300"><option value="paid">Paid holiday</option><option value="unpaid">Unpaid holiday</option><option value="observance">Observance</option></select><button class="rounded bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Add holiday</button></form></section><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold">Add weekly off</h2><form method="POST" action="{{ route('attendance.weekly-offs.store') }}" class="mt-4 space-y-3">@csrf<select name="weekday" class="w-full rounded border-slate-300">@foreach([1=>'Monday',2=>'Tuesday',3=>'Wednesday',4=>'Thursday',5=>'Friday',6=>'Saturday',7=>'Sunday'] as $day=>$label)<option value="{{ $day }}">{{ $label }}</option>@endforeach</select><select name="outlet_id" class="w-full rounded border-slate-300"><option value="">Company default</option>@foreach($outlets as $outlet)<option value="{{ $outlet->id }}">{{ $outlet->name }}</option>@endforeach</select><button class="rounded bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Add weekly off</button></form><div class="mt-5 space-y-2">@forelse($weeklyOffs as $off)<div class="rounded bg-slate-50 p-3 text-sm">{{ ['','Mon','Tue','Wed','Thu','Fri','Sat','Sun'][$off->weekday] }} · {{ $off->outlet_id ? 'Outlet rule' : 'Company default' }}</div>@empty<p class="text-sm text-slate-500">No weekly off rules yet.</p>@endforelse</div></section></div><section class="rounded-xl border border-slate-200 bg-white shadow-sm"><div class="divide-y divide-slate-100">@forelse($holidays as $holiday)<div class="flex justify-between gap-4 p-4"><span><strong>{{ $holiday->name }}</strong><span class="ml-2 text-sm text-slate-500">{{ $holiday->holiday_date->format('j M Y') }}</span></span><span class="text-sm text-slate-500">{{ $holiday->outlet?->name ?: 'All outlets' }}</span></div>@empty<div class="p-8 text-center text-sm text-slate-500">No holidays added yet.</div>@endforelse</div></section>{{ $holidays->links() }}</div>@endsection
