@extends('layouts.admin')
@section('title', 'Attendance history') @section('page-title', 'Attendance history')
@section('content')<div class="mx-auto max-w-6xl space-y-5"><div class="flex justify-between"><div><h1 class="text-2xl font-semibold text-slate-950">Attendance history</h1><p class="mt-1 text-sm text-slate-500">{{ $employee->display_name }} · Your own records only.</p></div><a href="{{ route('attendance.self') }}" class="text-sm font-semibold text-teal-700">Today</a></div><section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="px-4 py-3">Date</th><th class="px-4 py-3">Outlet</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Worked</th><th class="px-4 py-3">Correction</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($records as $record)<tr><td class="px-4 py-3 font-medium">{{ $record->attendance_date->format('j M Y') }}</td><td class="px-4 py-3">{{ $record->outlet?->name }}</td><td class="px-4 py-3">{{ str($record->attendance_status)->headline() }}</td><td class="px-4 py-3">{{ $record->worked_minutes }} min</td><td class="px-4 py-3">@if($record->correction_status === 'none')<details><summary class="cursor-pointer text-teal-700">Request correction</summary><form method="POST" action="{{ route('attendance.corrections.store', $record) }}" class="mt-2 space-y-2">@csrf<input name="checked_in_at" type="datetime-local" class="w-full rounded border-slate-300 text-xs"><input name="checked_out_at" type="datetime-local" class="w-full rounded border-slate-300 text-xs"><input name="reason" required placeholder="Why is a correction needed?" class="w-full rounded border-slate-300 text-xs"><button class="text-xs font-semibold text-teal-700">Submit</button></form></details>@else{{ str($record->correction_status)->headline() }}@endif</td></tr>@empty<tr><td colspan="5" class="px-4 py-10 text-center text-slate-500">No attendance records yet.</td></tr>@endforelse</tbody></table></div></section>{{ $records->links() }}</div>@endsection
