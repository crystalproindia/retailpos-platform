@extends('layouts.admin')
@section('title', 'Attendance register') @section('page-title', 'Attendance register')
@section('content')<div class="mx-auto max-w-7xl space-y-5"><div class="flex flex-wrap items-end justify-between gap-3"><div><p class="text-sm font-semibold text-teal-700">Attendance register</p><h1 class="mt-1 text-2xl font-semibold text-slate-950">Authorized attendance records</h1></div><a href="{{ route('attendance.export') }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700">Export CSV</a></div><form class="flex flex-wrap gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><input type="date" name="date" value="{{ request('date') }}" class="rounded-lg border-slate-300 text-sm"><select name="status" class="rounded-lg border-slate-300 text-sm"><option value="">All statuses</option>@foreach(['present','partial_day','missing_check_out','pending_correction','absent','on_leave','holiday','weekly_off'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ str($status)->headline() }}</option>@endforeach</select><button class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Filter</button></form><section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="px-4 py-3">Employee</th><th class="px-4 py-3">Date</th><th class="px-4 py-3">Outlet</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Worked</th><th class="px-4 py-3">Late</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($records as $record)<tr><td class="px-4 py-3 font-medium">{{ $record->employee?->display_name }}</td><td class="px-4 py-3">{{ $record->attendance_date->format('j M Y') }}</td><td class="px-4 py-3">{{ $record->outlet?->name }}</td><td class="px-4 py-3"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold">{{ str($record->attendance_status)->headline() }}</span></td><td class="px-4 py-3">{{ $record->worked_minutes }}m</td><td class="px-4 py-3">{{ $record->late_minutes }}m</td></tr>@empty<tr><td colspan="6" class="px-4 py-10 text-center text-slate-500">No attendance records match this view.</td></tr>@endforelse</tbody></table></div></section>{{ $records->links() }}</div>@endsection
