@extends('layouts.admin')
@section('title','Branding Manager') @section('page-title','Branding Manager')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><span>Branding</span>@endsection
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<form method="POST" action="{{ route('cms.branding.update') }}">@csrf @method('PUT')<x-form-section title="Brand System" help="Media references stay in the existing CMS library. Add each uploaded file by selecting it below."><div class="grid gap-5 md:grid-cols-2">@foreach($definitions as $key => $definition)<label class="block text-sm font-medium text-slate-700 dark:text-slate-200"><span>{{ $definition['label'] }}</span>@if($definition['type']==='media')<select name="{{ $key }}" class="mt-2 block w-full"><option value="">No media selected</option>@foreach($media as $item)<option value="{{ $item->id }}" @selected(old($key,$settings[$key]?->media_id)==$item->id)>{{ $item->filename }}</option>@endforeach</select>@else<input name="{{ $key }}" value="{{ old($key,$settings[$key]?->value) }}" @if(str_contains($key,'color')) placeholder="#0f766e" @endif class="mt-2 block w-full">@endif</label>@endforeach</div></x-form-section><x-sticky-form-actions><a href="{{ route('cms.dashboard') }}" class="rounded-lg px-4 py-2 text-sm font-semibold text-slate-600 dark:text-slate-300">Cancel</a><button class="rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Save branding</button></x-sticky-form-actions></form></div>
@endsection
