@extends('layouts.admin')
@section('title','Edit Case Study') @section('page-title','Edit Case Study')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><a href="{{ route('cms.case-studies.index') }}">Case Studies</a><span>/</span><span>{{ $study->title }}</span>@endsection
@section('content')<div class="space-y-6">@include('command-center.cms.partials.nav')<div class="flex flex-wrap gap-2"><form method="POST" action="{{ route($routePrefix.'.case-studies.preview',$study) }}">@csrf<button class="rounded-lg border border-sky-300 px-4 py-2 text-sm font-semibold text-sky-700">Preview Draft</button></form>@if($study->status === 'published')<a href="{{ url('/case-studies/'.$study->slug) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">View Live Page</a>@endif</div>@if(session('preview_url'))<section class="rounded-lg border border-sky-200 bg-sky-50 p-4 text-sm text-sky-900">Private preview link: <a class="font-semibold underline" href="{{ session('preview_url') }}" target="_blank">Open preview</a><form class="mt-3 inline" method="POST" action="{{ route($routePrefix.'.case-studies.preview.revoke',$study) }}">@csrf<button class="font-semibold underline">Revoke preview links</button></form></section>@endif<form method="POST" action="{{ route($routePrefix.'.case-studies.update',$study) }}">@csrf @method('PUT') @include('command-center.cms.case-studies.form')<x-sticky-form-actions><a href="{{ route($routePrefix.'.case-studies.index') }}" class="rounded-lg px-4 py-2 text-sm font-semibold">Cancel</a><button class="rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Save case study</button></x-sticky-form-actions></form></div>@endsection
