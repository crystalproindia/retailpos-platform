@extends('layouts.admin')
@section('title','Case Studies') @section('page-title','Case Study Manager')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><span>Case Studies</span>@endsection
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<div class="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><form class="flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search title or client"><select name="status"><option value="">All statuses</option>@foreach(['draft','published','scheduled'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->headline() }}</option>@endforeach</select><button class="rounded-lg border px-4 text-sm font-semibold">Filter</button></form><a href="{{ route('cms.case-studies.create') }}" class="rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">New case study</a></div><div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm dark:divide-slate-800"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500 dark:bg-slate-800"><tr><th class="px-5 py-3">Case Study</th><th class="px-5 py-3">Client</th><th class="px-5 py-3">Industry</th><th class="px-5 py-3">Status</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($studies as $study)<tr><td class="px-5 py-4"><a href="{{ route('cms.case-studies.edit',$study) }}" class="font-semibold text-slate-950 hover:text-teal-700 dark:text-white">{{ $study->title }}</a><p class="mt-1 text-xs text-slate-500">{{ $study->short_summary }}</p></td><td class="px-5 py-4">{{ $study->client_name }}</td><td class="px-5 py-4 text-slate-500">{{ $study->industry }}</td><td class="px-5 py-4"><x-status-badge :status="$study->status"/></td><td class="px-5 py-4"><div class="flex gap-3">@if($study->status!=='published')<form method="POST" action="{{ route('cms.case-studies.publish',$study) }}">@csrf<button class="text-sm font-semibold text-teal-700">Publish</button></form>@else<form method="POST" action="{{ route('cms.case-studies.unpublish',$study) }}">@csrf<button class="text-sm font-semibold text-amber-700">Unpublish</button></form>@endif<form method="POST" action="{{ route('cms.case-studies.destroy',$study) }}">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No case studies yet.</td></tr>@endforelse</tbody></table></div><div class="border-t px-5 py-4 dark:border-slate-800">{{ $studies->links() }}</div></div></div>
@endsection
