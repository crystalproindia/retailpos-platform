@extends('layouts.admin')
@section('title','Client Logos') @section('page-title','Client Logo Manager')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><span>Client Logos</span>@endsection
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<div class="grid gap-6 xl:grid-cols-[0.7fr_1.3fr]"><form method="POST" action="{{ route('cms.client-logos.store') }}" class="form-section">@csrf<h2 class="form-section-title">Add client logo</h2><div class="mt-5 space-y-4"><input name="name" required placeholder="Client name"><select name="logo_media_id"><option value="">Logo media (optional)</option>@foreach($media as $item)<option value="{{ $item->id }}">{{ $item->filename }}</option>@endforeach</select><input name="website_url" placeholder="Website URL"><input name="industry" placeholder="Industry"><input name="location" placeholder="Location"><textarea name="short_description" rows="3" placeholder="Demo-only description or approved client context"></textarea><select name="display_style"><option value="color">Color</option><option value="grayscale">Grayscale</option><option value="monochrome">Monochrome</option></select><label class="flex items-center gap-3 text-sm"><input type="checkbox" name="show_on_homepage" value="1" checked>Show on homepage</label><label class="flex items-center gap-3 text-sm"><input type="checkbox" name="is_active" value="1" checked>Active</label><button class="w-full rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Add logo</button></div></form><section class="form-section"><div class="mb-5 flex items-center justify-between"><div><h2 class="form-section-title">Logo library</h2><p class="form-section-help">Display behavior is managed independently from the media file.</p></div><form class="flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search"><button class="rounded-lg border px-3 text-sm font-semibold">Filter</button></form></div><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($logos as $logo)<article class="rounded-lg border border-slate-200 p-4 dark:border-slate-700"><div class="flex items-start justify-between gap-3"><div class="grid size-12 place-items-center rounded-lg bg-slate-100 text-sm font-bold text-slate-500 dark:bg-slate-800">{{ str($logo->name)->substr(0,2)->upper() }}</div><x-status-badge :status="$logo->is_active ? 'active' : 'inactive'"/></div><p class="mt-4 font-semibold text-slate-950 dark:text-white">{{ $logo->name }}</p><p class="mt-1 text-sm text-slate-500">{{ $logo->industry ?: 'No industry' }} · {{ str($logo->display_style)->headline() }}</p><form method="POST" action="{{ route('cms.client-logos.update',$logo) }}" class="mt-4 space-y-2">@csrf @method('PUT')<input name="name" value="{{ $logo->name }}"><input type="hidden" name="display_style" value="{{ $logo->display_style }}"><input type="hidden" name="is_active" value="{{ $logo->is_active ? 1 : 0 }}"><button class="text-sm font-semibold text-teal-700 dark:text-teal-300">Quick save</button></form><form method="POST" action="{{ route('cms.client-logos.destroy',$logo) }}" class="mt-2">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700">Move to trash</button></form></article>@empty<p class="text-sm text-slate-500">No client logos yet.</p>@endforelse</div><div class="mt-5">{{ $logos->links() }}</div></section></div></div>
@endsection
