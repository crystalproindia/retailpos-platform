@php $navValue = fn (string $key, mixed $default = null) => old($key, data_get($item, $key, $default)); @endphp
<label class="block text-sm font-medium text-slate-700">Link text<input name="label" value="{{ $navValue('label') }}" required maxlength="120" class="cms-input mt-1" placeholder="Products"><span class="mt-1 block text-xs font-normal text-slate-500">The words visitors will select.</span></label>
<label class="block text-sm font-medium text-slate-700">Where it goes<input name="url" value="{{ $navValue('url') }}" required class="cms-input mt-1" placeholder="/products"><span class="mt-1 block text-xs font-normal text-slate-500">Use a website path or an HTTPS link.</span></label>
<label class="block text-sm font-medium text-slate-700">Show in<select name="location" class="cms-input mt-1">@foreach($locations as $key => $label)<option value="{{ $key }}" @selected($navValue('location', 'header') === $key)>{{ $label }}</option>@endforeach</select></label>
<label class="block text-sm font-medium text-slate-700">Display order<input name="sort_order" type="number" min="0" max="65535" value="{{ $navValue('sort_order') }}" class="cms-input mt-1" placeholder="Automatic"><span class="mt-1 block text-xs font-normal text-slate-500">Use a lower number to show this link first, or leave blank for automatic order.</span></label>
<label class="block text-sm font-medium text-slate-700">Place under another link<select name="parent_id" class="cms-input mt-1"><option value="">No parent link</option>@foreach($items as $parent)<option value="{{ $parent->id }}" @selected((string) $navValue('parent_id') === (string) $parent->id) @disabled($item && $parent->id === $item->id)>{{ $parent->label }}</option>@endforeach</select><span class="mt-1 block text-xs font-normal text-slate-500">Optional. This prepares a nested menu without showing code.</span></label>
<div class="md:col-span-2 flex flex-wrap gap-6"><label class="flex items-center gap-2 text-sm font-medium text-slate-700"><input type="hidden" name="is_enabled" value="0"><input type="checkbox" name="is_enabled" value="1" @checked((bool) $navValue('is_enabled', true)) class="rounded border-slate-300 text-teal-600"> Show this link</label><label class="flex items-center gap-2 text-sm font-medium text-slate-700"><input type="hidden" name="opens_new_tab" value="0"><input type="checkbox" name="opens_new_tab" value="1" @checked((bool) $navValue('opens_new_tab', false)) class="rounded border-slate-300 text-teal-600"> Open external link in a new tab</label></div>
