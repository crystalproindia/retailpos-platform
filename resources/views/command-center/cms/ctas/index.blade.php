@extends('layouts.admin')
@section('title','CTA Blocks') @section('page-title','CTA Block Manager')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><span>CTAs</span>@endsection
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<div class="grid gap-6 xl:grid-cols-[0.7fr_1.3fr]"><form method="POST" action="{{ route('cms.ctas.store') }}" class="form-section">@csrf<h2 class="form-section-title">Add CTA block</h2><div class="mt-5 space-y-4"><input name="title" required placeholder="CTA title"><textarea name="description" rows="3" placeholder="Description"></textarea><input name="button_text" required placeholder="Primary button text"><input name="button_link" required placeholder="Primary button link"><input name="secondary_button_text" placeholder="Secondary button text"><input name="secondary_button_link" placeholder="Secondary button link"><input name="location" placeholder="Location or page"><input name="style" placeholder="Style foundation"><label class="flex items-center gap-3"><input type="checkbox" name="is_active" value="1" checked>Active</label><button class="w-full rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Add CTA</button></div></form><section class="grid gap-4 md:grid-cols-2">@forelse($ctas as $cta)<article class="form-section"><p class="font-semibold text-slate-950 dark:text-white">{{ $cta->title }}</p><p class="mt-2 text-sm text-slate-500">{{ $cta->description }}</p><div class="mt-5 flex flex-wrap gap-2"><span class="rounded-lg bg-teal-50 px-3 py-2 text-sm font-semibold text-teal-700 dark:bg-teal-950 dark:text-teal-200">{{ $cta->button_text }}</span><span class="text-sm text-slate-500">{{ $cta->location ?: 'Global' }}</span></div><form method="POST" action="{{ route('cms.ctas.destroy',$cta) }}" class="mt-4">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700">Delete</button></form></article>@empty<p class="text-sm text-slate-500">No CTA blocks yet.</p>@endforelse</section></div><div>{{ $ctas->links() }}</div></div>
@endsection
