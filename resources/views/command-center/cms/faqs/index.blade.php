@extends('layouts.admin')
@section('title','FAQ Manager') @section('page-title','FAQ Manager')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><span>FAQs</span>@endsection
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<form method="POST" action="{{ route('cms.faqs.store') }}" class="form-section">@csrf<h2 class="form-section-title">Add FAQ</h2><div class="mt-5 grid gap-4 md:grid-cols-2"><input name="question" required placeholder="Question" class="md:col-span-2"><textarea name="answer" required rows="4" placeholder="Answer" class="md:col-span-2"></textarea><input name="category" placeholder="Category"><input name="page_location" placeholder="Page or location"><input type="number" name="sort_order" value="0" placeholder="Sort order"><label class="flex items-center gap-3"><input type="checkbox" name="is_active" value="1" checked>Active</label></div><button class="mt-5 rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Add FAQ</button></form><section class="space-y-3">@forelse($faqs as $faq)<article class="form-section"><p class="font-semibold text-slate-950 dark:text-white">{{ $faq->question }}</p><p class="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $faq->answer }}</p><p class="mt-3 text-xs text-slate-500">{{ $faq->category ?: 'General' }} · {{ $faq->page_location ?: 'Any page' }}</p><form method="POST" action="{{ route('cms.faqs.destroy',$faq) }}" class="mt-3">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700">Delete</button></form></article>@empty<p class="form-section text-sm text-slate-500">No FAQs yet.</p>@endforelse</section><div>{{ $faqs->links() }}</div></div>
@endsection
