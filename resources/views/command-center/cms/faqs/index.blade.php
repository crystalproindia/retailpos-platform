@extends('layouts.admin')
@section('title', 'FAQ Manager') @section('page-title', 'FAQ Manager')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><span>FAQs</span>@endsection
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<form method="POST" action="{{ route('cms.faqs.store') }}" class="cms-panel">@csrf<p class="cms-kicker">New FAQ</p><h1 class="mt-2 text-xl font-semibold text-slate-900">Frequently asked questions</h1><div class="mt-5 grid gap-4 md:grid-cols-2"><input name="question" required placeholder="Question" class="md:col-span-2"><textarea name="answer" required rows="5" placeholder="A clear, approved answer" class="md:col-span-2"></textarea><input name="category" placeholder="Category"><input name="page_location" placeholder="Page or location"><input type="number" name="sort_order" value="0" placeholder="Sort order"><label class="cms-toggle"><input type="checkbox" name="is_active" value="1" checked> Active</label></div><button class="cms-button-primary mt-5">Add FAQ</button></form><section class="space-y-3">@forelse($faqs as $faq)<article class="cms-section-card"><div class="flex items-start justify-between gap-4"><div><p class="font-semibold text-slate-900">{{ $faq->question }}</p><p class="mt-3 text-sm leading-6 text-slate-600">{{ $faq->answer }}</p><p class="mt-4 text-xs font-semibold uppercase tracking-[0.1em] text-slate-400">{{ $faq->category ?: 'General' }} · {{ $faq->page_location ?: 'Any page' }}</p></div><span class="text-lg text-teal-600">+</span></div><form method="POST" action="{{ route('cms.faqs.destroy',$faq) }}" class="mt-4">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700">Move to trash</button></form></article>@empty<p class="cms-subtle-panel text-center text-sm text-slate-500">No FAQs yet.</p>@endforelse</section><div>{{ $faqs->links() }}</div></div>
@endsection
