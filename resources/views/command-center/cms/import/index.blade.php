@extends('layouts.admin')
@section('title', 'Website Content Import')
@section('page-title', 'Website Content Import')
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<section class="max-w-3xl rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><p class="text-sm font-semibold text-teal-700">Restricted tool</p><h1 class="mt-2 text-xl font-semibold text-slate-950">Import website content</h1><p class="mt-2 text-sm leading-6 text-slate-600">Upload a JSON manifest from the public website repository. Imports create drafts by default and never accept secrets or executable files.</p>@if($errors->any())<div class="mt-5 rounded-lg border border-rose-200 bg-rose-50 p-4 text-sm text-rose-800"><p class="font-semibold">The manifest needs attention</p><ul class="mt-2 list-disc space-y-1 pl-5">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form class="mt-6 space-y-5" method="POST" enctype="multipart/form-data" action="{{ route('website.import.store') }}">@csrf<input type="file" name="manifest" accept="application/json,.json" required class="block w-full rounded-lg border border-slate-300 p-3 text-sm"><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="dry_run" value="1"> Validate only (dry run)</label><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="update_existing" value="1"> Update existing matching content</label><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="publish" value="1"> Publish after import (requires complete content)</label><button class="rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Process manifest</button></form>@if(session('import_result'))<dl class="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-5">@foreach(session('import_result') as $label=>$count)<div class="rounded-lg bg-slate-50 p-3"><dt class="text-xs text-slate-500">{{ str($label)->headline() }}</dt><dd class="mt-1 text-lg font-semibold">{{ $count }}</dd></div>@endforeach</dl>@endif</section></div>
@endsection
