@extends('layouts.admin')
@section('title', 'Revision History')
@section('page-title', 'Revision History')
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')
<section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center justify-between gap-4"><div><p class="text-sm text-slate-500">{{ $entity->title }}</p><h1 class="text-xl font-semibold text-slate-950">Revision history</h1></div><a class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold" href="{{ route($routePrefix.'.'.$entityType.'.edit', $entity) }}">Back to editor</a></div>
<div class="mt-5 space-y-3">@forelse($revisions as $revision)<article class="rounded-lg border border-slate-200 p-4"><div class="flex flex-wrap items-start justify-between gap-3"><div><p class="font-semibold text-slate-900">Revision {{ $revision->revision_number }} · {{ str($revision->action)->headline() }}</p><p class="mt-1 text-sm text-slate-500">{{ $revision->created_at->format('d M Y, h:i A') }} by {{ $revision->creator?->name ?? 'System' }}</p>@if($revision->change_summary)<p class="mt-2 text-sm text-slate-700">{{ $revision->change_summary }}</p>@endif @if($revision->changed_fields)<p class="mt-2 text-xs text-slate-500">Changed: {{ implode(', ', $revision->changed_fields) }}</p>@endif</div><form method="POST" action="{{ route($routePrefix.'.'.$entityType.'.revisions.restore', [$entity, $revision]) }}" onsubmit="return confirm('Restore this revision as a draft?')">@csrf<button class="rounded-lg border border-teal-300 px-3 py-2 text-sm font-semibold text-teal-700">Restore as draft</button></form></div><details class="mt-3"><summary class="cursor-pointer text-sm font-semibold text-slate-700">View snapshot</summary><pre class="mt-3 overflow-auto rounded bg-slate-950 p-3 text-xs text-slate-100">{{ json_encode($revision->snapshot, JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) }}</pre></details></article>@empty<p class="text-sm text-slate-500">No reusable revisions have been recorded yet.</p>@endforelse</div></section></div>
@endsection
