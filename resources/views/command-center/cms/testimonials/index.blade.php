@extends('layouts.admin')
@section('title','Testimonials') @section('page-title','Testimonial Manager')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><span>Testimonials</span>@endsection
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<div class="grid gap-6 xl:grid-cols-[0.7fr_1.3fr]"><form method="POST" action="{{ route('cms.testimonials.store') }}" class="form-section">@csrf<h2 class="form-section-title">Add testimonial</h2><div class="mt-5 space-y-4"><input name="client_name" required placeholder="Client name"><input name="company_name" placeholder="Company name"><input name="designation" placeholder="Designation"><select name="case_study_id"><option value="">Related case study (optional)</option>@foreach($caseStudies as $study)<option value="{{ $study->id }}">{{ $study->title }}</option>@endforeach</select><textarea name="testimonial_text" required rows="5" placeholder="Approved testimonial text"></textarea><select name="rating"><option value="">Rating (optional)</option>@for($i=5;$i>=1;$i--)<option value="{{ $i }}">{{ $i }} stars</option>@endfor</select><label class="flex gap-3"><input type="checkbox" name="show_on_homepage" value="1" checked>Show on homepage</label><label class="flex gap-3"><input type="checkbox" name="is_active" value="1" checked>Active</label><button class="w-full rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Add testimonial</button></div></form><section class="space-y-4">@forelse($testimonials as $testimonial)<article class="form-section"><div class="flex items-start justify-between gap-4"><div><p class="font-semibold text-slate-950 dark:text-white">{{ $testimonial->client_name }}</p><p class="text-sm text-slate-500">{{ $testimonial->company_name }} {{ $testimonial->designation ? '· '.$testimonial->designation : '' }}</p></div><x-status-badge :status="$testimonial->is_active ? 'active' : 'inactive'"/></div><blockquote class="mt-4 text-sm leading-6 text-slate-600 dark:text-slate-300">“{{ $testimonial->testimonial_text }}”</blockquote><div class="mt-4 flex gap-3"><form method="POST" action="{{ route('cms.testimonials.destroy',$testimonial) }}">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700">Delete</button></form></div></article>@empty<div class="form-section text-sm text-slate-500">No testimonials yet.</div>@endforelse<div>{{ $testimonials->links() }}</div></section></div></div>
@endsection
