@extends('layouts.admin')
@section('title','Trust Metrics') @section('page-title','Trust Content')
@section('breadcrumbs')<span>/</span><a href="{{ route('cms.dashboard') }}">CMS</a><span>/</span><span>Trust Metrics</span>@endsection
@section('content')
<div class="space-y-6">@include('command-center.cms.partials.nav')<form method="POST" action="{{ route('cms.trust-metrics.store') }}" class="form-section">@csrf<h2 class="form-section-title">Add trust metric</h2><div class="mt-5 grid gap-4 md:grid-cols-4"><input name="value" required placeholder="500+"><input name="label" required placeholder="Businesses Served"><input name="icon" placeholder="Icon name"><input type="number" name="sort_order" value="0" placeholder="Sort order"><textarea name="description" rows="3" placeholder="Optional context" class="md:col-span-2"></textarea><label class="flex items-center gap-3"><input type="checkbox" name="show_on_homepage" value="1" checked>Homepage</label><label class="flex items-center gap-3"><input type="checkbox" name="is_active" value="1" checked>Active</label></div><button class="mt-5 rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Add metric</button></form><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@forelse($metrics as $metric)<article class="form-section"><p class="text-3xl font-semibold text-teal-700 dark:text-teal-300">{{ $metric->value }}</p><p class="mt-2 font-semibold text-slate-950 dark:text-white">{{ $metric->label }}</p><p class="mt-2 text-sm text-slate-500">{{ $metric->description }}</p><form method="POST" action="{{ route('cms.trust-metrics.destroy',$metric) }}" class="mt-4">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700">Delete</button></form></article>@empty<p class="text-sm text-slate-500">No trust metrics yet.</p>@endforelse</div><div>{{ $metrics->links() }}</div></div>
@endsection
