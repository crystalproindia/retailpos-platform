@extends('layouts.admin')
@section('title','GST Compliance') @section('page-title','GST Compliance')
@section('content')<div class="mx-auto max-w-6xl space-y-6"><div class="rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm">Accountant review required. RetailPOS prepares data but does not file GST returns or submit e-invoices/e-way bills.</div><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">@foreach(['Taxable'=>$totals->taxable??0,'CGST'=>$totals->cgst??0,'SGST'=>$totals->sgst??0,'IGST'=>$totals->igst??0,'Cess'=>$totals->cess??0] as $label=>$value)<div class="rounded-lg border bg-white p-4"><p class="text-xs text-slate-500">{{ $label }}</p><p class="mt-1 text-xl font-bold">{{ number_format($value,2) }}</p></div>@endforeach</div><div class="flex flex-wrap gap-2"><a class="rounded bg-slate-900 px-3 py-2 text-sm text-white" href="{{ route('compliance.gst.reports.index') }}">Reports</a><a class="rounded border px-3 py-2 text-sm" href="{{ route('compliance.gst.exports.index') }}">Exports</a><a class="rounded border px-3 py-2 text-sm" href="{{ route('compliance.gst.notes.index') }}">Credit/debit notes</a><a class="rounded border px-3 py-2 text-sm" href="{{ route('compliance.gst.guide') }}">Filing guide</a></div>@if($warnings)<p class="text-sm text-amber-700">{{ $warnings }} issued invoice(s) need place-of-supply review.</p>@endif</div>@endsection
