@extends('layouts.admin') @section('title','GST Exports') @section('page-title','GST Exports') @section('content')<div class="mx-auto max-w-5xl space-y-5"><div class="rounded border border-amber-200 bg-amber-50 p-4 text-sm font-medium">Review before filing. RetailPOS does not upload to the GST portal.</div><form method="POST" action="{{ route('compliance.gst.exports.download') }}" class="flex flex-wrap gap-3 rounded bg-white p-5">@csrf<input name="period" placeholder="YYYY-MM" class="rounded border"><select name="format" class="rounded border"><option value="csv">CSV</option><option value="json">JSON</option></select><label class="text-sm"><input type="checkbox" name="draft_export" value="1"> I confirm this is a draft export</label><button class="rounded bg-slate-900 px-4 py-2 text-white">Generate outward supply export</button></form><h2 class="font-semibold">Export history</h2><table class="min-w-full bg-white text-sm"><tbody>@forelse($exports as $export)<tr class="border-t"><td class="p-3">{{ $export->export_type }}</td><td class="p-3">{{ $export->period }}</td><td class="p-3">{{ $export->format }}</td><td class="p-3">{{ $export->created_at }}</td></tr>@empty<tr><td class="p-6 text-slate-500">No exports generated.</td></tr>@endforelse</tbody></table></div>@endsection
