@extends('layouts.admin')
@section('title','GST Credit and Debit Notes')
@section('page-title','GST Notes')
@section('content')
<div class="mx-auto max-w-6xl space-y-6"><section class="rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-950">Create reviewable draft notes only. Issuing, customer delivery, and return filing require authorised accountant review.</section><form method="POST" action="{{ route('compliance.gst.notes.store') }}" class="grid gap-4 rounded-lg border border-slate-200 bg-white p-5 md:grid-cols-2">@csrf<label>Original issued invoice<select name="invoice_id" required class="mt-1 w-full rounded border-slate-300">@foreach($invoices as $invoice)<option value="{{ $invoice->id }}">{{ $invoice->invoice_number }} · {{ $invoice->currency }} {{ $invoice->grand_total }}</option>@endforeach</select></label><label>Type<select name="note_type" class="mt-1 w-full rounded border-slate-300"><option value="credit">Credit note</option><option value="debit">Debit note</option></select></label><label>Taxable value<input name="taxable_value" type="number" step="0.01" min="0.01" required class="mt-1 w-full rounded border-slate-300"></label><label>Grand total<input name="grand_total" type="number" step="0.01" min="0.01" required class="mt-1 w-full rounded border-slate-300"></label><label class="md:col-span-2">Reason<textarea name="reason_description" required class="mt-1 w-full rounded border-slate-300"></textarea></label><div class="md:col-span-2"><button class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Create draft note</button></div></form><section class="overflow-hidden rounded-lg border border-slate-200 bg-white"><table class="min-w-full text-sm"><thead><tr><th class="p-3 text-left">Number</th><th class="p-3 text-left">Type</th><th class="p-3 text-left">Original invoice</th><th class="p-3 text-right">Total</th><th class="p-3">Status</th></tr></thead><tbody>@forelse($notes as $note)<tr class="border-t"><td class="p-3 font-semibold">{{ $note->note_number }}</td><td class="p-3">{{ ucfirst($note->note_type) }}</td><td class="p-3">{{ $note->original_invoice_number }}</td><td class="p-3 text-right">{{ $note->currency }} {{ number_format($note->grand_total,2) }}</td><td class="p-3">{{ ucfirst($note->status) }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">No GST notes yet.</td></tr>@endforelse</tbody></table></section></div>
@endsection
