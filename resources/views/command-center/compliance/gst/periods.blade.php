@extends('layouts.admin') @section('title','GST Return Periods') @section('page-title','GST Return Periods') @section('content')<div class="mx-auto max-w-5xl"><p class="mb-4 rounded border border-amber-200 bg-amber-50 p-4 text-sm">Lock only after accountant review. Reopening requires a recorded reason and privileged access.</p><table class="min-w-full bg-white text-sm"><thead><tr><th class="p-3 text-left">Period</th><th class="p-3">Financial year</th><th class="p-3">Status</th><th class="p-3">Action</th></tr></thead><tbody>@forelse($periods as $period)<tr class="border-t"><td class="p-3">{{ $period->period }}</td><td class="p-3">{{ $period->financial_year }}</td><td class="p-3">{{ $period->status }}</td><td class="p-3"><form method="POST" action="{{ route('compliance.gst.periods.transition',$period) }}">@csrf @method('PUT')<select name="status"><option value="under_review">Start review</option><option value="accountant_approved">Approve</option><option value="locked">Lock</option><option value="open">Reopen</option></select><input name="reason" placeholder="Reason when reopening" class="border"><button class="ml-2 text-teal-700">Update</button></form></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">No return periods created yet.</td></tr>@endforelse</tbody></table></div>@endsection
