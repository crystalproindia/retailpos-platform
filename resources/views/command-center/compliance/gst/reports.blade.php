@extends('layouts.admin') @section('title','GST Reports') @section('page-title','GST Reports') @section('content')<div class="mx-auto max-w-6xl"><div class="mb-5 rounded border border-amber-200 bg-amber-50 p-4 text-sm">Review before filing. Results are tenant-scoped and contain issued CRM invoices only.</div><h1 class="text-xl font-bold">{{ str($report)->replace('-',' ')->title() }}</h1><table class="mt-4 min-w-full bg-white text-sm"><thead><tr><th class="p-3 text-left">Invoice</th><th class="p-3">Date</th><th class="p-3 text-right">Taxable</th><th class="p-3 text-right">Tax</th><th class="p-3 text-right">Total</th></tr></thead><tbody>@forelse($rows as $row)<tr class="border-t"><td class="p-3">{{ $row->invoice_number }}</td><td class="p-3">{{ $row->issue_date }}</td><td class="p-3 text-right">{{ $row->taxable_total }}</td><td class="p-3 text-right">{{ $row->tax_total }}</td><td class="p-3 text-right">{{ $row->grand_total }} {{ $row->currency }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">No reportable documents.</td></tr>@endforelse</tbody></table></div>@endsection
