@extends('layouts.admin')
@section('title', 'Edit Onboarding')
@section('page-title', 'Edit Onboarding')
@section('content')
<div class="space-y-6">@include('command-center.crm.partials.nav')<form method="POST" action="{{ route('crm.onboarding.update', $onboarding) }}" class="grid gap-6 xl:grid-cols-[1.2fr_0.8fr]">@csrf @method('PUT')<section class="space-y-5 rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div><label class="text-sm font-medium">Title</label><input name="title" value="{{ old('title', $onboarding->title) }}" class="mt-1 w-full"></div><div class="grid gap-4 md:grid-cols-2"><div><label class="text-sm font-medium">Customer contact</label><input name="customer_contact_name" value="{{ old('customer_contact_name', $onboarding->customer_contact_name) }}" class="mt-1 w-full"></div><div><label class="text-sm font-medium">Phone</label><input name="customer_contact_phone" value="{{ old('customer_contact_phone', $onboarding->customer_contact_phone) }}" class="mt-1 w-full"></div><div><label class="text-sm font-medium">Email</label><input name="customer_contact_email" value="{{ old('customer_contact_email', $onboarding->customer_contact_email) }}" class="mt-1 w-full"></div><div><label class="text-sm font-medium">Store count</label><input type="number" min="0" name="store_count" value="{{ old('store_count', $onboarding->store_count) }}" class="mt-1 w-full"></div></div><textarea name="notes" rows="4" placeholder="Customer-facing implementation notes" class="w-full">{{ old('notes', $onboarding->notes) }}</textarea><textarea name="internal_remarks" rows="4" placeholder="Internal remarks" class="w-full">{{ old('internal_remarks', $onboarding->internal_remarks) }}</textarea></section><aside class="space-y-4 rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><select name="status" class="w-full">@foreach(\App\Enums\Crm\OnboardingStatus::cases() as $status)<option value="{{ $status->value }}" @selected(old('status', $onboarding->status->value) === $status->value)>{{ $status->label() }}</option>@endforeach</select><select name="priority" class="w-full">@foreach(\App\Enums\Crm\OnboardingPriority::cases() as $priority)<option value="{{ $priority->value }}" @selected(old('priority', $onboarding->priority->value) === $priority->value)>{{ $priority->label() }}</option>@endforeach</select><select name="assigned_to" class="w-full"><option value="">Sales owner</option>@foreach($owners as $owner)<option value="{{ $owner->id }}" @selected((string) old('assigned_to', $onboarding->assigned_to) === (string) $owner->id)>{{ $owner->name }}</option>@endforeach</select><select name="implementation_owner_id" class="w-full"><option value="">Implementation owner</option>@foreach($owners as $owner)<option value="{{ $owner->id }}" @selected((string) old('implementation_owner_id', $onboarding->implementation_owner_id) === (string) $owner->id)>{{ $owner->name }}</option>@endforeach</select><label class="text-xs font-medium text-slate-500">Start date<input type="date" name="start_date" value="{{ old('start_date', $onboarding->start_date?->toDateString()) }}" class="mt-1 w-full"></label><label class="text-xs font-medium text-slate-500">Target go-live<input type="date" name="target_go_live_date" value="{{ old('target_go_live_date', $onboarding->target_go_live_date?->toDateString()) }}" class="mt-1 w-full"></label><button class="w-full rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Save onboarding</button></aside></form></div>
@endsection
