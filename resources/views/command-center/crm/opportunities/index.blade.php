@extends('layouts.admin')

@section('title', 'Sales Opportunities')
@section('page-title', 'Sales Opportunities')

@section('content')
<div class="space-y-6">@include('command-center.crm.partials.nav')
    <section class="rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex flex-col justify-between gap-4 border-b border-slate-200 p-5 sm:flex-row sm:items-center dark:border-slate-800"><div><h1 class="text-xl font-semibold text-slate-950 dark:text-white">Sales opportunities</h1><p class="mt-1 text-sm text-slate-500">Weighted pipeline value is expected value multiplied by probability.</p></div><form class="flex gap-2"><select name="stage" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">All stages</option>@foreach(['qualified' => 'Qualified', 'demo_completed' => 'Demo completed', 'proposal_required' => 'Proposal required', 'quotation_sent' => 'Quotation sent', 'negotiation' => 'Negotiation', 'won' => 'Won', 'lost' => 'Lost'] as $value => $label)<option value="{{ $value }}" @selected(request('stage') === $value)>{{ $label }}</option>@endforeach</select><button class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">Filter</button></form></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500 dark:bg-slate-950"><tr><th class="p-4">Opportunity</th><th class="p-4">Stage</th><th class="p-4">Owner</th><th class="p-4 text-right">Expected / weighted</th><th class="p-4">Close</th><th class="p-4">Move</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($opportunities as $opportunity)<tr><td class="p-4"><p class="font-semibold text-slate-950 dark:text-white">{{ $opportunity->title }}</p><p class="mt-1 text-xs text-slate-500">{{ $opportunity->lead?->business_name ?? $opportunity->lead?->title }}</p></td><td class="p-4"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">{{ str($opportunity->stage)->replace('_', ' ')->headline() }}</span></td><td class="p-4">{{ $opportunity->assignedUser?->name ?? 'Unassigned' }}</td><td class="p-4 text-right">{{ $opportunity->currency }} {{ number_format((float) $opportunity->expected_value, 2) }}<p class="text-xs text-slate-500">{{ $opportunity->currency }} {{ number_format($opportunity->weightedValue(), 2) }}</p></td><td class="p-4">{{ $opportunity->expected_close_date?->format('d M Y') ?? '—' }}</td><td class="p-4"><form method="POST" action="{{ route('sales.opportunities.move', $opportunity) }}" class="flex gap-2">@csrf<select name="stage" class="rounded-lg border border-slate-300 px-2 py-1 text-xs"><option value="qualified">Qualified</option><option value="demo_completed">Demo completed</option><option value="proposal_required">Proposal required</option><option value="quotation_sent">Quotation sent</option><option value="negotiation">Negotiation</option><option value="won">Won</option><option value="lost">Lost</option></select><button class="text-xs font-semibold text-teal-700">Update</button></form></td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-sm text-slate-500">No opportunities yet. Create one from a qualified lead.</td></tr>@endforelse</tbody></table></div>@if($opportunities->hasPages())<div class="p-4">{{ $opportunities->links() }}</div>@endif</section>
</div>
@endsection
