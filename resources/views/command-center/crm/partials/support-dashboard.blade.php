<section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900">
    <div class="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><h2 class="text-base font-semibold text-slate-950 dark:text-white">Customer Support</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">SLA commitments and the customer requests that need attention next.</p></div><a href="{{ route('crm.support.tickets.index') }}" class="text-sm font-semibold text-sky-700 hover:text-sky-900 dark:text-sky-300">Open support desk</a></div>
    <div class="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-5">@foreach ([['Open Tickets', $supportMetrics['open'], 'bg-slate-50 dark:bg-slate-950'], ['Urgent', $supportMetrics['urgent'], 'bg-rose-50 dark:bg-rose-950/30'], ['Overdue', $supportMetrics['overdue'], 'bg-amber-50 dark:bg-amber-950/30'], ['Waiting for Customer', $supportMetrics['waiting_for_customer'], 'bg-sky-50 dark:bg-sky-950/30'], ['Resolved This Month', $supportMetrics['resolved_this_month'], 'bg-teal-50 dark:bg-teal-950/30']] as [$label, $value, $tone])<a href="{{ route('crm.support.tickets.index', $label === 'Overdue' ? ['overdue' => 1] : []) }}" class="support-summary-card rounded-lg border border-slate-200 p-4 transition dark:border-slate-800 {{ $tone }}"><p class="text-sm font-medium text-slate-500 dark:text-slate-400">{{ $label }}</p><p class="mt-2 text-2xl font-semibold text-slate-950 dark:text-white">{{ $value }}</p></a>@endforeach</div>
    <div class="mt-5 grid gap-3 lg:grid-cols-2">@forelse ($supportMetrics['recent'] as $ticket)<a href="{{ route('crm.support.tickets.show', $ticket) }}" class="support-ticket-row rounded-lg border border-slate-200 p-4 transition dark:border-slate-800"><div class="flex items-start justify-between gap-3"><div><p class="text-xs font-semibold text-slate-500">{{ $ticket->ticket_number }}</p><p class="mt-1 font-semibold text-slate-950 dark:text-white">{{ $ticket->subject }}</p><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">{{ $ticket->customer?->company_name ?? $ticket->reported_by_name ?? 'Unlinked request' }}</p></div><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">{{ $ticket->status->label() }}</span></div><p class="mt-3 text-xs text-slate-500">{{ $ticket->assignee?->name ?? 'Unassigned' }} · {{ $ticket->updated_at->diffForHumans() }}</p></a>@empty<p class="lg:col-span-2 rounded-lg border border-dashed border-slate-300 px-4 py-8 text-center text-sm text-slate-500 dark:border-slate-700 dark:text-slate-400">No support tickets have been created yet.</p>@endforelse</div>
</section>
