@extends('layouts.admin') @section('title',$proforma->proforma_number) @section('page-title',$proforma->proforma_number)
@section('content')<div class="space-y-6">@include('command-center.crm.partials.nav')<section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex flex-wrap justify-between gap-3"><div><p class="text-teal-700 dark:text-teal-300">{{$proforma->proforma_number}}</p><h1 class="text-2xl font-semibold">{{$proforma->title}}</h1><p class="text-sm text-slate-500">{{$proforma->customer_company}} · {{$proforma->status?->label()}}</p></div><div class="flex flex-wrap gap-2"><a href="{{route('crm.proformas.pdf',$proforma)}}" class="rounded-lg bg-slate-950 px-3 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Download PDF</a><a href="{{route('crm.proformas.pdf.preview',$proforma)}}" target="_blank" class="rounded-lg border px-3 py-2 text-sm">Preview PDF</a>@if($proforma->status?->value==='draft')<form method="POST" action="{{route('crm.proformas.sent',$proforma)}}">@csrf<button class="rounded-lg border px-3 py-2 text-sm">Mark as sent</button></form>@endif</div></div></section><section class="grid gap-6 lg:grid-cols-[1fr_340px]"><article class="rounded-lg border bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Items and totals</h2><div class="mt-4 space-y-3">@foreach($proforma->items as $item)<div class="flex justify-between border-b pb-3"><span>{{$item->name}} × {{$item->quantity}}</span><strong>{{$proforma->currency}} {{number_format((float)$item->line_total,2)}}</strong></div>@endforeach</div><dl class="ml-auto mt-5 max-w-sm space-y-2"><div class="flex justify-between"><dt>Subtotal</dt><dd>{{$proforma->currency}} {{number_format((float)$proforma->subtotal,2)}}</dd></div><div class="flex justify-between"><dt>Tax</dt><dd>{{number_format((float)$proforma->tax_total,2)}}</dd></div><div class="flex justify-between font-semibold"><dt>Grand Total</dt><dd>{{number_format((float)$proforma->grand_total,2)}}</dd></div><div class="flex justify-between text-teal-700"><dt>Paid</dt><dd>{{number_format((float)$proforma->paid_amount,2)}}</dd></div><div class="flex justify-between font-semibold text-rose-700"><dt>Balance</dt><dd>{{number_format((float)$proforma->balance_amount,2)}}</dd></div></dl></article><aside class="space-y-4"><section class="rounded-lg border bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Record payment</h2><form method="POST" action="{{route('crm.proformas.payments.store',$proforma)}}" class="mt-4 space-y-3">@csrf<input required name="amount" type="number" step="0.01" max="{{$proforma->balance_amount}}" placeholder="Amount" class="w-full rounded-lg border p-2"><input required name="payment_date" type="date" value="{{now()->toDateString()}}" class="w-full rounded-lg border p-2"><select name="payment_mode" class="w-full rounded-lg border p-2">@foreach(['cash','bank_transfer','upi','card','cheque','online','other'] as $m)<option>{{$m}}</option>@endforeach</select><input name="reference_number" placeholder="Reference" class="w-full rounded-lg border p-2"><button class="rounded-lg bg-slate-950 px-3 py-2 text-sm text-white dark:bg-teal-300 dark:text-slate-950">Record payment</button></form></section><section class="rounded-lg border bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Public link</h2>@if($proforma->public_url)<p class="mt-3 break-all text-xs">{{$proforma->public_url}}</p><button data-copy-text="{{$proforma->public_url}}" class="mt-2 text-sm font-semibold text-teal-700">Copy link</button>@else<form method="POST" action="{{route('crm.proformas.public-link',$proforma)}}" class="mt-3">@csrf<button class="rounded-lg border px-3 py-2 text-sm">Generate public link</button></form>@endif</section></aside></section><section class="grid gap-6 lg:grid-cols-2"><article class="rounded-lg border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Payment history</h2>@forelse($proforma->payments as $x)<p class="mt-3 text-sm">{{$x->payment_date?->format('d M Y')}} · {{$x->payment_mode}} · {{$proforma->currency}} {{number_format((float)$x->amount,2)}}</p>@empty<p class="mt-3 text-sm text-slate-500">No payments recorded.</p>@endforelse</article><article class="rounded-lg border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Share history</h2>@forelse($proforma->shares as $x)<p class="mt-3 text-sm">{{$x->channel}} · {{$x->status}}</p>@empty<p class="mt-3 text-sm text-slate-500">No shares recorded.</p>@endforelse</article></section></div>@endsection
