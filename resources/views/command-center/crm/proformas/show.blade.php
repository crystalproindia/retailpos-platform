@extends('layouts.admin')

@section('title', $proforma->proforma_number)
@section('page-title', $proforma->proforma_number)

@section('breadcrumbs')
    <span>/</span><span>CRM</span><span>/</span><a href="{{ route('crm.proformas.index') }}">Proforma Invoices</a><span>/</span><span>{{ $proforma->proforma_number }}</span>
@endsection

@section('content')
    @php
        $statusTone = match ($proforma->status?->tone()) { 'success' => 'bg-teal-100 text-teal-800 dark:bg-teal-900 dark:text-teal-100', 'danger' => 'bg-rose-100 text-rose-800 dark:bg-rose-900 dark:text-rose-100', 'info' => 'bg-sky-100 text-sky-800 dark:bg-sky-900 dark:text-sky-100', default => 'bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-200' };
        $whatsappPayload = session('whatsappPayload');
    @endphp
    <div class="space-y-6">
        @include('command-center.crm.partials.nav')
        <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900">
            <div class="flex flex-col justify-between gap-4 xl:flex-row xl:items-start"><div><div class="flex flex-wrap items-center gap-3"><p class="text-sm font-semibold text-teal-700 dark:text-teal-300">{{ $proforma->proforma_number }}</p><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $statusTone }}">{{ $proforma->status?->label() }}</span></div><h1 class="mt-2 text-2xl font-semibold text-slate-950 dark:text-white">{{ $proforma->title }}</h1><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">{{ $proforma->customer_company ?: $proforma->customer_name ?: 'No customer assigned' }}</p></div><div class="flex flex-wrap gap-2"><a href="{{ route('crm.proformas.pdf', $proforma) }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Download PDF</a><a href="{{ route('crm.proformas.pdf.preview', $proforma) }}" target="_blank" rel="noreferrer" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Preview PDF</a>@if($proforma->status?->value === 'draft')<form method="POST" action="{{ route('crm.proformas.mark-sent', $proforma) }}">@csrf<button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Mark as sent</button></form>@endif</div></div>
        </section>

        @if ($proforma->customer)
            <section class="rounded-lg border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-800 dark:bg-slate-900">
                <div class="flex flex-col justify-between gap-3 sm:flex-row sm:items-center">
                    <div><p class="font-semibold text-slate-950 dark:text-white">Customer onboarding</p><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Start or open the implementation checklist linked to this proforma.</p></div>
                    @if ($proforma->onboarding->first())
                        <a href="{{ route('crm.onboarding.show', $proforma->onboarding->first()) }}" class="rounded-lg border border-teal-300 px-4 py-2 text-sm font-semibold text-teal-700 dark:border-teal-800 dark:text-teal-300">Open Onboarding</a>
                    @else
                        <form method="POST" action="{{ route('crm.proformas.onboarding.start', $proforma) }}">@csrf<button class="rounded-lg border border-teal-300 px-4 py-2 text-sm font-semibold text-teal-700 dark:border-teal-800 dark:text-teal-300">Start Onboarding</button></form>
                    @endif
                </div>
            </section>
        @endif

        <section class="grid gap-4 lg:grid-cols-[1.1fr_0.9fr]">
            <article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><h2 class="text-base font-semibold text-slate-950 dark:text-white">Proforma delivery</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Share a customer-safe proforma without exposing internal CRM remarks.</p></div><span class="text-xs font-semibold uppercase text-slate-500 dark:text-slate-400">{{ $proforma->shares->count() }} share events</span></div><div class="mt-5 flex flex-wrap gap-2"><a href="{{ route('crm.proformas.email.create', $proforma) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Send by Email</a><a href="{{ route('crm.proformas.whatsapp', $proforma) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Send via WhatsApp</a></div>
                @if ($whatsappPayload)<div class="mt-4 rounded-lg border border-teal-200 bg-teal-50 p-4 text-sm dark:border-teal-900/70 dark:bg-teal-950/30"><div class="flex flex-wrap items-center justify-between gap-3"><div><p class="font-semibold text-teal-900 dark:text-teal-100">WhatsApp message prepared</p><p class="mt-1 text-xs text-teal-800 dark:text-teal-200">Opening WhatsApp does not mark this proforma as sent.</p></div>@if($whatsappPayload['url'])<a href="{{ $whatsappPayload['url'] }}" target="_blank" rel="noreferrer" class="rounded-lg bg-teal-700 px-3 py-2 text-sm font-semibold text-white">Open WhatsApp</a>@endif</div><p class="mt-3 whitespace-pre-line text-slate-700 dark:text-slate-200">{{ $whatsappPayload['message'] }}</p><div class="mt-3 flex flex-wrap gap-3"><button type="button" data-copy-text="{{ $whatsappPayload['message'] }}" class="text-sm font-semibold text-teal-800 dark:text-teal-200">Copy WhatsApp Message</button>@if($proforma->public_url)<button type="button" data-copy-text="{{ $proforma->public_url }}" class="text-sm font-semibold text-teal-800 dark:text-teal-200">Copy Public Link</button>@endif</div></div>@endif
            </article>
            <article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="text-base font-semibold text-slate-950 dark:text-white">Public proforma link</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">A private link presents only customer-facing information.</p>@if($proforma->public_url)<div class="mt-4 break-all rounded-lg bg-slate-50 p-3 text-xs text-slate-600 dark:bg-slate-950 dark:text-slate-300">{{ $proforma->public_url }}</div><div class="mt-3 flex flex-wrap gap-3"><a href="{{ $proforma->public_url }}" target="_blank" rel="noreferrer" class="text-sm font-semibold text-teal-700 dark:text-teal-300">Open public view</a><button type="button" data-copy-text="{{ $proforma->public_url }}" class="text-sm font-semibold text-slate-700 dark:text-slate-200">Copy public link</button></div>@else<form method="POST" action="{{ route('crm.proformas.public-link', $proforma) }}" class="mt-4">@csrf<button class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold text-slate-700 dark:border-slate-700 dark:text-slate-200">Generate secure link</button></form>@endif</article>
        </section>

        <section class="grid gap-6 xl:grid-cols-[minmax(0,1fr)_340px]"><div class="space-y-6"><article class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="border-b border-slate-200 px-5 py-4 dark:border-slate-800"><h2 class="font-semibold text-slate-950 dark:text-white">Items and totals</h2></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500 dark:bg-slate-950 dark:text-slate-400"><tr><th class="px-5 py-3">Item</th><th class="px-5 py-3 text-right">Qty</th><th class="px-5 py-3 text-right">Unit price</th><th class="px-5 py-3 text-right">Tax</th><th class="px-5 py-3 text-right">Total</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@foreach($proforma->items as $item)<tr><td class="px-5 py-4"><p class="font-medium text-slate-950 dark:text-white">{{ $item->name }}</p>@if($item->description)<p class="mt-1 text-xs text-slate-500 dark:text-slate-400">{{ $item->description }}</p>@endif</td><td class="px-5 py-4 text-right">{{ number_format((float) $item->quantity, 3) }}</td><td class="px-5 py-4 text-right">{{ number_format((float) $item->unit_price, 2) }}</td><td class="px-5 py-4 text-right">{{ number_format((float) $item->tax_amount, 2) }}</td><td class="px-5 py-4 text-right font-semibold">{{ $proforma->currency }} {{ number_format((float) $item->line_total, 2) }}</td></tr>@endforeach</tbody></table></div><dl class="ml-auto max-w-sm space-y-2 p-5 text-sm"><div class="flex justify-between text-slate-500"><dt>Subtotal</dt><dd>{{ $proforma->currency }} {{ number_format((float) $proforma->subtotal, 2) }}</dd></div><div class="flex justify-between text-slate-500"><dt>Tax</dt><dd>{{ $proforma->currency }} {{ number_format((float) $proforma->tax_total, 2) }}</dd></div><div class="flex justify-between border-t border-slate-200 pt-3 text-lg font-semibold"><dt>Grand total</dt><dd>{{ $proforma->currency }} {{ number_format((float) $proforma->grand_total, 2) }}</dd></div><div class="flex justify-between text-teal-700 dark:text-teal-300"><dt>Paid</dt><dd>{{ $proforma->currency }} {{ number_format((float) $proforma->paid_amount, 2) }}</dd></div><div class="flex justify-between font-semibold text-rose-700 dark:text-rose-300"><dt>Balance</dt><dd>{{ $proforma->currency }} {{ number_format((float) $proforma->balance_amount, 2) }}</dd></div></dl></article><article class="grid gap-6 md:grid-cols-2"><div class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Customer notes</h2><p class="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $proforma->notes ?: 'No customer-facing notes added.' }}</p></div><div class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Terms and conditions</h2><p class="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $proforma->terms_conditions ?: 'No terms added.' }}</p></div></article></div><aside class="space-y-6"><article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Record payment</h2><form method="POST" action="{{ route('crm.proformas.payments.store', $proforma) }}" class="mt-4 space-y-3">@csrf<input required name="amount" type="number" step="0.01" max="{{ $proforma->balance_amount }}" placeholder="Amount" class="w-full rounded-lg border border-slate-300 p-2.5 text-sm dark:border-slate-700 dark:bg-slate-950"><input required name="payment_date" type="date" value="{{ now()->toDateString() }}" class="w-full rounded-lg border border-slate-300 p-2.5 text-sm dark:border-slate-700 dark:bg-slate-950"><select name="payment_mode" class="w-full rounded-lg border border-slate-300 p-2.5 text-sm dark:border-slate-700 dark:bg-slate-950">@foreach(['cash','bank_transfer','upi','card','cheque','online','other'] as $mode)<option>{{ $mode }}</option>@endforeach</select><input name="reference_number" placeholder="Reference" class="w-full rounded-lg border border-slate-300 p-2.5 text-sm dark:border-slate-700 dark:bg-slate-950"><button class="rounded-lg bg-slate-950 px-3 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Record payment</button></form></article><article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Customer</h2><div class="mt-4 space-y-2 text-sm text-slate-600 dark:text-slate-300"><p class="font-medium text-slate-950 dark:text-white">{{ $proforma->customer_name ?: 'No contact name' }}</p><p>{{ $proforma->customer_company }}</p><p>{{ $proforma->customer_email }}</p><p>{{ $proforma->customer_phone }}</p>@if($proforma->billing_address)<p class="whitespace-pre-line pt-2 text-slate-500 dark:text-slate-400">{{ $proforma->billing_address }}</p>@endif</div></article><article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Internal remarks</h2><p class="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $proforma->internal_remarks ?: 'No internal remarks added.' }}</p></article></aside></section>

        <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div><h2 class="text-base font-semibold text-slate-950 dark:text-white">Payment history</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Recorded advance and settlement payments.</p></div><div class="mt-5 space-y-3">
            @forelse($proforma->payments as $payment)
                <div class="flex flex-col justify-between gap-2 rounded-lg border border-slate-200 px-4 py-3 text-sm sm:flex-row sm:items-center dark:border-slate-800"><div><p class="font-medium text-slate-950 dark:text-white">{{ str($payment->payment_mode)->replace('_', ' ')->headline() }} · {{ $proforma->currency }} {{ number_format((float) $payment->amount, 2) }}</p><p class="mt-1 text-xs text-slate-500 dark:text-slate-400">{{ $payment->payment_date?->format('d M Y') }} · {{ $payment->recorder?->name ?? 'System' }}@if($payment->reference_number) · {{ $payment->reference_number }}@endif</p></div></div>
            @empty
                <p class="rounded-lg border border-dashed border-slate-300 px-4 py-8 text-center text-sm text-slate-500 dark:border-slate-700">No payments recorded.</p>
            @endforelse
        </div></section>
        <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div><h2 class="text-base font-semibold text-slate-950 dark:text-white">Share history</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Client delivery preparation and email outcomes.</p></div><div class="mt-5 space-y-3">
            @forelse($proforma->shares as $share)
                @php
                    $shareTone = match ($share->status) { 'sent', 'downloaded' => 'bg-teal-100 text-teal-800 dark:bg-teal-900 dark:text-teal-100', 'failed' => 'bg-rose-100 text-rose-800 dark:bg-rose-900 dark:text-rose-100', 'prepared' => 'bg-sky-100 text-sky-800 dark:bg-sky-900 dark:text-sky-100', default => 'bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-200' };
                    $shareMetadata = $share->metadata ?? [];
                    $shareSummary = $shareMetadata['subject'] ?? (($shareMetadata['has_phone'] ?? true) === false ? 'No valid WhatsApp phone' : null);
                @endphp
                <div class="flex flex-col justify-between gap-3 rounded-lg border border-slate-200 px-4 py-3 text-sm sm:flex-row sm:items-center dark:border-slate-800"><div><p class="font-medium text-slate-950 dark:text-white">{{ str($share->channel)->replace('_', ' ')->headline() }}@if($share->recipient) <span class="font-normal text-slate-500 dark:text-slate-400">to {{ $share->recipient }}</span>@endif</p><p class="mt-1 text-xs text-slate-500 dark:text-slate-400">{{ $share->created_at?->format('d M Y, h:i A') }} by {{ $share->creator?->name ?? 'System' }}@if($shareSummary) · {{ $shareSummary }}@endif</p></div><span class="w-fit rounded-full px-2.5 py-1 text-xs font-semibold {{ $shareTone }}">{{ str($share->status)->headline() }}</span></div>
            @empty
                <p class="rounded-lg border border-dashed border-slate-300 px-4 py-8 text-center text-sm text-slate-500 dark:border-slate-700 dark:text-slate-400">No proforma delivery activity has been recorded yet.</p>
            @endforelse</div></section>
    </div>
@endsection
