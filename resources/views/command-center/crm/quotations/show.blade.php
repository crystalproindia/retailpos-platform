@extends('layouts.admin')

@section('title', $quotation->quotation_number)
@section('page-title', $quotation->quotation_number)

@section('breadcrumbs')
    <span>/</span><span>CRM</span><span>/</span><a href="{{ route('crm.quotations.index') }}">Quotations</a><span>/</span><span>{{ $quotation->quotation_number }}</span>
@endsection

@section('content')
    @php($statusTone = match($quotation->status?->tone()) { 'success' => 'bg-teal-100 text-teal-800 dark:bg-teal-900 dark:text-teal-100', 'danger' => 'bg-rose-100 text-rose-800 dark:bg-rose-900 dark:text-rose-100', 'info' => 'bg-sky-100 text-sky-800 dark:bg-sky-900 dark:text-sky-100', default => 'bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-200' })
    <div class="space-y-6">
        @include('command-center.crm.partials.nav')
        <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex flex-col justify-between gap-4 xl:flex-row xl:items-start"><div><div class="flex flex-wrap items-center gap-3"><p class="text-sm font-semibold text-teal-700 dark:text-teal-300">{{ $quotation->quotation_number }}</p><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $statusTone }}">{{ $quotation->status?->label() }}</span></div><h1 class="mt-2 text-2xl font-semibold text-slate-950 dark:text-white">{{ $quotation->title }}</h1><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">Linked to <a href="{{ route('crm.leads.show', $quotation->lead) }}" class="font-semibold text-teal-700 dark:text-teal-300">{{ $quotation->lead?->business_name ?? $quotation->lead?->title }}</a></p></div><div class="flex flex-wrap gap-2">@if ($quotation->status?->isEditable()) @can('crm.quotations.update')<a href="{{ route('crm.quotations.edit', $quotation) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Edit draft</a>@endcan @can('crm.quotations.send')<form method="POST" action="{{ route('crm.quotations.send', $quotation) }}">@csrf<button class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Mark sent</button></form>@endcan @endif @if ($quotation->status?->value === 'sent') @can('crm.quotations.accept')<form method="POST" action="{{ route('crm.quotations.accept', $quotation) }}">@csrf<button class="rounded-lg bg-teal-700 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Mark accepted</button></form>@endcan @can('crm.quotations.reject')<form method="POST" action="{{ route('crm.quotations.reject', $quotation) }}">@csrf<button class="rounded-lg border border-rose-200 px-4 py-2 text-sm font-semibold text-rose-700 hover:bg-rose-50 dark:border-rose-900 dark:text-rose-300">Mark rejected</button></form>@endcan @endif @if ($quotation->status?->value === 'accepted')<form method="POST" action="{{ route('crm.quotations.convert', $quotation) }}">@csrf<button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 dark:border-slate-700 dark:text-slate-200">Prepare customer conversion</button></form>@endif</div></div></section>
        @if ($quotation->crmCustomer)
            <section class="rounded-lg border border-teal-200 bg-teal-50 p-5 shadow-sm dark:border-teal-900/70 dark:bg-teal-950/30">
                <div class="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><p class="text-sm font-semibold text-teal-800 dark:text-teal-200">Converted CRM customer</p><p class="mt-1 text-sm text-slate-600 dark:text-slate-300">{{ $quotation->crmCustomer->customer_code }} · {{ $quotation->crmCustomer->company_name }}</p></div><a href="{{ route('crm.customers.show', $quotation->crmCustomer) }}" class="inline-flex w-fit items-center justify-center rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">View customer</a></div>
            </section>
        @endif
        @if ($quotation->status?->value === 'accepted')
            <section class="rounded-lg border border-teal-200 bg-teal-50 p-4 dark:border-teal-900/70 dark:bg-teal-950/30"><div class="flex items-center justify-between gap-3"><div><p class="font-semibold text-slate-950 dark:text-white">Ready for advance payment tracking</p><p class="mt-1 text-sm text-slate-600 dark:text-slate-300">Create a proforma invoice from this accepted proposal.</p></div><a href="{{ route('crm.proformas.create-from-quotation', $quotation) }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Create Proforma Invoice</a></div></section>
        @endif
        <section class="grid gap-4 lg:grid-cols-[1.1fr_0.9fr]">
            <article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900">
                <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><h2 class="text-base font-semibold text-slate-950 dark:text-white">Proposal delivery</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Share a client-ready proposal without exposing internal CRM information.</p></div><span class="text-xs font-semibold uppercase text-slate-500 dark:text-slate-400">{{ $quotation->shares->count() }} share events</span></div>
                <div class="mt-5 flex flex-wrap gap-2"><a href="{{ route('crm.quotations.pdf.download', $quotation) }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Download PDF</a><a href="{{ route('crm.quotations.pdf.preview', $quotation) }}" target="_blank" rel="noreferrer" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Preview PDF</a><a href="{{ route('crm.quotations.email.create', $quotation) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Send by Email</a><a href="{{ route('crm.quotations.whatsapp', $quotation) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Open WhatsApp</a>@if ($quotation->public_url)<button type="button" data-copy-text="{{ $whatsappShare['message'] }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200 dark:hover:bg-slate-800">Copy WhatsApp Message</button>@endif</div>
                @if (session('whatsappMessage'))<div class="mt-4 rounded-lg bg-amber-50 p-3 text-sm text-amber-800 dark:bg-amber-950/30 dark:text-amber-200"><p class="font-semibold">Copy the prepared WhatsApp message</p><p class="mt-1 whitespace-pre-line">{{ session('whatsappMessage') }}</p></div>@endif
            </article>
            <article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex items-start justify-between gap-4"><div><h2 class="text-base font-semibold text-slate-950 dark:text-white">Public proposal link</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">@if ($quotation->public_url){{ $quotation->valid_until?->isPast() ? 'Link expired' : 'Link active' }}@else Link not generated @endif</p></div>@if($quotation->public_url)<span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $quotation->valid_until?->isPast() ? 'bg-amber-100 text-amber-800 dark:bg-amber-950 dark:text-amber-200' : 'bg-teal-100 text-teal-800 dark:bg-teal-950 dark:text-teal-200' }}">{{ $quotation->valid_until?->isPast() ? 'Expired' : 'Active' }}</span>@endif</div>@if ($quotation->public_url)<div class="mt-4 break-all rounded-lg bg-slate-50 p-3 text-xs text-slate-600 dark:bg-slate-950 dark:text-slate-300">{{ $quotation->public_url }}</div><div class="mt-3 flex flex-wrap gap-3"><a href="{{ $quotation->public_url }}" target="_blank" rel="noreferrer" class="text-sm font-semibold text-teal-700 dark:text-teal-300">Open public view</a><button type="button" data-copy-text="{{ $quotation->public_url }}" class="text-sm font-semibold text-slate-700 dark:text-slate-200">Copy public link</button><form method="POST" action="{{ route('crm.quotations.public-link', $quotation) }}">@csrf<input type="hidden" name="regenerate" value="1"><button class="text-sm font-semibold text-slate-700 dark:text-slate-200">Regenerate link</button></form></div>@else<form method="POST" action="{{ route('crm.quotations.public-link', $quotation) }}" class="mt-4">@csrf<button class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold text-slate-700 dark:border-slate-700 dark:text-slate-200">Generate public link</button></form>@endif</article>
        </section>
        <section class="grid gap-6 xl:grid-cols-[minmax(0,1fr)_340px]"><div class="space-y-6"><article class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="border-b border-slate-200 px-5 py-4 dark:border-slate-800"><h2 class="font-semibold text-slate-950 dark:text-white">Items and totals</h2></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500 dark:bg-slate-950 dark:text-slate-400"><tr><th class="px-5 py-3">Item</th><th class="px-5 py-3 text-right">Qty</th><th class="px-5 py-3 text-right">Unit price</th><th class="px-5 py-3 text-right">Discount</th><th class="px-5 py-3 text-right">Tax</th><th class="px-5 py-3 text-right">Total</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@foreach ($quotation->items as $item)<tr><td class="px-5 py-4"><p class="font-medium text-slate-950 dark:text-white">{{ $item->name }}</p>@if($item->description)<p class="mt-1 text-xs text-slate-500 dark:text-slate-400">{{ $item->description }}</p>@endif</td><td class="px-5 py-4 text-right text-slate-600 dark:text-slate-300">{{ number_format((float) $item->quantity, 3) }}</td><td class="px-5 py-4 text-right text-slate-600 dark:text-slate-300">{{ number_format((float) $item->unit_price, 2) }}</td><td class="px-5 py-4 text-right text-slate-600 dark:text-slate-300">{{ number_format((float) $item->discount_amount, 2) }}</td><td class="px-5 py-4 text-right text-slate-600 dark:text-slate-300">{{ number_format((float) $item->tax_amount, 2) }} <span class="text-xs">({{ number_format((float) $item->tax_rate, 3) }}%)</span></td><td class="px-5 py-4 text-right font-semibold text-slate-950 dark:text-white">{{ number_format((float) $item->line_total, 2) }}</td></tr>@endforeach</tbody></table></div><div class="ml-auto max-w-sm space-y-2 p-5 text-sm"><div class="flex justify-between text-slate-500 dark:text-slate-400"><span>Subtotal</span><span>{{ $quotation->currency }} {{ number_format((float) $quotation->subtotal, 2) }}</span></div><div class="flex justify-between text-slate-500 dark:text-slate-400"><span>Discount</span><span>{{ $quotation->currency }} {{ number_format((float) $quotation->discount_total, 2) }}</span></div><div class="flex justify-between text-slate-500 dark:text-slate-400"><span>Tax</span><span>{{ $quotation->currency }} {{ number_format((float) $quotation->tax_total, 2) }}</span></div><div class="flex justify-between border-t border-slate-200 pt-3 text-lg font-semibold text-slate-950 dark:border-slate-800 dark:text-white"><span>Grand total</span><span>{{ $quotation->currency }} {{ number_format((float) $quotation->grand_total, 2) }}</span></div></div></article><article class="grid gap-6 md:grid-cols-2"><div class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Notes</h2><p class="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $quotation->notes ?: 'No customer-facing notes added.' }}</p></div><div class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Terms and conditions</h2><p class="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $quotation->terms_conditions ?: 'No terms added.' }}</p></div></article><article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Quotation history</h2><div class="mt-4 space-y-3">@forelse($quotation->auditLogs as $auditLog)<div class="rounded-lg border border-slate-200 px-4 py-3 dark:border-slate-800"><p class="text-sm font-medium text-slate-950 dark:text-white">{{ $auditLog->description }}</p><p class="mt-1 text-xs text-slate-500 dark:text-slate-400">{{ $auditLog->created_at?->format('d M Y, h:i A') }} by {{ $auditLog->user?->name ?? 'System' }}</p></div>@empty<p class="text-sm text-slate-500 dark:text-slate-400">No quotation history recorded yet.</p>@endforelse</div></article></div><aside class="space-y-6"><article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Customer</h2><div class="mt-4 space-y-2 text-sm text-slate-600 dark:text-slate-300"><p class="font-medium text-slate-950 dark:text-white">{{ $quotation->customer_name ?? 'No contact name' }}</p><p>{{ $quotation->customer_company }}</p><p>{{ $quotation->customer_email }}</p><p>{{ $quotation->customer_phone }}</p>@if($quotation->billing_address)<p class="whitespace-pre-line pt-2 text-slate-500 dark:text-slate-400">{{ $quotation->billing_address }}</p>@endif</div></article><article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Sharing</h2><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">A private link presents only customer-facing information.</p>@if($quotation->public_url)<div class="mt-4 rounded-lg bg-slate-50 p-3 text-xs break-all text-slate-600 dark:bg-slate-950 dark:text-slate-300">{{ $quotation->public_url }}</div><div class="mt-3 flex flex-wrap gap-2"><a href="{{ $quotation->public_url }}" target="_blank" rel="noreferrer" class="text-sm font-semibold text-teal-700 dark:text-teal-300">Open public view</a><button type="button" data-copy-text="{{ $quotation->public_url }}" class="text-sm font-semibold text-slate-700 dark:text-slate-200">Copy link</button></div>@else @can('crm.quotations.update')<form method="POST" action="{{ route('crm.quotations.public-link', $quotation) }}" class="mt-4">@csrf<button class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold text-slate-700 dark:border-slate-700 dark:text-slate-200">Generate secure link</button></form>@endcan @endif</article><article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Internal remarks</h2><p class="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $quotation->internal_remarks ?: 'No internal remarks added.' }}</p></article></aside></section>
        <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex items-center justify-between gap-4"><div><h2 class="text-base font-semibold text-slate-950 dark:text-white">Share history</h2><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Client delivery activity for this proposal.</p></div></div><div class="mt-5 space-y-3">@forelse ($quotation->shares as $share)<div class="flex flex-col justify-between gap-2 rounded-lg border border-slate-200 px-4 py-3 text-sm sm:flex-row sm:items-center dark:border-slate-800"><div><p class="font-medium text-slate-950 dark:text-white">{{ str($share->channel)->replace('_', ' ')->headline() }}</p><p class="mt-1 text-xs text-slate-500 dark:text-slate-400">{{ $share->recipient ?? 'No recipient recorded' }} · {{ $share->created_at?->format('d M Y, h:i A') }} by {{ $share->creator?->name ?? 'System' }}</p></div><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">{{ str($share->status)->headline() }}</span></div>@empty<p class="rounded-lg border border-dashed border-slate-300 px-4 py-8 text-center text-sm text-slate-500 dark:border-slate-700 dark:text-slate-400">No proposal delivery activity has been recorded yet.</p>@endforelse</div></section>
    </div>
@endsection
