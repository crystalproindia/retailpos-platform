@php($maximum = max(1, $items->max($valueKey ?? 'value')))
<div class="mt-4 space-y-3">@forelse($items as $item)@php($value = $item->{$valueKey ?? 'value'})<div><div class="flex justify-between gap-3 text-sm"><span class="font-medium text-slate-700 dark:text-slate-200">{{ str($item->label)->replace('_',' ')->headline() }}</span><span class="text-slate-500">{{ number_format((float)$value, 0) }}</span></div><div class="mt-2 h-2 rounded-full bg-slate-100 dark:bg-slate-800"><div class="h-full rounded-full bg-teal-500" style="width: {{ ($value / $maximum) * 100 }}%"></div></div></div>@empty<p class="text-sm text-slate-500">No report data in this period.</p>@endforelse</div>
