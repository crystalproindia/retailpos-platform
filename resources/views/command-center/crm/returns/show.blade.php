@extends('layouts.admin')
@section('title', $return->credit_note_number)
@section('page-title', 'Credit Note')
@section('content')
<div class="mx-auto max-w-7xl space-y-6">
    <div class="flex flex-wrap items-center gap-2 text-sm"><a href="{{ route('sales.invoices.show', $return->invoice) }}" class="font-semibold text-teal-700 dark:text-teal-300">{{ $return->invoice->invoice_number }}</a><span class="text-slate-400">/</span><span class="text-slate-500">{{ $return->credit_note_number }}</span></div>
    <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 sm:p-6"><div class="flex flex-col gap-5 lg:flex-row lg:items-start lg:justify-between"><div><div class="flex flex-wrap items-center gap-3"><p class="text-xs font-semibold uppercase text-teal-700 dark:text-teal-300">Credit Note</p><span class="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-semibold text-emerald-800 dark:bg-emerald-950 dark:text-emerald-200">Finalized</span></div><h1 class="mt-2 text-2xl font-semibold text-slate-950 dark:text-white">{{ $return->credit_note_number }}</h1><p class="mt-2 text-sm text-slate-500">Original invoice {{ $return->invoice->invoice_number }} · {{ $return->issue_date->format('d M Y') }}</p></div><div class="flex flex-wrap gap-2"><a target="_blank" rel="noopener" href="{{ route('sales.credit-notes.print', $return) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 dark:border-slate-700 dark:text-slate-200">Print</a><a href="{{ route('sales.credit-notes.pdf', $return) }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Download PDF</a></div></div></section>
    <section class="grid gap-6 lg:grid-cols-[minmax(0,1.4fr)_22rem]"><article class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="border-b border-slate-200 p-5 dark:border-slate-800"><h2 class="font-semibold text-slate-950 dark:text-white">Returned items</h2><p class="mt-1 text-sm text-slate-500">Credited to {{ $return->customer_company_snapshot ?: $return->customer_name_snapshot ?: 'the invoice customer' }}</p></div><div class="divide-y divide-slate-100 dark:divide-slate-800">@foreach($return->items as $item)<div class="grid gap-3 p-5 text-sm sm:grid-cols-[minmax(0,1fr)_auto_auto]"><div><p class="font-semibold text-slate-950 dark:text-white">{{ $item->product_name_snapshot }}</p><p class="mt-1 text-xs text-slate-500">Returned {{ $item->return_quantity }} {{ $item->unit_snapshot }} · {{ str($item->inventory_disposition)->replace('_',' ')->headline() }}</p></div><div><p class="text-xs text-slate-500">Tax reversal</p><p class="mt-1 font-medium">{{ $return->currency }} {{ number_format((float) $item->tax_reversal, 2) }}</p></div><div class="sm:text-right"><p class="text-xs text-slate-500">Credit amount</p><p class="mt-1 font-semibold">{{ $return->currency }} {{ number_format((float) $item->credit_total, 2) }}</p></div></div>@endforeach</div></article><aside class="space-y-6"><article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Credit summary</h2><dl class="mt-4 space-y-2 text-sm"><div class="flex justify-between gap-3"><dt>Gross value</dt><dd>{{ number_format((float) $return->gross_total, 2) }}</dd></div><div class="flex justify-between gap-3"><dt>Discount reversal</dt><dd>{{ number_format((float) $return->discount_total, 2) }}</dd></div><div class="flex justify-between gap-3"><dt>Tax reversal</dt><dd>{{ number_format((float) $return->tax_total, 2) }}</dd></div><div class="flex justify-between gap-3 border-t border-slate-200 pt-2 text-base font-semibold dark:border-slate-700"><dt>Total credit</dt><dd>{{ $return->currency }} {{ number_format((float) $return->credit_total, 2) }}</dd></div></dl></article><article class="rounded-lg border border-amber-200 bg-amber-50 p-5 text-sm dark:border-amber-900 dark:bg-amber-950/20"><h2 class="font-semibold text-amber-950 dark:text-amber-100">Financial effect</h2><p class="mt-2 text-amber-900 dark:text-amber-200">{{ $return->currency }} {{ number_format((float) $return->receivable_credit_applied, 2) }} reduced the invoice balance.</p>@if((float)$return->customer_credit_due > 0)<p class="mt-2 font-semibold text-amber-950 dark:text-amber-100">{{ $return->currency }} {{ number_format((float) $return->customer_credit_due, 2) }} remains due to the customer. No refund was created.</p>@endif</article><article class="rounded-lg border border-slate-200 bg-white p-5 text-sm shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Audit details</h2><dl class="mt-3 space-y-2 text-slate-600 dark:text-slate-300"><div><dt class="text-xs text-slate-500">Reason</dt><dd>{{ str($return->reason_code)->replace('_',' ')->headline() }}</dd></div><div><dt class="text-xs text-slate-500">Finalized by</dt><dd>{{ $return->finalizer?->name }}</dd></div><div><dt class="text-xs text-slate-500">Finalized at</dt><dd>{{ $return->finalized_at->format('d M Y, H:i') }}</dd></div>@if($return->reason_note)<div><dt class="text-xs text-slate-500">Note</dt><dd>{{ $return->reason_note }}</dd></div>@endif</dl></article></aside></section>
</div>
@endsection
