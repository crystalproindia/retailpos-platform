@extends('layouts.admin') @section('title','Customer Groups') @section('page-title','Customer Groups') @section('breadcrumbs')<span>/</span><span>Customers</span><span>/</span><span>Groups</span>@endsection
@section('content')<div class="space-y-6">@include('command-center.customers.partials.nav')<div class="grid gap-6 xl:grid-cols-[0.7fr_1.3fr]"><form method="POST" action="{{ route('customers.groups.store') }}" class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">@csrf<h1 class="text-lg font-semibold">New customer group</h1><div class="mt-5 space-y-4"><input name="name" required placeholder="Group name"><input name="slug" placeholder="Slug (optional)"><textarea name="description" rows="4" placeholder="Description"></textarea><input type="number" step="0.01" name="discount_percentage" placeholder="Discount percentage foundation"><input type="number" step="0.01" name="loyalty_multiplier" placeholder="Loyalty multiplier foundation"><label class="flex gap-3"><input type="checkbox" name="is_default" value="1">Default group</label><label class="flex gap-3"><input type="checkbox" name="is_active" value="1" checked>Active</label><button class="w-full rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Create group</button></div></form><section class="grid gap-4 md:grid-cols-2">@forelse($groups as $group)<article class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between"><div><h2 class="font-semibold text-slate-900">{{ $group->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $group->description }}</p></div>@if($group->is_default)<span class="rounded-full bg-teal-50 px-2 py-1 text-xs font-semibold text-teal-700">Default</span>@endif</div><div class="mt-5 grid grid-cols-2 gap-3 text-sm"><div class="rounded-lg bg-slate-50 p-3"><span class="block text-xs text-slate-500">Members</span><b>{{ $group->members_count }}</b></div><div class="rounded-lg bg-slate-50 p-3"><span class="block text-xs text-slate-500">Loyalty multiplier</span><b>{{ $group->loyalty_multiplier ?: '—' }}</b></div></div><form method="POST" action="{{ route('customers.groups.destroy',$group) }}" class="mt-4">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700">Move to trash</button></form></article>@empty<p class="text-sm text-slate-500">No groups yet.</p>@endforelse<div class="md:col-span-2">{{ $groups->links() }}</div></section></div></div>@endsection
