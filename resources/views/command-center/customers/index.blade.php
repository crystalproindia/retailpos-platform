@extends('layouts.admin')
@section('title','Customers') @section('page-title','Customers')
@section('breadcrumbs')<span>/</span><span>Customers</span><span>/</span><span>Directory</span>@endsection
@section('content')<div class="space-y-6">@include('command-center.customers.partials.nav')<div class="flex flex-col gap-3 sm:flex-row sm:justify-between"><form class="grid gap-3 sm:grid-cols-4"><input name="search" value="{{ request('search') }}" placeholder="Search name, phone, number"><select name="status"><option value="">All statuses</option>@foreach(['active','inactive','lost','blocked'] as $value)<option value="{{ $value }}">{{ str($value)->headline() }}</option>@endforeach</select><select name="group_id"><option value="">All groups</option>@foreach($groups as $group)<option value="{{ $group->id }}">{{ $group->name }}</option>@endforeach</select><button class="rounded-lg border px-4 text-sm font-semibold">Filter</button></form><a href="{{ route('customers.create') }}" class="rounded-lg bg-teal-600 px-4 py-2.5 text-center text-sm font-semibold text-white">New customer</a></div><section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Customer</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Group</th><th class="px-5 py-3">Loyalty</th><th class="px-5 py-3">Value</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($customers as $customer)<tr><td class="px-5 py-4"><a href="{{ route('customers.show',$customer) }}" class="font-semibold text-slate-900">{{ $customer->display_name }}</a><p class="mt-1 text-xs text-slate-500">{{ $customer->customer_number }} · {{ $customer->phone }}</p></td><td class="px-5 py-4"><x-status-badge :status="$customer->status"/></td><td class="px-5 py-4">{{ $customer->groups->first()?->group?->name ?: '—' }}</td><td class="px-5 py-4">{{ $customer->loyaltyAccount?->tier ? str($customer->loyaltyAccount->tier)->headline() : '—' }}</td><td class="px-5 py-4">{{ number_format($customer->total_purchase_amount,2) }}</td><td class="px-5 py-4 text-right"><a href="{{ route('customers.edit',$customer) }}" class="font-semibold text-teal-700">Edit</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No customers found.</td></tr>@endforelse</tbody></table></div><div class="border-t px-5 py-4">{{ $customers->links() }}</div></section></div>@endsection
