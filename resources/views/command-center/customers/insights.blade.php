@extends('layouts.admin') @section('title','Customer Insights') @section('page-title','Customer Insights') @section('breadcrumbs')<span>/</span><span>Customers</span><span>/</span><span>Insights</span>@endsection
@section('content')<div class="space-y-6">@include('command-center.customers.partials.nav')<section class="flex items-end justify-between rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div><p class="text-xs font-semibold uppercase tracking-[0.12em] text-teal-700">Rule-based only</p><h1 class="mt-2 text-xl font-semibold">Customer intelligence</h1><p class="mt-2 text-sm text-slate-600">Scores are transparent heuristics based on available customer foundation values, not AI predictions.</p></div><form method="POST" action="{{ route('customers.insights.refresh') }}">@csrf<button class="rounded-lg bg-teal-600 px-4 py-2.5 text-sm font-semibold text-white">Refresh insights</button></form></section><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@foreach($customers as $customer)<article class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex justify-between"><a href="{{ route('customers.show',$customer) }}" class="font-semibold text-slate-900">{{ $customer->display_name }}</a><span class="rounded-full bg-slate-100 px-2 py-1 text-xs">{{ $customer->insight->segment_label }}</span></div><div class="mt-5 grid grid-cols-3 gap-2 text-center text-sm"><div class="rounded-lg bg-slate-50 p-3"><span class="block text-xs text-slate-500">Value</span><b>{{ $customer->insight->customer_value_score }}</b></div><div class="rounded-lg bg-slate-50 p-3"><span class="block text-xs text-slate-500">Retention</span><b>{{ $customer->insight->retention_risk_score }}</b></div><div class="rounded-lg bg-slate-50 p-3"><span class="block text-xs text-slate-500">Returns</span><b>{{ $customer->insight->return_risk_score }}</b></div></div></article>@endforeach</div><div>{{ $customers->links() }}</div></div>@endsection
