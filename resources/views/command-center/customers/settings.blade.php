@extends('layouts.admin') @section('title','Customer Settings') @section('page-title','Customer Settings') @section('breadcrumbs')<span>/</span><span>Customers</span><span>/</span><span>Settings</span>@endsection
@section('content')<div class="space-y-6">@include('command-center.customers.partials.nav')<form method="POST" action="{{ route('customers.settings.update') }}" class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">@csrf @method('PUT')<h1 class="text-xl font-semibold">Customer intelligence settings</h1><p class="mt-2 text-sm text-slate-600">Thresholds drive rule-based reminders and retention signals until POS history is connected.</p><div class="mt-6 grid gap-5 md:grid-cols-2 xl:grid-cols-3">@foreach(['customer_number_prefix'=>'Customer number prefix','next_customer_number'=>'Next customer number','birthday_reminder_days_before'=>'Birthday reminder days','inactive_customer_days'=>'Inactive customer days','lost_customer_days'=>'Lost customer days','frequent_return_threshold_count'=>'Frequent return count','frequent_return_threshold_days'=>'Frequent return window days','loyalty_points_per_amount'=>'Points per amount','loyalty_amount_per_point'=>'Amount per point'] as $field=>$label)<label class="text-sm font-semibold">{{ $label }}<input name="{{ $field }}" value="{{ old($field,$settings->{$field}) }}" class="mt-2 block w-full"></label>@endforeach<label class="text-sm font-semibold">Default group<select name="default_customer_group_id" class="mt-2 block w-full"><option value="">No default group</option>@foreach($groups as $group)<option value="{{ $group->id }}" @selected($settings->default_customer_group_id===$group->id)>{{ $group->name }}</option>@endforeach</select></label></div><div class="mt-6 flex flex-wrap gap-3">@foreach(['loyalty_enabled'=>'Enable loyalty','wallet_enabled'=>'Enable wallet','allow_negative_wallet'=>'Allow negative wallet'] as $field=>$label)<label class="flex min-h-11 items-center gap-3 rounded-lg border border-slate-200 bg-slate-50 px-3 text-sm font-medium"><input type="hidden" name="{{ $field }}" value="0"><input type="checkbox" name="{{ $field }}" value="1" @checked($settings->{$field})>{{ $label }}</label>@endforeach</div><div class="sticky bottom-3 mt-6 flex justify-end rounded-xl border border-slate-200 bg-white/95 p-3 shadow-lg"><button class="rounded-lg bg-teal-600 px-5 py-2.5 text-sm font-semibold text-white">Save settings</button></div></form></div>@endsection
