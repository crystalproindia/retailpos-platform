@extends('layouts.admin') @section('title',$customer->display_name) @section('page-title','Customer Profile') @section('breadcrumbs')<span>/</span><a href="{{ route('customers.index') }}">Customers</a><span>/</span><span>{{ $customer->display_name }}</span>@endsection
@section('content')<div class="space-y-6">@include('command-center.customers.partials.nav')<section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p class="text-xs font-semibold uppercase tracking-[0.12em] text-teal-700">{{ $customer->customer_number }}</p><h1 class="mt-2 text-2xl font-semibold text-slate-900">{{ $customer->display_name }}</h1><p class="mt-2 text-sm text-slate-500">{{ $customer->phone ?: 'No phone' }} · {{ $customer->email ?: 'No email' }}</p></div><div class="flex gap-2"><x-status-badge :status="$customer->status"/><a href="{{ route('customers.edit',$customer) }}" class="rounded-lg border px-4 py-2 text-sm font-semibold">Edit</a></div></div></section><div class="grid gap-5 md:grid-cols-2 xl:grid-cols-4">@foreach(['Loyalty points'=>$customer->loyalty_points_balance,'Wallet balance'=>number_format($customer->wallet_balance,2),'Purchase value'=>number_format($customer->total_purchase_amount,2),'Return amount'=>number_format($customer->total_return_amount,2)] as $label=>$value)<article class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-2 text-2xl font-semibold text-slate-900">{{ $value }}</p></article>@endforeach</div><div class="grid gap-6 xl:grid-cols-[1.1fr_0.9fr]"><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold text-slate-900">Rule-based intelligence</h2><div class="mt-5 grid grid-cols-3 gap-3">@foreach(['Value score'=>$customer->insight?->customer_value_score,'Retention risk'=>$customer->insight?->retention_risk_score,'Return risk'=>$customer->insight?->return_risk_score] as $label=>$value)<div class="rounded-lg bg-slate-50 p-4"><p class="text-xs text-slate-500">{{ $label }}</p><p class="mt-2 text-xl font-semibold">{{ $value ?? '—' }}</p></div>@endforeach</div><p class="mt-4 text-sm text-slate-500">Purchase, return, invoice, and promotion history will be linked by future POS workflows.</p><h3 class="mt-6 font-semibold">Activity timeline</h3><div class="mt-3 space-y-3">@forelse($customer->activities as $activity)<div class="rounded-lg border border-slate-100 p-3"><p class="text-sm font-semibold">{{ $activity->title }}</p><p class="mt-1 text-xs text-slate-500">{{ $activity->occurred_at->diffForHumans() }}</p></div>@empty<p class="text-sm text-slate-500">No activity yet.</p>@endforelse</div></section><aside class="space-y-5"><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold">Groups</h2><div class="mt-3 flex flex-wrap gap-2">@forelse($customer->groups as $member)<span class="rounded-full bg-teal-50 px-3 py-1 text-sm font-semibold text-teal-700">{{ $member->group->name }}</span>@empty<span class="text-sm text-slate-500">No groups assigned.</span>@endforelse</div></section><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold">Addresses and contacts</h2><div class="mt-3 space-y-2 text-sm text-slate-600">@foreach($customer->addresses as $address)<p>{{ $address->type }}: {{ $address->address_line_1 }}, {{ $address->city }}</p>@endforeach @foreach($customer->contacts as $contact)<p>{{ $contact->name }}{{ $contact->phone ? ' · '.$contact->phone : '' }}</p>@endforeach</div></section></aside></div></div>@endsection
