@extends('layouts.admin') @section('title',$category->exists?'Edit category':'Add category') @section('page-title','Expense Category')
@section('content') @include('command-center.finance.partials.nav')<form method="post" action="{{ $category->exists?route('finance.expense-categories.update',$category):route('finance.expense-categories.store') }}" class="mx-auto max-w-xl space-y-5">@csrf @if($category->exists) @method('PUT') @endif<h1 class="text-2xl font-semibold text-slate-950 dark:text-white">{{ $category->exists?'Edit':'Add' }} expense category</h1><section class="space-y-4 rounded-lg border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><label class="block">Name<input required name="name" value="{{ old('name',$category->name) }}" class="mt-1 w-full rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="block">Classification<select name="classification" class="mt-1 w-full rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950">@foreach(['operating_expense'=>'Operating Expense','other_expense'=>'Other Expense','other_income'=>'Other Income'] as $key=>$label)<option value="{{ $key }}" @selected(old('classification',$category->classification)===$key)>{{ $label }}</option>@endforeach</select></label><label class="flex gap-2"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$category->is_active))> Active for new expenses</label></section><button class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Save category</button></form>@endsection
