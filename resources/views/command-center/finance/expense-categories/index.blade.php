@extends('layouts.admin') @section('title','Expense Categories') @section('page-title','Expense Categories')
@section('content') @include('command-center.finance.partials.nav')<div class="space-y-5"><header class="flex justify-between"><div><h1 class="text-2xl font-semibold text-slate-950 dark:text-white">Expense Categories</h1><p class="text-sm text-slate-500">Classification controls where new transactions appear in Profit & Loss.</p></div><a href="{{ route('finance.expense-categories.create') }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Add category</a></header><section class="rounded-lg border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div class="divide-y dark:divide-slate-800">@foreach($categories as $category)<div class="flex items-center justify-between gap-4 p-4"><div><p class="font-semibold text-slate-950 dark:text-white">{{ $category->name }} @if($category->is_system)<span class="text-xs text-slate-500">Default</span>@endif</p><p class="text-sm text-slate-500">{{ str($category->classification)->headline() }} · {{ $category->transactions_count }} transactions · {{ $category->is_active?'Active':'Inactive' }}</p></div><a href="{{ route('finance.expense-categories.edit',$category) }}" class="text-sm font-semibold text-teal-700 dark:text-teal-300">Edit</a></div>@endforeach</div><div class="p-4">{{ $categories->links() }}</div></section></div>@endsection
