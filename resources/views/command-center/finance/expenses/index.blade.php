@extends('layouts.admin')
@section('title','Expenses') @section('page-title','Expenses')
@section('content')
@include('command-center.finance.partials.nav')
<div class="space-y-6"><header class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-teal-700 dark:text-teal-300">Financial ledger</p><h1 class="text-2xl font-semibold text-slate-950 dark:text-white">Expenses</h1><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Drafts are editable. Posted records are retained as financial evidence.</p></div>@can('finance.expenses.create')<a href="{{ route('finance.expenses.create') }}" class="rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Add expense</a>@endcan</header>
<form class="grid gap-3 rounded-lg border border-slate-200 bg-white p-4 sm:grid-cols-2 xl:grid-cols-5 dark:border-slate-800 dark:bg-slate-900"><input name="search" value="{{ request('search') }}" placeholder="Payee or reference" class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"><select name="category_id" class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category_id')==$category->id)>{{ $category->name }}</option>@endforeach</select><select name="status" class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"><option value="">All statuses</option>@foreach(['draft','posted','reversed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><select name="outlet_id" class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"><option value="">All permitted outlets</option>@foreach($outlets as $outlet)<option value="{{ $outlet->id }}" @selected(request('outlet_id')==$outlet->id)>{{ $outlet->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Filter</button></form>
<section class="overflow-hidden rounded-lg border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div class="overflow-x-auto"><table class="min-w-[760px] text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500 dark:bg-slate-950 dark:text-slate-400"><tr><th class="px-4 py-3 whitespace-nowrap">Date</th><th class="px-4 py-3 whitespace-nowrap">Category</th><th class="px-4 py-3 whitespace-nowrap">Outlet</th><th class="px-4 py-3 whitespace-nowrap">Payee</th><th class="px-4 py-3 whitespace-nowrap">Amount</th><th class="px-4 py-3 whitespace-nowrap">Status</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($entries as $entry)<tr class="hover:bg-slate-50 dark:hover:bg-slate-800/50"><td class="px-4 py-4 whitespace-nowrap">{{ $entry->transaction_date?->format('d M Y') }}</td><td class="px-4 py-4"><p class="font-medium text-slate-950 dark:text-white">{{ $entry->category->name }}</p><p class="text-xs text-slate-500">{{ str($entry->classification_snapshot)->headline() }}</p></td><td class="px-4 py-4">{{ $entry->branch?->name ?? 'Company-wide' }}</td><td class="px-4 py-4">{{ $entry->payee ?: '—' }}</td><td class="px-4 py-4 font-semibold whitespace-nowrap">{{ $entry->currency }} {{ number_format((float)$entry->amount,2) }}</td><td class="px-4 py-4"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold dark:bg-slate-800">{{ ucfirst($entry->status) }}</span></td><td class="px-4 py-4"><a class="font-semibold text-teal-700 dark:text-teal-300" href="{{ route('finance.expenses.show',$entry) }}">View</a></td></tr>@empty<tr><td colspan="7" class="p-10 text-center text-slate-500">No expense transactions match these authorized filters.</td></tr>@endforelse</tbody></table></div>@if($entries->hasPages())<div class="border-t p-4 dark:border-slate-800">{{ $entries->links() }}</div>@endif</section></div>
@endsection
