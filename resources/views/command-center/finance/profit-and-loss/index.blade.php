@extends('layouts.admin')
@section('title','Profit & Loss') @section('page-title','Profit & Loss')
@section('content')
@include('command-center.finance.partials.nav')
<div class="space-y-6">
 <header class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-teal-700 dark:text-teal-300">Financial performance</p><h1 class="text-2xl font-semibold text-slate-950 dark:text-white">Profit &amp; Loss</h1><p class="text-sm text-slate-500">{{ $report['period']['from'] }} to {{ $report['period']['to'] }} · {{ $scope['label'] }}</p></div><div class="flex gap-2">@can('finance.profit_and_loss.export')<a href="{{ route('finance.profit-and-loss.csv',request()->query()) }}" class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold dark:border-slate-700">CSV</a><a href="{{ route('finance.profit-and-loss.pdf',request()->query()) }}" class="rounded-lg bg-slate-950 px-3 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">PDF</a>@endcan</div></header>
 <form class="grid gap-3 rounded-lg border border-slate-200 bg-white p-4 sm:grid-cols-3 dark:border-slate-800 dark:bg-slate-900"><select name="period" class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950">@foreach(['today'=>'Today','this_week'=>'This week','this_month'=>'This month','last_month'=>'Last month','financial_year'=>'Financial year','custom'=>'Custom range'] as $key=>$label)<option value="{{ $key }}" @selected(request('period','this_month')===$key)>{{ $label }}</option>@endforeach</select><select name="outlet_id" class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950">@if(auth()->user()->isAdministrator())<option value="all">Company / Consolidated</option>@endif @foreach($outlets as $outlet)<option value="{{ $outlet->id }}" @selected(request('outlet_id')==$outlet->id)>{{ $outlet->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-950 px-4 py-2 font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Apply</button><input type="date" name="date_from" value="{{ request('date_from') }}" class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"><input type="date" name="date_to" value="{{ request('date_to') }}" class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"></form>
 <section class="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Net Sales',$report['net_sales']],['Gross Profit',$report['gross_profit']],['Operating Expenses',$report['operating_expenses']],['Net Profit',$report['net_profit']]] as [$label,$value])<article class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p class="text-xs font-semibold uppercase text-slate-500">{{ $label }}</p><p class="mt-2 text-2xl font-semibold text-slate-950 dark:text-white">₹{{ number_format($value/100,2) }}</p></article>@endforeach</section>
 <section class="rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900"><div class="space-y-3 text-sm"><h2 class="font-semibold text-slate-950 dark:text-white">Revenue</h2><div class="flex justify-between"><span>Gross Sales</span><span>₹{{ number_format($report['gross_sales']/100,2) }}</span></div><div class="flex justify-between"><span>Less: Returns / Credits</span><span>₹{{ number_format($report['returns_credits']/100,2) }}</span></div><div class="flex justify-between border-t pt-3 font-semibold"><span>Net Sales</span><span>₹{{ number_format($report['net_sales']/100,2) }}</span></div><h2 class="pt-4 font-semibold text-slate-950 dark:text-white">Cost of Sales</h2><div class="flex justify-between"><span>COGS</span><span>₹{{ number_format($report['cogs']/100,2) }}</span></div><div class="flex justify-between border-t pt-3 font-semibold"><span>Gross Profit <small class="font-normal text-slate-500">{{ $report['gross_margin_percent'] ?? '—' }}%</small></span><span>₹{{ number_format($report['gross_profit']/100,2) }}</span></div><h2 class="pt-4 font-semibold text-slate-950 dark:text-white">Operating Expenses</h2>@foreach($report['operating_expenses_by_category'] as $row)<a href="{{ route('finance.expenses.index',array_merge(request()->query(),['category_id'=>$row['id']])) }}" class="flex justify-between text-teal-700 dark:text-teal-300"><span>{{ $row['category'] }}</span><span>₹{{ number_format($row['amount']/100,2) }}</span></a>@endforeach<div class="flex justify-between border-t pt-3 font-semibold"><span>Total Operating Expenses</span><span>₹{{ number_format($report['operating_expenses']/100,2) }}</span></div><div class="flex justify-between border-t pt-3 text-base font-semibold"><span>Operating Profit <small class="font-normal text-slate-500">{{ $report['operating_margin_percent'] ?? '—' }}%</small></span><span>₹{{ number_format($report['operating_profit']/100,2) }}</span></div><div class="flex justify-between pt-4"><span>Other Income</span><span>₹{{ number_format($report['other_income']/100,2) }}</span></div><div class="flex justify-between"><span>Other Expenses</span><span>₹{{ number_format($report['other_expenses']/100,2) }}</span></div><div class="flex justify-between border-t pt-4 text-lg font-bold text-teal-700 dark:text-teal-300"><span>NET PROFIT <small class="font-normal">{{ $report['net_margin_percent'] ?? '—' }}%</small></span><span>₹{{ number_format($report['net_profit']/100,2) }}</span></div></div>@if($report['has_unallocated_company_expenses'])<p class="mt-5 rounded-lg bg-amber-50 p-3 text-sm text-amber-800 dark:bg-amber-950/30 dark:text-amber-200">Outlet profit excludes unallocated company-wide expenses.</p>@endif<p class="mt-5 text-xs text-slate-500">Net Profit is based on financial transactions recorded in RetailPOS. External or unrecorded expenses are not included.</p></section>
 <section class="overflow-x-auto rounded-lg border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Monthly comparison</h2><table class="mt-3 min-w-[900px] text-sm"><thead class="text-left text-xs uppercase text-slate-500"><tr><th>Month</th><th>Net Sales</th><th>COGS</th><th>Gross Profit</th><th>Gross Margin</th><th>Operating Expenses</th><th>Operating Profit</th><th>Other Income</th><th>Other Expenses</th><th>Net Profit</th><th>Net Margin</th></tr></thead><tbody>@forelse($monthly as $row)<tr class="border-t dark:border-slate-800"><td class="py-2">{{ $row['label'] }}<span class="block text-xs text-slate-500">{{ $row['period']['from'] }} – {{ $row['period']['to'] }}</span></td>@foreach(['net_sales','cogs','gross_profit','gross_margin_percent','operating_expenses','operating_profit','other_income','other_expenses','net_profit','net_margin_percent'] as $key)<td>{{ str_ends_with($key,'percent') ? ($row['report'][$key] ?? '—').'%' : '₹'.number_format($row['report'][$key]/100,2) }}</td>@endforeach</tr>@empty<tr><td colspan="11" class="py-6 text-center text-slate-500">No comparison periods are available.</td></tr>@endforelse</tbody></table></section>
 @if($outletComparison->count()>1)<section class="overflow-x-auto rounded-lg border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Outlet comparison</h2><table class="mt-3 min-w-[900px] text-sm"><thead class="text-left text-xs uppercase text-slate-500"><tr><th>Outlet</th><th>Net Sales</th><th>COGS</th><th>Gross Profit</th><th>Gross Margin</th><th>Operating Expenses</th><th>Operating Profit</th><th>Other Income</th><th>Other Expenses</th><th>Net Profit</th><th>Net Margin</th></tr></thead><tbody>@foreach($outletComparison as $row)<tr class="border-t dark:border-slate-800"><td class="py-2">{{ $row['outlet']->name }}</td>@foreach(['net_sales','cogs','gross_profit','gross_margin_percent','operating_expenses','operating_profit','other_income','other_expenses','net_profit','net_margin_percent'] as $key)<td>{{ str_ends_with($key,'percent') ? ($row['report'][$key] ?? '—').'%' : '₹'.number_format($row['report'][$key]/100,2) }}</td>@endforeach</tr>@endforeach</tbody></table><p class="mt-3 text-xs text-slate-500">Outlet profit excludes unallocated company-wide expenses.</p></section>@endif
</div>
@endsection
