@extends('layouts.admin')
@section('title','Stock Counts')
@section('page-title','Stock Counts')
@section('breadcrumbs')<span>/</span><a href="{{ route('inventory.dashboard') }}">Inventory</a><span>/</span><span>Counts</span>@endsection
@section('content')
@include('command-center.inventory.partials.nav')
<div class="mx-auto max-w-7xl space-y-5"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-xl font-semibold">Physical and cycle counts</h2><p class="mt-1 text-sm text-slate-500">Count first, review variance, then post an auditable stock correction.</p></div>@can('inventory.counts.create')<a href="{{ route('inventory.counts.create') }}" class="inline-flex min-h-11 items-center justify-center rounded-lg bg-slate-950 px-4 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">New stock count</a>@endcan</div><section class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="hidden overflow-x-auto md:block"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500 dark:bg-slate-950"><tr><th class="px-5 py-3">Count</th><th class="px-5 py-3">Location</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Assigned</th><th class="px-5 py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($counts as $count)<tr><td class="px-5 py-4 font-semibold">{{ $count->count_number }}</td><td class="px-5 py-4">{{ $count->warehouse?->name }}</td><td class="px-5 py-4 text-slate-500">{{ str($count->type)->headline() }}</td><td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold dark:bg-slate-800">{{ str($count->status->value)->headline() }}</span></td><td class="px-5 py-4 text-slate-500">{{ $count->assignee?->name ?? 'Unassigned' }}</td><td class="px-5 py-4 text-right"><a href="{{ route('inventory.counts.show',$count) }}" class="inline-flex min-h-11 items-center font-semibold text-teal-700">Open</a></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-500">No stock counts yet.</td></tr>@endforelse</tbody></table></div><div class="divide-y divide-slate-100 md:hidden dark:divide-slate-800">@foreach($counts as $count)<a href="{{ route('inventory.counts.show',$count) }}" class="block p-4"><div class="flex justify-between gap-3"><p class="font-semibold">{{ $count->count_number }}</p><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold dark:bg-slate-800">{{ str($count->status->value)->headline() }}</span></div><p class="mt-1 text-sm text-slate-500">{{ $count->warehouse?->name }} · {{ str($count->type)->headline() }}</p></a>@endforeach</div>@if($counts->hasPages())<div class="border-t border-slate-200 p-4">{{ $counts->links() }}</div>@endif</section></div>
@endsection
