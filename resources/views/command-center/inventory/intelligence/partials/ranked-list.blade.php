@php($badge = ['emerald' => 'bg-emerald-100 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-200', 'amber' => 'bg-amber-100 text-amber-700 dark:bg-amber-950 dark:text-amber-200', 'rose' => 'bg-rose-100 text-rose-700 dark:bg-rose-950 dark:text-rose-200'][$tone])
<section class="rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex items-center justify-between border-b border-slate-200 p-4 dark:border-slate-800"><h2 class="font-semibold text-slate-950 dark:text-white">{{ $title }}</h2><a href="{{ route('inventory.intelligence.export', ['dataset' => $dataset] + request()->except('page')) }}" class="text-xs font-semibold text-teal-700 dark:text-teal-300">Export</a></div><div class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($rows->take(6) as $row)<a href="{{ route('inventory.products.show', $row['product_id']) }}" class="flex items-center justify-between gap-3 p-4 hover:bg-slate-50 dark:hover:bg-slate-800/50"><div class="min-w-0"><p class="truncate text-sm font-semibold text-slate-900 dark:text-white">{{ $row['product'] }}</p><p class="mt-1 text-xs text-slate-500">{{ $row['warehouse'] }} · {{ number_format($row['on_hand'], 3) }} on hand</p></div><span class="shrink-0 rounded-full px-2 py-1 text-xs font-semibold {{ $badge }}">{{ $row['is_fast'] ? number_format($row['daily_velocity'], 2).'/day' : $money($row['stock_value_minor']) }}</span></a>@empty<p class="p-8 text-center text-sm text-slate-500">No products in this classification.</p>@endforelse</div></section>
