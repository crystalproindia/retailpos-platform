<section class="mt-6 rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900">
    <div class="border-b border-slate-200 p-5 dark:border-slate-800"><h2 class="font-semibold text-slate-950 dark:text-white">Filtered stock detail</h2><p class="mt-1 text-sm text-slate-500">Up to 500 authorized product-location rows. Use filters to narrow large catalogs.</p></div>
    <div class="hidden overflow-x-auto lg:block"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-950/60 dark:text-slate-400"><tr><th class="px-5 py-3">Product</th><th class="px-4 py-3">Location</th><th class="px-4 py-3 text-right">On hand</th><th class="px-4 py-3 text-right">Sold</th><th class="px-4 py-3 text-right">Days left</th><th class="px-4 py-3 text-right">Stock value</th><th class="px-5 py-3">Health</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($rows as $row)<tr><td class="px-5 py-4"><div class="flex items-center gap-3"><x-product-thumbnail :src="$row['image_url']" :name="$row['product']" /><div class="min-w-0"><a href="{{ route('inventory.products.show', $row['product_id']) }}" class="font-semibold text-slate-900 hover:text-teal-700 dark:text-white dark:hover:text-teal-300">{{ $row['product'] }}</a><p class="mt-1 text-xs text-slate-500">{{ $row['sku'] }} · {{ $row['category'] }} · {{ $row['brand'] }}</p></div></div></td><td class="px-4 py-4"><p class="font-medium text-slate-700 dark:text-slate-200">{{ $row['warehouse'] }}</p><p class="mt-1 text-xs text-slate-500">{{ $row['outlet'] }}</p></td><td class="px-4 py-4 text-right">{{ number_format($row['on_hand'], 3) }}</td><td class="px-4 py-4 text-right">{{ number_format($row['units_sold'], 3) }}</td><td class="px-4 py-4 text-right">{{ $row['days_remaining'] === null ? 'Not enough sales data' : number_format($row['days_remaining'], 1) }}</td><td class="px-4 py-4 text-right font-medium">{{ $money($row['stock_value_minor']) }}</td><td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">{{ str($row['health'])->replace('_', ' ')->headline() }}</span></td></tr>@empty<tr><td colspan="7" class="px-5 py-10 text-center text-slate-500">No stock rows match these filters.</td></tr>@endforelse</tbody></table></div>
    <div class="divide-y divide-slate-100 dark:divide-slate-800 lg:hidden">@forelse($rows as $row)<article class="p-4"><div class="flex items-start justify-between gap-3"><div class="flex min-w-0 items-center gap-3"><x-product-thumbnail :src="$row['image_url']" :name="$row['product']" /><div class="min-w-0"><a href="{{ route('inventory.products.show', $row['product_id']) }}" class="truncate font-semibold text-slate-900 dark:text-white">{{ $row['product'] }}</a><p class="mt-1 truncate text-xs text-slate-500">{{ $row['sku'] }} · {{ $row['warehouse'] }}</p></div></div><span class="shrink-0 rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">{{ str($row['health'])->replace('_', ' ')->headline() }}</span></div><dl class="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt class="text-xs text-slate-500">On hand</dt><dd class="font-medium">{{ number_format($row['on_hand'], 3) }}</dd></div><div><dt class="text-xs text-slate-500">Stock value</dt><dd class="font-medium">{{ $money($row['stock_value_minor']) }}</dd></div><div><dt class="text-xs text-slate-500">Sold in window</dt><dd class="font-medium">{{ number_format($row['units_sold'], 3) }}</dd></div><div><dt class="text-xs text-slate-500">Days remaining</dt><dd class="font-medium">{{ $row['days_remaining'] === null ? 'Not enough sales data' : number_format($row['days_remaining'], 1) }}</dd></div></dl></article>@empty<p class="p-8 text-center text-sm text-slate-500">No stock rows match these filters.</p>@endforelse</div>
</section>
