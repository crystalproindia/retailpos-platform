<section class="mt-6 rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900">
    <div class="flex items-start justify-between gap-4 border-b border-slate-200 p-5 dark:border-slate-800"><div><h2 class="font-semibold text-slate-950 dark:text-white">Inter-outlet transfer opportunities</h2><p class="mt-1 text-sm text-slate-500">Source safety stock is preserved. Nothing moves until an authorized user creates and confirms a transfer.</p></div><a href="{{ route('inventory.intelligence.export', ['dataset' => 'transfers'] + request()->except('page')) }}" class="shrink-0 text-sm font-semibold text-teal-700 dark:text-teal-300">Export CSV</a></div>
    <div class="grid gap-3 p-4 md:grid-cols-2 xl:grid-cols-3">@forelse($rows as $row)<article class="rounded-lg border border-slate-200 p-4 dark:border-slate-700"><div class="flex items-start justify-between gap-3"><div class="flex min-w-0 items-center gap-3"><x-product-thumbnail :src="$row['image_url']" :name="$row['product']" /><div class="min-w-0"><p class="truncate font-semibold text-slate-900 dark:text-white">{{ $row['product'] }}</p><p class="mt-1 text-xs text-slate-500">{{ $row['sku'] }}</p></div></div><span class="shrink-0 rounded-full bg-indigo-100 px-2 py-1 text-xs font-semibold text-indigo-700 dark:bg-indigo-950 dark:text-indigo-200">{{ number_format($row['suggested_quantity'], 3) }} units</span></div><div class="mt-4 grid grid-cols-[1fr_auto_1fr] items-center gap-2 text-sm"><div><p class="text-xs text-slate-500">From</p><p class="mt-1 font-medium">{{ $row['source_warehouse'] }}</p><p class="text-xs text-slate-500">{{ number_format($row['source_stock'], 3) }} available</p></div><span class="text-slate-400">→</span><div><p class="text-xs text-slate-500">To</p><p class="mt-1 font-medium">{{ $row['destination_warehouse'] }}</p><p class="text-xs text-slate-500">{{ number_format($row['destination_stock'], 3) }} available</p></div></div><p class="mt-3 text-xs leading-5 text-slate-500">{{ $row['reason'] }}</p>@can('inventory.transfers.create')<a href="{{ route('inventory.transfers.create', ['source_warehouse_id' => $row['source_warehouse_id'], 'destination_warehouse_id' => $row['destination_warehouse_id'], 'product_id' => $row['product_id'], 'quantity' => $row['suggested_quantity']]) }}" class="mt-4 inline-flex rounded-md bg-slate-950 px-3 py-2 text-xs font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Create stock transfer</a>@endcan</article>@empty<p class="col-span-full py-8 text-center text-sm text-slate-500">No safe transfer opportunity is available in your authorized scope.</p>@endforelse</div>
</section>
