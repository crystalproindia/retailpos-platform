@extends('layouts.admin')
@section('title','Stock Availability')
@section('page-title','Stock Availability')
@section('content')
@include('command-center.inventory.partials.nav')
<div class="mx-auto max-w-7xl space-y-5"><div><h2 class="text-xl font-semibold">Which location has this product?</h2><p class="mt-1 text-sm text-slate-500">Search or scan a barcode to see authorized stock locations instantly.</p></div><form method="GET" class="grid gap-3 rounded-lg border border-slate-200 bg-white p-4 shadow-sm sm:grid-cols-[1fr_240px_auto] dark:border-slate-800 dark:bg-slate-900"><input autofocus name="search" value="{{request('search')}}" placeholder="Scan barcode, SKU, or product name" class="min-h-12 rounded-lg border-slate-300 text-base dark:border-slate-700 dark:bg-slate-950"><select name="warehouse_id" class="min-h-12 rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-950"><option value="">All authorized locations</option>@foreach($warehouses as $warehouse)<option value="{{$warehouse->id}}" @selected((string)request('warehouse_id')===(string)$warehouse->id)>{{$warehouse->name}}</option>@endforeach</select><button class="min-h-12 rounded-lg bg-slate-950 px-5 font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Find stock</button></form><div class="grid gap-4 lg:grid-cols-2">@forelse($products as $product)@php $onHand=(float)$product->stockLevels->sum('quantity_on_hand');$available=(float)$product->stockLevels->sum('quantity_available');$damaged=(float)$product->stockLevels->sum('quantity_damaged');@endphp<a href="{{route('inventory.stock.product',$product)}}" class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md dark:border-slate-800 dark:bg-slate-900"><div class="flex items-start justify-between gap-3"><div><p class="font-semibold text-slate-950 dark:text-white">{{$product->name}}</p><p class="mt-1 text-xs text-slate-500">{{$product->sku}}@if($product->barcode) · {{$product->barcode}}@endif</p></div><div class="text-right"><p class="text-xs text-slate-500">Available</p><p class="text-2xl font-semibold text-teal-700 dark:text-teal-300">{{number_format($available,3)}}</p></div></div><div class="mt-4 space-y-2">@forelse($product->stockLevels as $level)<div class="flex items-center justify-between rounded-lg bg-slate-50 px-3 py-2 text-sm dark:bg-slate-800"><span>{{$level->warehouse?->name}}@if($level->location) / {{$level->location->code}}@endif</span><span class="font-semibold">{{$level->quantity_available}}</span></div>@empty<p class="text-sm text-slate-500">No stock recorded in an authorized location.</p>@endforelse</div><div class="mt-4 flex gap-4 border-t border-slate-100 pt-3 text-xs text-slate-500 dark:border-slate-800"><span>On hand <strong class="text-slate-800 dark:text-slate-100">{{number_format($onHand,3)}}</strong></span><span>Damaged <strong class="text-slate-800 dark:text-slate-100">{{number_format($damaged,3)}}</strong></span></div></a>@empty<div class="rounded-lg border border-dashed border-slate-300 p-12 text-center text-sm text-slate-500 lg:col-span-2">No matching active products.</div>@endforelse</div>{{$products->links()}}</div>
@endsection
