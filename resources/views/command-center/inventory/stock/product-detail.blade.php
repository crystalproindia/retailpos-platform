@extends('layouts.admin')
@section('title',$product->name.' Stock')
@section('page-title',$product->name)
@section('content')
@include('command-center.inventory.partials.nav')
<div class="mx-auto max-w-7xl space-y-6"><div class="flex flex-wrap items-start justify-between gap-3"><div class="flex size-20 shrink-0 items-center justify-center overflow-hidden rounded-lg bg-slate-100 text-2xl font-semibold text-slate-400 dark:bg-slate-800">@if($product->imageUrl(true))<img src="{{$product->imageUrl(true)}}" alt="{{$product->name}}" class="size-full object-contain">@else{{str($product->name)->substr(0,1)}}@endif</div><div><p class="font-mono text-xs text-slate-500">{{$product->sku}}@if($product->barcode) · {{$product->barcode}}@endif</p><h2 class="mt-1 text-2xl font-semibold">{{$product->name}}</h2><p class="mt-1 text-sm text-slate-500">Last purchase: {{$inventory['last_purchase']?Carbon\Carbon::parse($inventory['last_purchase'])->format('d M Y'):'Not recorded'}} · Last sale: {{$inventory['last_sale']?Carbon\Carbon::parse($inventory['last_sale'])->format('d M Y'):'Not recorded'}}</p></div><a href="{{route('inventory.products.show',$product)}}" class="inline-flex min-h-11 items-center rounded-lg border border-slate-300 px-4 text-sm font-semibold">Product profile</a></div><div class="grid grid-cols-2 gap-3 lg:grid-cols-5">@foreach([['On hand',$inventory['total_on_hand']],['Available',$inventory['total_available']],['Reserved',$inventory['total_reserved']],['In transit',$inventory['total_in_transit']],['Damaged',$inventory['total_damaged']]] as [$label,$value])<div class="rounded-lg border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p class="text-xs font-medium text-slate-500">{{$label}}</p><p class="mt-2 text-2xl font-semibold">{{number_format($value,3)}}</p></div>@endforeach</div><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h3 class="font-semibold">Stock by location</h3><div class="mt-4 grid gap-3 md:grid-cols-2">@forelse($inventory['levels'] as $level)<div class="rounded-lg border border-slate-200 p-4 dark:border-slate-700"><div class="flex justify-between gap-3"><div><p class="font-semibold">{{$level->warehouse?->name}}</p><p class="text-xs text-slate-500">{{$level->warehouse?->branch?->name??'Central warehouse'}}@if($level->location) · {{$level->location->code}}@endif</p></div><p class="text-xl font-semibold">{{$level->quantity_available}}</p></div><div class="mt-3 grid grid-cols-3 text-xs text-slate-500"><span>On hand<br><strong class="text-slate-800 dark:text-slate-100">{{$level->quantity_on_hand}}</strong></span><span>Reserved<br><strong class="text-slate-800 dark:text-slate-100">{{$level->quantity_reserved}}</strong></span><span>Damaged<br><strong class="text-slate-800 dark:text-slate-100">{{$level->quantity_damaged}}</strong></span></div></div>@empty<p class="text-sm text-slate-500">No stock recorded.</p>@endforelse</div></section><div class="grid gap-6 xl:grid-cols-2"><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h3 class="font-semibold">Batch and expiry</h3><div class="mt-4 space-y-2">@forelse($inventory['batches'] as $batch)<div class="flex justify-between rounded-lg bg-slate-50 p-3 text-sm dark:bg-slate-800"><span><strong>{{$batch->batch_number}}</strong><small class="block text-slate-500">{{$batch->warehouse?->name}}</small></span><span class="text-right">{{$batch->quantity_available}}<small class="block {{$batch->expires_at?->isPast()?'text-rose-600':'text-slate-500'}}">{{$batch->expires_at?->format('d M Y')??'No expiry'}}</small></span></div>@empty<p class="text-sm text-slate-500">No batch records.</p>@endforelse</div></section><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h3 class="font-semibold">Recent transfers</h3><div class="mt-4 space-y-2">@forelse($inventory['transfers'] as $transfer)<a href="{{route('inventory.transfers.show',$transfer)}}" class="flex justify-between rounded-lg bg-slate-50 p-3 text-sm dark:bg-slate-800"><span><strong>{{$transfer->transfer_number}}</strong><small class="block text-slate-500">{{$transfer->sourceWarehouse?->name}} to {{$transfer->destinationWarehouse?->name}}</small></span><span>{{str($transfer->status)->replace('_',' ')->headline()}}</span></a>@empty<p class="text-sm text-slate-500">No recent transfers.</p>@endforelse</div></section></div><section class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="border-b border-slate-200 px-5 py-4 dark:border-slate-800"><h3 class="font-semibold">Recent stock ledger</h3></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500 dark:bg-slate-950"><tr><th class="px-5 py-3">Date</th><th class="px-5 py-3">Location</th><th class="px-5 py-3">Movement</th><th class="px-5 py-3 text-right">In</th><th class="px-5 py-3 text-right">Out</th><th class="px-5 py-3 text-right">Balance</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@foreach($inventory['movements'] as $movement)<tr><td class="px-5 py-3">{{$movement->occurred_at?->format('d M Y, h:i A')}}</td><td class="px-5 py-3">{{$movement->warehouse?->name}}</td><td class="px-5 py-3">{{str($movement->movement_type)->replace('_',' ')->headline()}}</td><td class="px-5 py-3 text-right">{{$movement->direction==='in'?$movement->quantity:'-'}}</td><td class="px-5 py-3 text-right">{{$movement->direction==='out'?$movement->quantity:'-'}}</td><td class="px-5 py-3 text-right font-semibold">{{$movement->quantity_after}}</td></tr>@endforeach</tbody></table></div></section></div>
@endsection
