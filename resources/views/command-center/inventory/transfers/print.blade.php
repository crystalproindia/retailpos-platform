<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $transfer->transfer_number }}</title><style>body{font:14px system-ui,sans-serif;color:#0f172a;margin:32px}header{display:flex;justify-content:space-between;border-bottom:2px solid #0f172a;padding-bottom:18px}h1{font-size:24px;margin:0}.muted{color:#64748b}.route{display:grid;grid-template-columns:1fr 1fr;gap:24px;margin:24px 0}.box{border:1px solid #cbd5e1;padding:16px;border-radius:6px}table{width:100%;border-collapse:collapse}th,td{padding:10px;border-bottom:1px solid #e2e8f0;text-align:left}th{text-transform:uppercase;font-size:11px;color:#64748b}.right{text-align:right}@media print{body{margin:12mm}.no-print{display:none}}</style></head><body><header><div><p class="muted">STOCK TRANSFER</p><h1>{{ $transfer->transfer_number }}</h1><p class="muted">{{ str($transfer->status)->replace('_',' ')->headline() }}</p></div><button class="no-print" onclick="window.print()">Print</button></header><div class="route"><div class="box"><p class="muted">FROM</p><strong>{{ $transfer->sourceWarehouse?->name }}</strong><p>{{ $transfer->sourceWarehouse?->branch?->name ?? 'Central warehouse' }}</p></div><div class="box"><p class="muted">TO</p><strong>{{ $transfer->destinationWarehouse?->name }}</strong><p>{{ $transfer->destinationWarehouse?->branch?->name ?? 'Central warehouse' }}</p></div></div><table><thead><tr><th>Product</th><th>SKU</th><th class="right">Requested</th><th class="right">Dispatched</th><th class="right">Received</th></tr></thead><tbody>@foreach($transfer->items as $item)<tr><td>{{ $item->product->name }}</td><td>{{ $item->product->sku }}</td><td class="right">{{ $item->requested_quantity }}</td><td class="right">{{ $item->dispatched_quantity }}</td><td class="right">{{ $item->received_quantity }}</td></tr>@endforeach</tbody></table><p class="muted" style="margin-top:24px">Requested by {{ $transfer->requester?->name }} on {{ $transfer->created_at->format('d M Y, h:i A') }}</p>@if($transfer->notes)<p><strong>Note:</strong> {{ $transfer->notes }}</p>@endif</body></html>
