@extends('layouts.admin')
@section('title', 'Store setup complete')
@section('page-title', 'Store setup complete')
@section('content')
<div class="mx-auto max-w-3xl"><section class="rounded-lg border border-teal-200 bg-white p-7 text-center shadow-sm dark:border-teal-900 dark:bg-slate-900"><span class="inline-grid size-14 place-items-center rounded-full bg-teal-100 text-2xl font-bold text-teal-800 dark:bg-teal-950 dark:text-teal-200">✓</span><h1 class="mt-5 text-2xl font-semibold text-slate-950 dark:text-white">Your store is ready!</h1><p class="mt-2 text-sm leading-6 text-slate-500 dark:text-slate-400">RetailPOS has prepared the essential settings for your business. You can change every configuration later from Settings and Invoice Designs.</p><div class="mt-6 grid gap-3 text-left sm:grid-cols-2">@foreach([['Categories','Starter categories were prepared without removing anything.'],['Tax settings','Only the GST details and rates you confirmed were considered.'],['Invoice design',$setup->recommendations['invoice_template']['name'] ?? 'Invoice design is ready.'],['Barcode settings','A label template is created only when your scanner choice supported it.']] as [$label,$copy])<div class="rounded-lg bg-slate-50 p-4 dark:bg-slate-950"><p class="font-semibold text-slate-950 dark:text-white">{{ $label }}</p><p class="mt-1 text-sm text-slate-500">{{ $copy }}</p></div>@endforeach</div><p class="mt-5 text-sm leading-6 text-slate-500 dark:text-slate-400">You can add products manually now or prepare the CSV template. Upload, preview, and bulk import will be introduced in a dedicated future release.</p><div class="mt-7 flex flex-wrap justify-center gap-3"><a href="{{ route('sales.invoices.create') }}" class="rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Create My First Invoice</a><a href="{{ route('inventory.products.create') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 dark:border-slate-700 dark:text-slate-200">Add Products</a><a href="{{ route('onboarding.store-setup.template') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 dark:border-slate-700 dark:text-slate-200">Download Product Template</a><a href="{{ route('dashboard') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 dark:border-slate-700 dark:text-slate-200">View Dashboard</a></div></section></div>
@endsection
