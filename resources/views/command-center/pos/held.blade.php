@extends('layouts.pos')
@section('title', 'Held POS Bills')
@section('content')
<main class="min-h-screen bg-slate-50 p-4 sm:p-6 lg:p-8"><div class="mx-auto max-w-6xl"><header class="flex flex-wrap items-start justify-between gap-4"><div><a href="{{ route('pos.dashboard') }}" class="text-sm font-semibold text-teal-700">POS dashboard</a><h1 class="mt-2 text-2xl font-bold text-slate-950">Held bills</h1><p class="mt-1 text-sm text-slate-500">Resume bills saved under your cashier session.</p></div><a href="{{ route('pos.terminal') }}" class="pos-primary-action">Open terminal</a></header><form class="mt-6 max-w-md"><label class="sr-only" for="held-bill-search">Search held bills</label><div class="flex gap-2"><input id="held-bill-search" name="q" value="{{ request('q') }}" class="min-w-0 flex-1" placeholder="Search bill, customer, or mobile"><button class="pos-secondary-action">Search</button></div></form><section class="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">@forelse($heldSales as $sale)<article class="pos-held-card"><div class="flex items-start justify-between gap-3"><div><p class="text-xs font-semibold uppercase tracking-[0.12em] text-slate-400">{{ $sale->sale_number }}</p><h2 class="mt-1 font-bold text-slate-900">{{ $sale->customer?->display_name ?? 'Walk-in customer' }}</h2><p class="mt-1 text-sm text-slate-500">{{ $sale->customer?->phone ?? 'No mobile recorded' }}</p></div><span class="rounded-lg bg-teal-50 px-2.5 py-1 text-sm font-bold text-teal-700">{{ number_format($sale->total_amount, 2) }}</span></div><div class="mt-4 flex items-center justify-between border-t border-slate-100 pt-3 text-xs text-slate-500"><span>{{ $sale->items->count() }} items</span><span>{{ $sale->held_at?->diffForHumans() }}</span></div><a href="{{ route('pos.held.resume', $sale) }}" class="pos-primary-action mt-4 w-full">Resume bill</a></article>@empty<div class="col-span-full pos-empty-list"><x-icon name="pos" class="size-9 text-slate-300" /><p class="mt-3 font-semibold text-slate-700">No held bills found</p><p class="mt-1 text-sm text-slate-500">Bills held from your terminal will appear here.</p></div>@endforelse</section></div></main>
@endsection
