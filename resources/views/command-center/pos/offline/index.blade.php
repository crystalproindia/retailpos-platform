@extends('layouts.pos')
@section('title', 'Offline POS Sync Monitor')
@section('content')
<main class="min-h-screen bg-slate-50 p-4 sm:p-8"><div class="mx-auto max-w-7xl"><header class="flex flex-wrap items-start justify-between gap-4"><div><a href="{{ route('pos.dashboard') }}" class="text-sm font-semibold text-teal-700">POS dashboard</a><h1 class="mt-2 text-2xl font-bold text-slate-950">Offline sync monitor</h1><p class="mt-1 text-sm text-slate-500">Pending, synced, warning, duplicate, and failed offline bill records.</p></div><a href="{{ route('pos.terminal') }}" class="pos-primary-action">Open terminal</a></header><section class="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full min-w-[54rem] text-left text-sm"><thead class="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-[.1em] text-slate-500"><tr><th class="px-4 py-3">Status</th><th class="px-4 py-3">Offline reference</th><th class="px-4 py-3">Device / cashier</th><th class="px-4 py-3">Server bill</th><th class="px-4 py-3">Warning or error</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($records as $record)<tr><td class="px-4 py-3"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold text-slate-600">{{ str($record->status)->replace('_',' ')->headline() }}</span></td><td class="px-4 py-3 font-mono text-xs">{{ $record->payload['offline_reference'] ?? $record->offline_uuid }}</td><td class="px-4 py-3"><p>{{ $record->device_id ?? 'Browser device' }}</p><p class="text-xs text-slate-500">{{ $record->user?->name }}</p></td><td class="px-4 py-3">{{ $record->metadata['sale_number'] ?? '—' }}</td><td class="max-w-xs px-4 py-3 text-slate-600">{{ $record->warning_message ?? $record->error_message ?? '—' }}</td><td class="px-4 py-3">@if(in_array($record->status,['failed','pending']))<form method="POST" action="{{ route('pos.offline.records.retry',$record) }}">@csrf<button class="text-sm font-bold text-teal-700">Retry</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="px-4 py-16 text-center text-slate-500">No offline sync records are available.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 px-4 py-3">{{ $records->links() }}</div></section></div></main>
@endsection
