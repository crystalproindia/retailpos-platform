@extends('layouts.pos')

@section('title', 'Return Controls')

@section('content')
<main class="min-h-screen bg-slate-50 p-4 sm:p-6 lg:p-8"><div class="mx-auto max-w-3xl"><header><a href="{{ route('pos.returns.index') }}" class="text-sm font-semibold text-teal-700">Returns</a><h1 class="mt-2 text-2xl font-bold text-slate-950">Return controls</h1><p class="mt-1 text-sm text-slate-500">Set tenant-level safeguards for post-sale returns. These controls apply to future returns only.</p></header>@if(session('status'))<div class="mt-5 rounded-xl border border-teal-200 bg-teal-50 p-4 text-sm text-teal-800">{{ session('status') }}</div>@endif
<form method="POST" action="{{ route('pos.returns.settings.update') }}" class="mt-6 space-y-6">@csrf @method('PUT')<section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="grid gap-5 sm:grid-cols-2"><label class="text-sm font-semibold text-slate-700">Return window in days<input type="number" min="0" max="365" name="return_window_days" value="{{ old('return_window_days',$settings->return_window_days) }}" class="mt-1 block w-full rounded-lg border-slate-300 text-sm"><span class="mt-1 block text-xs font-normal text-slate-500">Managers can use an audited override outside this window.</span></label><label class="text-sm font-semibold text-slate-700">Approval threshold<input name="approval_threshold" value="{{ old('approval_threshold',$settings->approval_threshold) }}" inputmode="decimal" class="mt-1 block w-full rounded-lg border-slate-300 text-sm" placeholder="Always require approval"><span class="mt-1 block text-xs font-normal text-slate-500">Optional extra threshold above which approval is always required.</span></label></div><div class="mt-6 grid gap-4 sm:grid-cols-2">@foreach(['receipt_required'=>'Require original receipt or invoice','manager_approval_required'=>'Require manager approval','cashiers_may_initiate'=>'Allow cashiers to initiate','refund_original_method_only'=>'Require original refund method','store_credit_allowed'=>'Allow identified-customer store credit','damaged_may_restock'=>'Allow damaged goods to be restocked','anonymous_returns_allowed'=>'Allow walk-in returns'] as $field=>$label)<label class="flex gap-3 rounded-lg border border-slate-200 p-3 text-sm font-medium text-slate-700"><input type="hidden" name="{{ $field }}" value="0"><input type="checkbox" name="{{ $field }}" value="1" @checked(old($field,$settings->{$field})) class="mt-0.5 rounded border-slate-300 text-teal-600"><span>{{ $label }}</span></label>@endforeach</div></section><div class="flex justify-end"><button class="pos-primary-action">Save controls</button></div></form></div></main>
@endsection
