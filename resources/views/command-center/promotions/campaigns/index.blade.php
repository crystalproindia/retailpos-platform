@extends('layouts.admin')
@section('title','Campaigns') @section('page-title','Promotion Campaigns')
@section('breadcrumbs')<span>/</span><a href="{{ route('promotions.dashboard') }}">Promotions</a><span>/</span><span>Campaigns</span>@endsection
@section('content')
@include('command-center.promotions.partials.nav')
<div class="mb-4 flex flex-col gap-3 md:flex-row md:justify-between"><form method="GET" class="flex flex-wrap gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search campaigns" class="rounded-lg border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900"><select name="status" class="rounded-lg border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ str($status->value)->headline() }}</option>@endforeach</select><button class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Filter</button></form>@can('promotions.campaigns.create')<a href="{{ route('promotions.campaigns.create') }}" class="rounded-lg bg-teal-600 px-4 py-2 text-center text-sm font-semibold text-white">New campaign</a>@endcan</div>
<div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm dark:divide-slate-800"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800"><tr><th class="px-5 py-3">Campaign</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Window</th><th class="px-5 py-3">Rules</th><th class="px-5 py-3">Status</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($campaigns as $campaign)<tr><td class="px-5 py-3"><a href="{{ route('promotions.campaigns.show',$campaign) }}" class="font-semibold text-slate-950 hover:text-teal-700 dark:text-white">{{ $campaign->name }}</a><p class="text-xs text-slate-500">Priority {{ $campaign->priority }}</p></td><td class="px-5 py-3">{{ str($campaign->campaign_type->value)->headline() }}</td><td class="px-5 py-3 text-slate-500">{{ $campaign->start_at?->format('d M Y') ?? 'Any time' }} - {{ $campaign->end_at?->format('d M Y') ?? 'No end' }}</td><td class="px-5 py-3">{{ $campaign->rules_count }}</td><td class="px-5 py-3"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">{{ str($campaign->status->value)->headline() }}</span></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No campaigns match these filters.</td></tr>@endforelse</tbody></table></div><div class="border-t px-5 py-4 dark:border-slate-800">{{ $campaigns->links() }}</div></div>
@endsection
