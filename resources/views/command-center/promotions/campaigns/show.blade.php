@extends('layouts.admin')
@section('title',$campaign->name) @section('page-title',$campaign->name)
@section('breadcrumbs')<span>/</span><a href="{{ route('promotions.dashboard') }}">Promotions</a><span>/</span><a href="{{ route('promotions.campaigns.index') }}">Campaigns</a><span>/</span><span>{{ $campaign->name }}</span>@endsection
@section('content')
@include('command-center.promotions.partials.nav')
<div class="mb-6 flex flex-wrap items-center justify-between gap-3"><div><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">{{ str($campaign->status->value)->headline() }}</span><p class="mt-3 text-sm text-slate-500">{{ $campaign->description ?: 'No campaign description.' }}</p></div>@can('promotions.campaigns.update')<a href="{{ route('promotions.campaigns.edit',$campaign) }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Edit campaign</a>@endcan</div>
<div class="grid gap-6 lg:grid-cols-3"><section class="lg:col-span-2 overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="border-b px-5 py-4 dark:border-slate-800"><h2 class="font-semibold text-slate-950 dark:text-white">Linked promotion rules</h2></div><div class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($campaign->rules as $rule)<a href="{{ route('promotions.rules.show',$rule) }}" class="block px-5 py-4 hover:bg-slate-50 dark:hover:bg-slate-800"><p class="font-medium text-slate-950 dark:text-white">{{ $rule->name }}</p><p class="text-xs text-slate-500">{{ str($rule->promotion_type->value)->headline() }}</p></a>@empty<p class="px-5 py-8 text-sm text-slate-500">No promotion rules are linked to this campaign.</p>@endforelse</div></section><aside class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><dl class="space-y-4 text-sm"><div><dt class="text-slate-500">Type</dt><dd class="mt-1 font-medium text-slate-950 dark:text-white">{{ str($campaign->campaign_type->value)->headline() }}</dd></div><div><dt class="text-slate-500">Schedule</dt><dd class="mt-1 font-medium text-slate-950 dark:text-white">{{ $campaign->start_at?->format('d M Y H:i') ?? 'Any time' }}</dd></div><div><dt class="text-slate-500">Priority</dt><dd class="mt-1 font-medium text-slate-950 dark:text-white">{{ $campaign->priority }}</dd></div></dl></aside></div>
@endsection
