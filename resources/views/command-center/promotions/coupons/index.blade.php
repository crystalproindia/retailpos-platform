@extends('layouts.admin')
@section('title','Coupons') @section('page-title','Promotion Coupons')
@section('breadcrumbs')<span>/</span><a href="{{ route('promotions.dashboard') }}">Promotions</a><span>/</span><span>Coupons</span>@endsection
@section('content')
@include('command-center.promotions.partials.nav')
<div class="mb-4 flex flex-col gap-3 md:flex-row md:justify-between"><form method="GET" class="flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search coupon code" class="rounded-lg border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900"><button class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Filter</button></form>@can('promotions.coupons.create')<a href="{{ route('promotions.coupons.create') }}" class="rounded-lg bg-teal-600 px-4 py-2 text-center text-sm font-semibold text-white">New coupon</a>@endcan</div>
<div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm dark:divide-slate-800"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800"><tr><th class="px-5 py-3">Coupon</th><th class="px-5 py-3">Rule</th><th class="px-5 py-3">Usage</th><th class="px-5 py-3">Validity</th><th class="px-5 py-3">Status</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($coupons as $coupon)<tr><td class="px-5 py-3"><a href="{{ route('promotions.coupons.edit',$coupon) }}" class="font-mono font-semibold text-slate-950 dark:text-white">{{ $coupon->code }}</a><p class="text-xs text-slate-500">{{ $coupon->description }}</p></td><td class="px-5 py-3">{{ $coupon->rule?->name }}</td><td class="px-5 py-3">{{ $coupon->used_count }}{{ $coupon->usage_limit_total ? ' / '.$coupon->usage_limit_total : '' }}</td><td class="px-5 py-3 text-slate-500">{{ $coupon->end_at?->format('d M Y') ?? 'No end' }}</td><td class="px-5 py-3"><span class="rounded-full px-2 py-1 text-xs font-semibold {{ $coupon->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-700' }}">{{ $coupon->is_active ? 'Active' : 'Disabled' }}</span></td><td class="px-5 py-3">@can('promotions.coupons.disable')<form method="POST" action="{{ route('promotions.coupons.toggle',$coupon) }}">@csrf<button class="text-sm font-medium text-teal-700 dark:text-teal-300">{{ $coupon->is_active ? 'Disable' : 'Enable' }}</button></form>@endcan</td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No coupons match these filters.</td></tr>@endforelse</tbody></table></div><div class="border-t px-5 py-4 dark:border-slate-800">{{ $coupons->links() }}</div></div>
@endsection
