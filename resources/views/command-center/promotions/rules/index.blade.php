@extends('layouts.admin')
@section('title','Promotion Rules') @section('page-title','Promotion Rules')
@section('breadcrumbs')<span>/</span><a href="{{ route('promotions.dashboard') }}">Promotions</a><span>/</span><span>Rules</span>@endsection
@section('content')
@include('command-center.promotions.partials.nav')
<div class="mb-4 flex flex-col gap-3 md:flex-row md:justify-between"><form method="GET" class="flex flex-wrap gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search rules" class="rounded-lg border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900"><select name="status" class="rounded-lg border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ str($status->value)->headline() }}</option>@endforeach</select><select name="promotion_type" class="rounded-lg border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="">All types</option>@foreach($types as $type)<option value="{{ $type->value }}" @selected(request('promotion_type')===$type->value)>{{ str($type->value)->headline() }}</option>@endforeach</select><button class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-teal-300 dark:text-slate-950">Filter</button></form>@can('promotions.rules.create')<a href="{{ route('promotions.rules.create') }}" class="rounded-lg bg-teal-600 px-4 py-2 text-center text-sm font-semibold text-white">New rule</a>@endcan</div>
<div class="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm dark:divide-slate-800"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800"><tr><th class="px-5 py-3">Rule</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Campaign</th><th class="px-5 py-3">Behavior</th><th class="px-5 py-3">Status</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($rules as $rule)<tr><td class="px-5 py-3"><a href="{{ route('promotions.rules.show',$rule) }}" class="font-semibold text-slate-950 hover:text-teal-700 dark:text-white">{{ $rule->name }}</a><p class="text-xs text-slate-500">Priority {{ $rule->priority }}</p></td><td class="px-5 py-3">{{ str($rule->promotion_type->value)->headline() }}</td><td class="px-5 py-3 text-slate-500">{{ $rule->campaign?->name ?? 'Standalone' }}</td><td class="px-5 py-3"><span class="text-xs text-slate-500">{{ $rule->requires_coupon ? 'Coupon' : 'Auto' }} · {{ $rule->stackable ? 'Stackable' : 'Single' }}</span></td><td class="px-5 py-3"><span class="rounded-full px-2 py-1 text-xs font-semibold {{ $rule->is_active ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-900 dark:text-emerald-200' : 'bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-200' }}">{{ str($rule->status->value)->headline() }}</span></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No promotion rules match these filters.</td></tr>@endforelse</tbody></table></div><div class="border-t px-5 py-4 dark:border-slate-800">{{ $rules->links() }}</div></div>
@endsection
