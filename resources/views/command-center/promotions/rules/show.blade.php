@extends('layouts.admin')
@section('title',$rule->name) @section('page-title',$rule->name)
@section('breadcrumbs')<span>/</span><a href="{{ route('promotions.dashboard') }}">Promotions</a><span>/</span><a href="{{ route('promotions.rules.index') }}">Rules</a><span>/</span><span>{{ $rule->name }}</span>@endsection
@section('content')
@include('command-center.promotions.partials.nav')
<div class="mb-6 flex flex-wrap items-start justify-between gap-3"><div><div class="flex flex-wrap gap-2"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">{{ str($rule->promotion_type->value)->headline() }}</span><span class="rounded-full px-2 py-1 text-xs font-semibold {{ $rule->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-700' }}">{{ str($rule->status->value)->headline() }}</span></div><p class="mt-3 text-sm text-slate-500">{{ $rule->description ?: 'No rule description.' }}</p></div><div class="flex flex-wrap gap-2">@can('promotions.rules.update')<a href="{{ route('promotions.rules.edit',$rule) }}" class="rounded-lg border px-4 py-2 text-sm font-semibold">Edit</a>@endcan @can('promotions.rules.approve')@if(!$rule->approved_at)<form method="POST" action="{{ route('promotions.rules.approve',$rule) }}">@csrf<button class="rounded-lg border border-teal-200 px-4 py-2 text-sm font-semibold text-teal-700">Approve</button></form>@endif@endcan @can('promotions.rules.activate')@if(!$rule->is_active)<form method="POST" action="{{ route('promotions.rules.activate',$rule) }}">@csrf<button class="rounded-lg bg-teal-600 px-4 py-2 text-sm font-semibold text-white">Activate</button></form>@else<form method="POST" action="{{ route('promotions.rules.pause',$rule) }}">@csrf<button class="rounded-lg bg-amber-500 px-4 py-2 text-sm font-semibold text-white">Pause</button></form>@endif@endcan</div></div>
<div class="grid gap-6 lg:grid-cols-3"><section class="lg:col-span-2 space-y-6"><div class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Actions</h2><div class="mt-4 divide-y divide-slate-100 dark:divide-slate-800">@foreach($rule->actions as $action)<div class="py-3 text-sm"><span class="font-medium text-slate-950 dark:text-white">{{ str($action->action_type->value)->headline() }}</span><span class="ml-2 text-slate-500">{{ $action->discount_percentage ? $action->discount_percentage.'%' : '' }} {{ $action->discount_value ? '₹'.$action->discount_value : '' }} {{ $action->fixed_price ? '₹'.$action->fixed_price.' bundle price' : '' }} {{ $action->buy_quantity ? 'Buy '.$action->buy_quantity : '' }} {{ $action->get_quantity ? ' Get '.$action->get_quantity : '' }}</span></div>@endforeach</div></div><div class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Targeting</h2><div class="mt-4 grid gap-3 text-sm md:grid-cols-2"><p>Products: {{ $rule->productTargets->count() ?: 'All' }}</p><p>Categories: {{ $rule->categoryTargets->count() ?: 'All' }}</p><p>Brands: {{ $rule->brandTargets->count() ?: 'All' }}</p><p>Branches: {{ $rule->branchTargets->count() ?: 'All' }}</p><p>Channels: {{ $rule->channelTargets->count() ?: 'All' }}</p></div></div></section><aside class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><dl class="space-y-4 text-sm"><div><dt class="text-slate-500">Campaign</dt><dd class="mt-1 font-medium text-slate-950 dark:text-white">{{ $rule->campaign?->name ?? 'Standalone' }}</dd></div><div><dt class="text-slate-500">Priority</dt><dd class="mt-1 font-medium text-slate-950 dark:text-white">{{ $rule->priority }}</dd></div><div><dt class="text-slate-500">Stacking</dt><dd class="mt-1 font-medium text-slate-950 dark:text-white">{{ $rule->stackable ? 'Allowed' : 'Not allowed' }}</dd></div><div><dt class="text-slate-500">Coupon</dt><dd class="mt-1 font-medium text-slate-950 dark:text-white">{{ $rule->requires_coupon ? 'Required' : 'Not required' }}</dd></div></dl></aside></div>
@endsection
