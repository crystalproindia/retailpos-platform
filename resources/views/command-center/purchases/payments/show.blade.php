@extends('layouts.admin')
@section('title', $payment->payment_number)
@section('page-title', $payment->payment_number)
@section('content')
@include('command-center.purchases.partials.nav')
<section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex flex-wrap justify-between gap-3"><div><p class="font-semibold">{{ $payment->supplier->name }}</p><p class="text-sm text-slate-500">{{ str($payment->payment_type)->headline() }} · {{ str($payment->status)->headline() }} · {{ $payment->payment_date->format('d M Y') }}</p></div><p class="text-lg font-semibold">₹{{ number_format((float)$payment->amount,2) }}</p></div><div class="mt-5 overflow-x-auto"><table class="min-w-full text-sm"><thead class="text-left text-xs uppercase text-slate-500"><tr><th class="py-2">Allocated invoice</th><th class="text-right">Amount</th></tr></thead><tbody>@forelse($payment->allocations as $allocation)<tr class="border-t"><td class="py-3">{{ $allocation->invoice->invoice_number }}</td><td class="text-right">₹{{ number_format((float)$allocation->amount,2) }}</td></tr>@empty<tr><td colspan="2" class="py-5 text-slate-500">This payment remains available as a supplier advance.</td></tr>@endforelse</tbody></table></div></section>@can('supplier-payments.reverse')@if($payment->status !== 'reversed')<form method="POST" action="{{ route('purchases.payments.reverse',$payment) }}" class="mt-5 flex gap-2">@csrf<input required name="reason" placeholder="Reversal reason" class="rounded border-slate-300"><button class="rounded border border-rose-300 px-3 py-2 text-sm text-rose-700">Reverse payment</button></form>@endif@endcan
@endsection
