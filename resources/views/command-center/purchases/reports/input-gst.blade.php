@extends('layouts.admin')
@section('title','Input GST Register')
@section('page-title','Input GST Register')
@section('content')
@include('command-center.purchases.partials.nav')
<p class="mb-5 text-sm text-amber-700">Prepared from approved purchase-invoice snapshots. Review with your accountant before filing; this is not GST portal reconciliation or return filing.</p><div class="grid gap-3 md:grid-cols-4">@foreach($totals as $label=>$amount)<section class="rounded-lg border bg-white p-4 shadow-sm"><p class="text-sm text-slate-500">Input {{ strtoupper($label) }}</p><p class="mt-1 text-xl font-semibold">₹{{ number_format((float)$amount,2) }}</p></section>@endforeach</div><section class="mt-5 overflow-hidden rounded-lg border bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Supplier</th><th>GSTIN</th><th>Invoice</th><th>POS</th><th class="text-right">CGST</th><th class="text-right">SGST</th><th class="text-right">IGST</th><th class="px-5">RCM</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr class="border-t"><td class="px-5 py-3">{{ $invoice->supplier->name }}</td><td>{{ $invoice->supplier->gstin ?: 'Not recorded' }}</td><td>{{ $invoice->supplier_invoice_number }}</td><td>{{ $invoice->place_of_supply_state_code ?: 'Not recorded' }}</td><td class="text-right">₹{{ number_format((float)$invoice->input_cgst,2) }}</td><td class="text-right">₹{{ number_format((float)$invoice->input_sgst,2) }}</td><td class="text-right">₹{{ number_format((float)$invoice->input_igst,2) }}</td><td class="px-5">{{ $invoice->reverse_charge ? 'Yes' : 'No' }}</td></tr>@empty<tr><td colspan="8" class="p-8 text-center text-slate-500">No approved purchase invoices to report.</td></tr>@endforelse</tbody></table></div></section>
@endsection
