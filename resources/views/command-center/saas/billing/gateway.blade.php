@extends('layouts.admin')
@section('title','Payment Gateway')
@section('page-title','Payment Gateway')
@section('breadcrumbs')<span>/</span><span>Platform</span><span>/</span><span>Billing</span><span>/</span><span>Gateway</span>@endsection
@section('content')
@include('command-center.saas.partials.nav')
<div class="mx-auto max-w-3xl space-y-6"><section class="rounded-lg border border-sky-200 bg-sky-50 p-5 text-sm text-sky-950"><strong>Razorpay test mode only.</strong> This release cannot activate live payments. Saved key and webhook secrets are encrypted and never displayed.</section><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex justify-between gap-4"><div><h2 class="font-semibold">Razorpay billing</h2><p class="mt-1 text-sm text-slate-500">{{ $test['message'] }}</p></div><span class="rounded-full px-3 py-1 text-sm font-semibold {{ $test['configured']?'bg-teal-100 text-teal-800':'bg-amber-100 text-amber-900' }}">{{ $test['configured']?'Ready for test':'Not configured' }}</span></div><form method="POST" action="{{ route('saas.billing.gateway.update') }}" class="mt-5 grid gap-4 md:grid-cols-2">@csrf @method('PUT')<label class="text-sm font-medium">Test key ID<input required name="key_id" value="{{ old('key_id',$connection?->settings['key_id'] ?? '') }}" class="mt-2 block w-full rounded-lg border-slate-300 text-sm"></label><label class="text-sm font-medium">Account email<input name="account_email" value="{{ old('account_email',$connection?->account_email) }}" class="mt-2 block w-full rounded-lg border-slate-300 text-sm"></label><label class="text-sm font-medium">Test key secret<input name="key_secret" type="password" placeholder="{{ $connection?->getRawOriginal('access_token')?'Saved securely; leave blank to keep':'Required to enable' }}" class="mt-2 block w-full rounded-lg border-slate-300 text-sm"></label><label class="text-sm font-medium">Webhook secret<input name="webhook_secret" type="password" placeholder="{{ $connection?->getRawOriginal('refresh_token')?'Saved securely; leave blank to keep':'Required to enable' }}" class="mt-2 block w-full rounded-lg border-slate-300 text-sm"></label><input type="hidden" name="mode" value="test"><label class="flex items-center gap-2 text-sm font-medium md:col-span-2"><input type="hidden" name="enabled" value="0"><input type="checkbox" name="enabled" value="1" @checked(old('enabled',$connection?->settings['enabled'] ?? false))>Enable Razorpay test mode</label><div><button class="rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Save test settings</button></div></form><form method="POST" action="{{ route('saas.billing.gateway.test') }}" class="mt-4">@csrf<button class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold">Validate configuration without charging</button></form></section></div>
@endsection
