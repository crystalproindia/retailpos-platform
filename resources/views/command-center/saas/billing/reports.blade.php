@extends('layouts.admin')
@section('title','SaaS Billing Reports')
@section('page-title','SaaS Billing Reports')
@section('breadcrumbs')<span>/</span><a href="{{ route('saas.billing.index') }}">Billing</a><span>/</span><span>Reports</span>@endsection
@section('content')
@include('command-center.saas.partials.nav')
<div class="grid gap-6 xl:grid-cols-2"><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Tenant billing report</h2><div class="mt-4 space-y-3">@forelse($tenantRevenue as $row)<div class="flex justify-between border-b border-slate-100 pb-3 text-sm dark:border-slate-800"><span>{{ $row->company?->name }}</span><span>INR {{ number_format((float)$row->total,2) }}</span></div>@empty<p class="text-sm text-slate-500">No issued billing records yet.</p>@endforelse</div></section><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Plan revenue report</h2><div class="mt-4 space-y-3">@forelse($planRevenue as $row)<div class="flex justify-between border-b border-slate-100 pb-3 text-sm dark:border-slate-800"><span>{{ $row->plan?->name ?? 'Historical plan' }} <span class="text-slate-500">({{ $row->invoice_count }})</span></span><span>INR {{ number_format((float)$row->total,2) }}</span></div>@empty<p class="text-sm text-slate-500">No issued billing records yet.</p>@endforelse</div></section></div><section class="mt-6 rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">GST and credit summary</h2><div class="mt-4 grid gap-4 sm:grid-cols-3 lg:grid-cols-6">@foreach(['Taxable'=>$tax?->taxable ?? 0,'CGST'=>$tax?->cgst ?? 0,'SGST'=>$tax?->sgst ?? 0,'IGST'=>$tax?->igst ?? 0,'Cess'=>$tax?->cess ?? 0,'Refund adjustments'=>$tax?->refunds ?? 0] as $label=>$amount)<div><p class="text-xs uppercase text-slate-500">{{ $label }}</p><p class="mt-1 font-semibold">INR {{ number_format((float)$amount,2) }}</p></div>@endforeach</div><p class="mt-5 text-sm text-slate-500">Review with your accountant before filing. These reports do not submit GST returns or generate IRNs.</p></section>
@endsection
