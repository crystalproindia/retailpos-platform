@extends('layouts.admin')

@section('title', $plan->exists ? 'Edit Plan' : 'New Plan')
@section('page-title', $plan->exists ? 'Edit Plan' : 'New Plan')
@section('breadcrumbs')<span>/</span><span>Platform</span><span>/</span><a href="{{ route('saas.plans.index') }}">Plans</a><span>/</span><span>{{ $plan->exists ? 'Edit' : 'New' }}</span>@endsection

@section('content')
    @include('command-center.saas.partials.nav')
    <form method="POST" action="{{ $plan->exists ? route('saas.plans.update', $plan) : route('saas.plans.store') }}" class="space-y-6">@csrf @if($plan->exists) @method('PUT') @endif
        <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Plan details</h2><p class="mt-1 text-sm text-slate-500">Use a stable code. It is used for integrations and should not be a marketing label.</p><div class="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3"><label class="text-sm font-medium">Name<input required name="name" value="{{ old('name', $plan->name) }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="text-sm font-medium">Code<input required name="code" value="{{ old('code', $plan->code) }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="text-sm font-medium">Status<select name="status" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950">@foreach(['draft','active','archived'] as $status)<option value="{{ $status }}" @selected(old('status',$plan->status ?: 'draft') === $status)>{{ str($status)->headline() }}</option>@endforeach</select></label><label class="text-sm font-medium">Billing interval<select name="billing_interval" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950">@foreach(['monthly','quarterly','half-yearly','yearly','custom','manual'] as $interval)<option value="{{ $interval }}" @selected(old('billing_interval',$plan->billing_interval ?: 'monthly') === $interval)>{{ str($interval)->headline() }}</option>@endforeach</select></label><label class="text-sm font-medium">Currency<input required name="currency" maxlength="3" value="{{ old('currency', $plan->currency ?: 'INR') }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="text-sm font-medium">Base price<input required name="base_price" type="number" min="0" step="0.01" value="{{ old('base_price', $plan->base_price ?? 0) }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="text-sm font-medium">Setup fee<input name="setup_fee" type="number" min="0" step="0.01" value="{{ old('setup_fee', $plan->setup_fee ?? 0) }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="text-sm font-medium">Tax percentage<input name="tax_percentage" type="number" min="0" max="100" step="0.001" value="{{ old('tax_percentage', $plan->tax_percentage ?? 0) }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="text-sm font-medium">Trial days<input name="trial_days" type="number" min="0" value="{{ old('trial_days', $plan->trial_days ?? 0) }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="text-sm font-medium">Grace-period days<input name="grace_period_days" type="number" min="0" value="{{ old('grace_period_days', $plan->grace_period_days ?? 0) }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label><label class="text-sm font-medium">Sort order<input name="sort_order" type="number" min="0" value="{{ old('sort_order', $plan->sort_order ?? 0) }}" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label></div><label class="mt-4 block text-sm font-medium">Description<textarea name="description" rows="3" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950">{{ old('description', $plan->description) }}</textarea></label><label class="mt-4 block text-sm font-medium">Internal notes<textarea name="notes" rows="3" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950">{{ old('notes', $plan->notes) }}</textarea></label><div class="mt-4 flex flex-wrap gap-5 text-sm"><label><input type="checkbox" name="is_public" value="1" @checked(old('is_public', $plan->is_public))> Show as an option to tenants</label><label><input type="checkbox" name="is_recommended" value="1" @checked(old('is_recommended', $plan->is_recommended))> Recommended</label><label><input type="checkbox" name="is_custom" value="1" @checked(old('is_custom', $plan->is_custom))> Custom/negotiated</label></div></section>
        <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Included features</h2><p class="mt-1 text-sm text-slate-500">Feature keys stay stable even when plan names change.</p><div class="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">@foreach(config('saas.features') as $feature)<label class="flex items-center gap-2 rounded-md border border-slate-200 p-3 text-sm dark:border-slate-800"><input type="hidden" name="features[{{ $feature }}]" value="0"><input type="checkbox" name="features[{{ $feature }}]" value="1" @checked(old('features.'.$feature, $plan->features->firstWhere('feature_key',$feature)?->is_enabled))><span>{{ str($feature)->replace('_',' ')->headline() }}</span></label>@endforeach</div></section>
        <section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold text-slate-950 dark:text-white">Usage limits</h2><p class="mt-1 text-sm text-slate-500">Leave a value blank for unlimited. Entering zero does not mean unlimited and is rejected.</p><div class="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@foreach(config('saas.usage_limits') as $limit)<label class="text-sm font-medium">{{ str($limit)->replace('_',' ')->headline() }}<input name="limits[{{ $limit }}]" type="number" min="1" value="{{ old('limits.'.$limit, $plan->limits->firstWhere('limit_key',$limit)?->limit_value) }}" placeholder="Unlimited" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-950"></label>@endforeach</div></section>
        <div class="flex justify-end gap-3"><a href="{{ route('saas.plans.index') }}" class="rounded-md px-4 py-2 text-sm font-medium text-slate-600 dark:text-slate-300">Cancel</a><button class="rounded-md bg-slate-950 px-4 py-2 text-sm font-medium text-white dark:bg-teal-300 dark:text-slate-950">Save plan</button></div>
    </form>
@endsection
