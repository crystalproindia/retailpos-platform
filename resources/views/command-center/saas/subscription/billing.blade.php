@extends('layouts.admin')
@section('title','Billing')
@section('page-title','Subscription Billing')
@section('breadcrumbs')<span>/</span><a href="{{ route('account.subscription.index') }}">Subscription</a><span>/</span><span>Billing</span>@endsection
@section('content')
<div class="mx-auto max-w-5xl space-y-6"><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex items-center justify-between"><div><h2 class="font-semibold">Invoices</h2><p class="mt-1 text-sm text-slate-500">Subscription invoices, payment status, and downloadable records.</p></div></div><div class="mt-5 overflow-x-auto"><table class="min-w-full text-sm"><thead class="text-left text-xs uppercase text-slate-500"><tr><th class="pb-3">Invoice</th><th class="pb-3">Due</th><th class="pb-3">Amount</th><th class="pb-3">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($invoices as $invoice)<tr><td class="py-4 font-medium">{{ $invoice->invoice_number }}</td><td class="py-4">{{ $invoice->due_date?->format('d M Y') ?? '—' }}</td><td class="py-4">{{ $invoice->currency }} {{ number_format((float)$invoice->grand_total,2) }}</td><td class="py-4"><x-status-badge :status="$invoice->status" /></td><td class="py-4 text-right"><a href="{{ route('account.subscription.billing.show',$invoice) }}" class="font-semibold text-teal-700">View</a></td></tr>@empty<tr><td colspan="5" class="py-10 text-center text-slate-500">No subscription invoices are available.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $invoices->links() }}</div></section><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Recent payments</h2><div class="mt-4 space-y-3">@forelse($payments as $payment)<div class="flex items-center justify-between rounded-lg border border-slate-200 p-3 text-sm dark:border-slate-800"><span>{{ $payment->receipt_number }}</span><span>{{ $payment->currency }} {{ number_format((float)$payment->amount,2) }}</span></div>@empty<p class="text-sm text-slate-500">No payments have been confirmed yet.</p>@endforelse</div></section></div>
@endsection
