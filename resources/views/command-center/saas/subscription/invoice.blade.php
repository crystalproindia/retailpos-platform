@extends('layouts.admin')
@section('title',$invoice->invoice_number)
@section('page-title','Subscription Invoice')
@section('breadcrumbs')<span>/</span><a href="{{ route('account.subscription.billing.index') }}">Billing</a><span>/</span><span>{{ $invoice->invoice_number }}</span>@endsection
@section('content')
<div class="mx-auto max-w-4xl space-y-6"><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-sm text-slate-500">{{ $invoice->invoice_number }}</p><h2 class="mt-1 text-2xl font-semibold">{{ $invoice->currency }} {{ number_format((float)$invoice->grand_total,2) }}</h2><p class="mt-1 text-sm text-slate-500">Due {{ $invoice->due_date?->format('d M Y') ?? 'on issue' }}</p></div><x-status-badge :status="$invoice->status" /></div><div class="mt-5 divide-y divide-slate-100 border-y border-slate-200 dark:divide-slate-800 dark:border-slate-800">@foreach($invoice->items as $item)<div class="flex justify-between py-4"><span>{{ $item->name }}</span><strong>{{ $invoice->currency }} {{ number_format((float)$item->line_total,2) }}</strong></div>@endforeach</div><div class="mt-5 flex flex-wrap gap-3"><a href="{{ route('account.subscription.billing.pdf',$invoice) }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold">Download invoice</a>@if($invoice->isPayable())<form method="POST" action="{{ route('account.subscription.billing.checkout',$invoice) }}">@csrf<button class="rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Pay now</button></form>@endif</div></section><section class="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h3 class="font-semibold">Payment receipts</h3><div class="mt-4 space-y-3">@forelse($invoice->payments as $payment)<div class="flex items-center justify-between text-sm"><span>{{ $payment->receipt_number }}</span><a href="{{ route('account.subscription.billing.receipt',[$invoice,$payment]) }}" class="font-semibold text-teal-700">Download receipt</a></div>@empty<p class="text-sm text-slate-500">A receipt will appear after payment is confirmed.</p>@endforelse</div></section></div>
@endsection
