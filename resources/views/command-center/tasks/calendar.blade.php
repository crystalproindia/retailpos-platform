@extends('layouts.admin')
@section('title', 'Task Calendar')
@section('page-title', 'Task Calendar')
@section('breadcrumbs')<a href="{{ route('tasks.index') }}">Tasks</a><span>/</span><span>Calendar</span>@endsection
@section('content')
    <div class="mx-auto max-w-7xl space-y-6"><div class="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><p class="text-sm font-semibold text-teal-700">Task calendar</p><h1 class="mt-1 text-2xl font-semibold text-slate-950 dark:text-white">{{ $start->format('j M') }} - {{ $end->format('j M Y') }}</h1></div><div class="flex flex-wrap gap-2"><a href="{{ route('tasks.calendar', ['start' => $start->copy()->subWeek()->toDateString()]) }}" class="min-h-11 rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200">Previous week</a><a href="{{ route('tasks.calendar') }}" class="min-h-11 rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200">This week</a><a href="{{ route('tasks.calendar', ['start' => $start->copy()->addWeek()->toDateString()]) }}" class="min-h-11 rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200">Next week</a><a href="{{ route('tasks.today') }}" class="min-h-11 rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-200">Accessible list view</a></div></div><section class="grid gap-3 md:grid-cols-7">@foreach(range(0, 6) as $offset)@php($day = $start->copy()->addDays($offset))<article class="min-h-44 rounded-xl border border-slate-200 bg-white p-3 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p class="text-sm font-semibold text-slate-900 dark:text-white">{{ $day->format('D') }} <span class="text-slate-500">{{ $day->format('j') }}</span></p><div class="mt-3 space-y-2">@forelse($calendarTasks->get($day->toDateString(), collect()) as $task)<a href="{{ route('tasks.show', $task) }}" class="block rounded-lg p-2 text-xs font-medium {{ $task->task_type->value === 'personal' ? 'bg-violet-50 text-violet-800 dark:bg-violet-950/30 dark:text-violet-200' : 'bg-teal-50 text-teal-800 dark:bg-teal-950/30 dark:text-teal-200' }}">{{ $task->due_at?->format('g:i A') }} · {{ $task->title }}</a>@empty<p class="text-xs text-slate-400">No tasks</p>@endforelse</div></article>@endforeach</section></div>
@endsection
