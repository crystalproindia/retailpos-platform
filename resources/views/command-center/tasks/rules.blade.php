@extends('layouts.admin')
@section('title', 'Task Rules')
@section('page-title', 'Task Rules')
@section('breadcrumbs')<a href="{{ route('tasks.index') }}">Tasks</a><span>/</span><span>Task rules</span>@endsection
@section('content')
    <div class="mx-auto max-w-5xl space-y-6"><div><p class="text-sm font-semibold text-teal-700">Controlled automation</p><h1 class="mt-1 text-2xl font-semibold text-slate-950 dark:text-white">Task rules</h1><p class="mt-2 max-w-3xl text-sm leading-6 text-slate-500 dark:text-slate-400">Rules are explicit and off by default. Each run is bounded, tenant scoped, and prevents duplicate work for the same trigger.</p></div><div class="space-y-4">@foreach($registry->definitions() as $key => $rule)@php($setting = $settings->get($key))<section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><form method="POST" action="{{ route('tasks.rules.update', $key) }}" class="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">@csrf @method('PUT')<div><h2 class="font-semibold text-slate-950 dark:text-white">{{ $rule['name'] }}</h2><p class="mt-1 max-w-2xl text-sm text-slate-500 dark:text-slate-400">{{ $rule['description'] }}</p></div><div class="flex flex-wrap items-end gap-3"><label class="flex min-h-11 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm font-medium dark:border-slate-700"><input type="hidden" name="is_enabled" value="0"><input type="checkbox" name="is_enabled" value="1" @checked($setting?->is_enabled)> Enabled</label><label><span class="block text-xs font-semibold text-slate-500">Threshold hours</span><input name="threshold_hours" type="number" min="1" max="8760" value="{{ $setting?->threshold_hours ?? $rule['threshold_hours'] }}" class="mt-1 min-h-11 w-28 rounded-lg border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-950"></label><button class="min-h-11 rounded-lg bg-slate-950 px-4 text-sm font-semibold text-white hover:bg-slate-800 dark:bg-white dark:text-slate-950">Save</button></div></form></section>@endforeach</div></div>
@endsection
