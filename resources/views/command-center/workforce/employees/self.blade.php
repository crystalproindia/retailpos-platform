@extends('layouts.admin')
@section('title', 'My workforce profile')
@section('page-title', 'My profile')
@section('content')
    <div class="mx-auto max-w-4xl space-y-6"><section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><p class="text-sm text-slate-500">{{ $employee->employee_number }}</p><h1 class="mt-1 text-2xl font-semibold text-slate-950">{{ $employee->display_name }}</h1><p class="mt-1 text-slate-500">{{ $employee->job_title ?: 'Job title not set' }} · {{ $employee->primaryBranch?->name ?: 'No primary outlet' }}</p></section><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center justify-between gap-3"><div><h2 class="font-semibold text-slate-950">My tasks</h2><p class="mt-1 text-sm text-slate-500">Personal tasks are visible only to you and never count toward workforce metrics.</p></div><div class="flex items-center gap-3"><a href="{{ route('tasks.index') }}#quick-add" class="text-sm font-semibold text-teal-700">Add task</a><a href="{{ route('tasks.today') }}" class="text-sm font-semibold text-teal-700">Open tasks</a></div></div><div class="mt-4 grid gap-3 sm:grid-cols-4"><a href="{{ route('tasks.today') }}" class="rounded-lg bg-sky-50 p-4"><p class="text-xs text-sky-700">Due today</p><p class="mt-1 text-xl font-semibold">{{ $workTaskMetrics['today'] }}</p></a><a href="{{ route('tasks.overdue') }}" class="rounded-lg bg-rose-50 p-4"><p class="text-xs text-rose-700">Overdue work</p><p class="mt-1 text-xl font-semibold">{{ $workTaskMetrics['overdue'] }}</p></a><a href="{{ route('tasks.personal') }}" class="rounded-lg bg-violet-50 p-4"><p class="text-xs text-violet-700">Personal</p><p class="mt-1 text-xl font-semibold">{{ $personalTaskMetrics['today'] + $personalTaskMetrics['upcoming'] + $personalTaskMetrics['overdue'] }}</p></a><a href="{{ route('tasks.completed') }}" class="rounded-lg bg-emerald-50 p-4"><p class="text-xs text-emerald-700">Completed today</p><p class="mt-1 text-xl font-semibold">{{ $workTaskMetrics['completed_today'] }}</p></a></div></section><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold text-slate-950">My operational context</h2><p class="mt-2 text-sm text-slate-600">{{ $metrics['notice'] }}</p>@if($metrics['available'])<div class="mt-4 grid gap-3 sm:grid-cols-3"><div class="rounded-lg bg-teal-50 p-4"><p class="text-xs text-teal-700">Completed sales</p><p class="mt-1 text-xl font-semibold">{{ $metrics['sales_count'] }}</p></div><div class="rounded-lg bg-slate-50 p-4"><p class="text-xs text-slate-500">Net sales</p><p class="mt-1 text-xl font-semibold">{{ number_format($metrics['net_sales'] / 100, 2) }}</p></div><div class="rounded-lg bg-slate-50 p-4"><p class="text-xs text-slate-500">Average order</p><p class="mt-1 text-xl font-semibold">{{ $metrics['average_order_value'] === null ? 'Not available' : number_format($metrics['average_order_value'] / 100, 2) }}</p></div></div>@endif</section><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold text-slate-950">My reviews and recognition</h2><p class="mt-2 text-sm text-slate-500">Manager-only notes, other employees, and company-wide access settings are not visible here.</p><div class="mt-4 space-y-3">@forelse($employee->reviews->where('status', 'submitted') as $review)<article class="rounded-lg bg-slate-50 p-3 text-sm"><strong>{{ str($review->cycle)->headline() }} review</strong><p class="mt-1 text-slate-600">{{ $review->comments }}</p></article>@empty<p class="text-sm text-slate-500">No submitted review is available.</p>@endforelse</div></section></div>
@endsection
