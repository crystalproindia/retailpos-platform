<!doctype html><html><head><meta charset="utf-8"><style>@page{margin:28px}body{font-family:DejaVu Sans,sans-serif;color:#172033;font-size:11px}h1{font-size:22px;margin:0}.muted{color:#64748b}.row{display:table;width:100%}.col{display:table-cell;width:50%;vertical-align:top}.right{text-align:right}table{width:100%;border-collapse:collapse;margin-top:24px}th,td{border-bottom:1px solid #dbe2ea;padding:8px 5px;text-align:left}th{background:#f1f5f9;font-size:9px;text-transform:uppercase}.totals{margin:18px 0 0 auto;width:270px}.totals td{border:0;padding:4px}.total td{border-top:1px solid #94a3b8;font-weight:bold;font-size:13px}.note{margin-top:24px;padding:12px;background:#f8fafc}</style></head><body><div class="row"><div class="col"><p class="muted">{{ $return->company_name_snapshot }}</p><p>{{ $return->company_address_snapshot }}</p>@if($return->company_tax_number_snapshot)<p>GSTIN {{ $return->company_tax_number_snapshot }}</p>@endif</div><div class="col right"><h1>CREDIT NOTE</h1><p><strong>{{ $return->credit_note_number }}</strong></p><p>{{ $return->issue_date->format('d M Y') }}</p></div></div><div class="row" style="margin-top:24px"><div class="col"><p class="muted">Credit to</p><p><strong>{{ $return->customer_company_snapshot ?: $return->customer_name_snapshot }}</strong></p><p>{{ $return->customer_address_snapshot }}</p>@if($return->customer_tax_number_snapshot)<p>GSTIN {{ $return->customer_tax_number_snapshot }}</p>@endif</div><div class="col right"><p class="muted">Original Invoice</p><p><strong>{{ $return->invoice->invoice_number }}</strong></p><p>{{ $return->invoice->issue_date?->format('d M Y') }}</p></div></div><table><thead><tr><th>Item</th><th>Returned Qty</th><th>Taxable</th><th>Tax</th><th class="right">Credit</th></tr></thead><tbody>@foreach($return->items as $item)<tr><td>{{ $item->product_name_snapshot }}@if($item->hsn_sac_snapshot)<br><span class="muted">HSN/SAC {{ $item->hsn_sac_snapshot }}</span>@endif</td><td>{{ $item->return_quantity }} {{ $item->unit_snapshot }}</td><td>{{ number_format((float)$item->taxable_reversal,2) }}</td><td>{{ number_format((float)$item->tax_reversal,2) }}</td><td class="right">{{ number_format((float)$item->credit_total,2) }}</td></tr>@endforeach</tbody></table><table class="totals"><tr><td>Taxable reversal</td><td class="right">{{ $return->currency }} {{ number_format((float)$return->taxable_total,2) }}</td></tr><tr><td>CGST / SGST / IGST</td><td class="right">{{ number_format((float)$return->cgst_total,2) }} / {{ number_format((float)$return->sgst_total,2) }} / {{ number_format((float)$return->igst_total,2) }}</td></tr><tr><td>Tax reversal</td><td class="right">{{ $return->currency }} {{ number_format((float)$return->tax_total,2) }}</td></tr><tr class="total"><td>Total Credit</td><td class="right">{{ $return->currency }} {{ number_format((float)$return->credit_total,2) }}</td></tr></table><div class="note"><strong>Reason:</strong> {{ str($return->reason_code)->replace('_',' ')->headline() }}@if($return->reason_note)<br>{{ $return->reason_note }}@endif<br><span class="muted">This credit note references the original invoice and does not itself confirm a payment refund.</span></div></body></html>
