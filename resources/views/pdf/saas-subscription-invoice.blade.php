<!doctype html><html><head><style>@page{margin:32px}body{color:#172033;font-family:DejaVu Sans;font-size:10px}.header{border-bottom:2px solid #0f766e;padding-bottom:14px}.right{text-align:right}.items{border-collapse:collapse;margin-top:20px;width:100%}.items th{background:#0f172a;color:#fff;padding:8px;text-align:left}.items td{border-bottom:1px solid #e2e8f0;padding:8px}.number{text-align:right}.summary{margin-left:auto;margin-top:18px;width:240px}.summary td{padding:4px}</style></head><body><table class="header" width="100%"><tr><td><strong>RETAILPOS.BIZ</strong><h1>Subscription Invoice</h1></td><td class="right"><strong>{{ $invoice->invoice_number }}</strong><br>Issued {{ $invoice->issue_date?->format('d M Y') }}<br>Due {{ $invoice->due_date?->format('d M Y') }}</td></tr></table><table style="margin-top:18px" width="100%"><tr><td width="55%"><strong>Bill to</strong><br>{{ $invoice->billing_company ?: $invoice->billing_name }}<br>{{ $invoice->billing_email }}<br>{!! nl2br(e($invoice->billing_address)) !!}</td><td><strong>GSTIN</strong><br>{{ $invoice->customer_gstin ?: '-' }}<br><strong>Place of supply</strong><br>{{ $invoice->place_of_supply_state_code ?: '-' }}</td></tr></table><table class="items"><thead><tr><th>Item</th><th class="number">Taxable value</th><th class="number">Tax</th><th class="number">Total</th></tr></thead><tbody>@foreach($invoice->items as $item)<tr><td><strong>{{ $item->name }}</strong><br>{{ $item->description }}</td><td class="number">{{ number_format((float)$item->taxable_value,2) }}</td><td class="number">{{ number_format((float)($item->cgst_amount+$item->sgst_amount+$item->igst_amount+$item->cess_amount),2) }}</td><td class="number">{{ number_format((float)$item->line_total,2) }}</td></tr>@endforeach</tbody></table><table class="summary"><tr><td>Taxable value</td><td class="right">{{ $invoice->currency }} {{ number_format((float)$invoice->taxable_total,2) }}</td></tr><tr><td>CGST / SGST</td><td class="right">{{ number_format((float)$invoice->cgst_total,2) }} / {{ number_format((float)$invoice->sgst_total,2) }}</td></tr><tr><td>IGST</td><td class="right">{{ number_format((float)$invoice->igst_total,2) }}</td></tr><tr><td><strong>Total</strong></td><td class="right"><strong>{{ $invoice->currency }} {{ number_format((float)$invoice->grand_total,2) }}</strong></td></tr><tr><td>Paid</td><td class="right">{{ $invoice->currency }} {{ number_format((float)$invoice->amount_paid,2) }}</td></tr><tr><td>Balance due</td><td class="right">{{ $invoice->currency }} {{ number_format((float)$invoice->balance_due,2) }}</td></tr></table><p style="margin-top:28px">Review GST records with your accountant before filing. No GST portal filing or e-invoice submission is performed by this document.</p></body></html>
