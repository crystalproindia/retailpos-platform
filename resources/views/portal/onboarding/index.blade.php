@extends('layouts.portal') @section('title','Onboarding') @section('content')
<div><h1 class="text-2xl font-semibold">Onboarding progress</h1><p class="mt-2 text-sm text-slate-600">A simple view of the implementation work your team is completing with CrystalPro.</p></div><div class="mt-6 grid gap-4">@forelse($onboardings as $onboarding)<a href="{{ route('portal.onboarding.show',$onboarding) }}" class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm transition hover:border-teal-300 hover:shadow-md"><div class="flex flex-col justify-between gap-3 sm:flex-row"><div><p class="text-sm text-slate-500">{{ $onboarding->onboarding_number }}</p><h2 class="mt-1 text-lg font-semibold">{{ $onboarding->title }}</h2><p class="mt-2 text-sm text-slate-500">Target go-live {{ $onboarding->target_go_live_date?->format('d M Y') ?? 'to be confirmed' }}</p></div><span class="h-fit rounded-full bg-teal-100 px-2.5 py-1 text-xs font-semibold text-teal-800">{{ $onboarding->status->label() }}</span></div><div class="mt-5 h-2 overflow-hidden rounded-full bg-slate-100"><div class="h-full rounded-full bg-teal-500" style="width: {{ $onboarding->progress_percent }}%"></div></div><p class="mt-2 text-sm font-medium text-slate-700">{{ $onboarding->progress_percent }}% complete</p></a>@empty<div class="rounded-xl border border-dashed border-slate-300 bg-white p-12 text-center text-sm text-slate-500">There is no active onboarding plan to show.</div>@endforelse</div>
@endsection
