@extends('layouts.portal') @section('title',$onboarding->title) @section('content')
<a href="{{ route('portal.onboarding.index') }}" class="text-sm font-semibold text-teal-700">Back to onboarding</a><section class="mt-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><p class="text-sm text-slate-500">{{ $onboarding->onboarding_number }}</p><h1 class="mt-1 text-2xl font-semibold">{{ $onboarding->title }}</h1><p class="mt-2 text-sm text-slate-600">Target go-live {{ $onboarding->target_go_live_date?->format('d M Y') ?? 'to be confirmed' }}</p></div><span class="h-fit rounded-full bg-teal-100 px-2.5 py-1 text-xs font-semibold text-teal-800">{{ $onboarding->status->label() }}</span></div><div class="mt-7 h-3 overflow-hidden rounded-full bg-slate-100"><div class="h-full rounded-full bg-teal-500" style="width: {{ $onboarding->progress_percent }}%"></div></div><p class="mt-2 text-sm font-semibold">{{ $onboarding->progress_percent }}% complete</p></section><section class="mt-6 grid gap-6 lg:grid-cols-2"><article class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold">Your checklist</h2><div class="mt-4 space-y-3">@forelse($onboarding->tasks as $task)<div class="flex gap-3 rounded-lg border border-slate-100 p-3"><span class="mt-0.5 grid h-5 w-5 shrink-0 place-items-center rounded-full {{ $task->completed_at ? 'bg-teal-500 text-white' : 'border border-slate-300 text-transparent' }}">✓</span><div><p class="text-sm font-medium">{{ $task->title }}</p><p class="mt-1 text-xs text-slate-500">{{ $task->completed_at ? 'Completed' : 'Pending' }}</p></div></div>@empty<p class="text-sm text-slate-500">Your implementation checklist will appear here once it is prepared.</p>@endforelse</div></article><article class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold">Documents and updates</h2><div class="mt-4 space-y-3">@forelse($onboarding->documents as $document)<div class="rounded-lg border border-slate-100 p-3"><p class="text-sm font-medium">{{ $document->title }}</p><p class="mt-1 text-xs text-slate-500">{{ $document->status?->label() ?? str($document->status)->headline() }}</p></div>@empty<p class="text-sm text-slate-500">No documents are requested at the moment.</p>@endforelse @foreach($onboarding->onboardingNotes->where('visibility','customer_safe') as $note)<div class="rounded-lg bg-slate-50 p-3"><p class="text-sm leading-6 text-slate-700">{{ $note->note }}</p></div>@endforeach</div></article></section>
@endsection
