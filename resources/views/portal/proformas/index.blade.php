@extends('layouts.portal') @section('title','Proformas') @section('content')
<div><h1 class="text-2xl font-semibold">Proforma invoices</h1><p class="mt-2 text-sm text-slate-600">Review invoice amounts and payment status.</p></div><div class="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">@forelse($proformas as $proforma)<a href="{{ route('portal.proformas.show',$proforma) }}" class="flex items-center justify-between gap-4 border-b border-slate-100 p-5 transition hover:bg-slate-50 last:border-0"><div><p class="font-semibold">{{ $proforma->proforma_number }}</p><p class="mt-1 text-sm text-slate-500">Due {{ $proforma->due_date?->format('d M Y') ?? 'Not specified' }}</p></div><div class="text-right"><p class="font-semibold">{{ $proforma->currency }} {{ number_format((float)$proforma->balance_amount,2) }} due</p><span class="mt-1 inline-block rounded-full bg-sky-100 px-2.5 py-1 text-xs font-semibold text-sky-800">{{ $proforma->status->label() }}</span></div></a>@empty<div class="p-12 text-center text-sm text-slate-500">No proforma invoices are available yet.</div>@endforelse</div><div class="mt-5">{{ $proformas->links() }}</div>
@endsection
