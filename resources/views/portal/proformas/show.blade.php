@extends('layouts.portal') @section('title',$proforma->proforma_number) @section('content')
<a href="{{ route('portal.proformas.index') }}" class="text-sm font-semibold text-teal-700">Back to proformas</a><div class="mt-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><p class="text-sm text-slate-500">{{ $proforma->proforma_number }}</p><h1 class="mt-1 text-2xl font-semibold">{{ $proforma->title }}</h1><p class="mt-2 text-sm text-slate-600">Invoice date {{ $proforma->invoice_date->format('d M Y') }}</p></div><span class="h-fit rounded-full bg-sky-100 px-2.5 py-1 text-xs font-semibold text-sky-800">{{ $proforma->status->label() }}</span></div><div class="mt-6 grid gap-4 sm:grid-cols-3"><div class="rounded-lg bg-slate-50 p-4"><p class="text-xs font-semibold uppercase text-slate-500">Grand total</p><p class="mt-2 text-lg font-semibold">{{ $proforma->currency }} {{ number_format((float)$proforma->grand_total,2) }}</p></div><div class="rounded-lg bg-slate-50 p-4"><p class="text-xs font-semibold uppercase text-slate-500">Paid</p><p class="mt-2 text-lg font-semibold">{{ $proforma->currency }} {{ number_format((float)$proforma->paid_amount,2) }}</p></div><div class="rounded-lg bg-teal-50 p-4"><p class="text-xs font-semibold uppercase text-teal-700">Balance</p><p class="mt-2 text-lg font-semibold text-teal-900">{{ $proforma->currency }} {{ number_format((float)$proforma->balance_amount,2) }}</p></div></div><div class="mt-7 overflow-hidden rounded-lg border border-slate-200"><table class="w-full text-sm"><thead class="bg-slate-50 text-left text-slate-500"><tr><th class="p-3">Item</th><th class="p-3 text-right">Qty</th><th class="p-3 text-right">Total</th></tr></thead><tbody>@foreach($proforma->items as $item)<tr class="border-t border-slate-100"><td class="p-3 font-medium">{{ $item->name }}</td><td class="p-3 text-right">{{ $item->quantity }}</td><td class="p-3 text-right">{{ $proforma->currency }} {{ number_format((float)$item->line_total,2) }}</td></tr>@endforeach</tbody></table></div>@if($proforma->notes)<div class="mt-6 rounded-lg bg-slate-50 p-4"><p class="text-sm font-semibold">Notes</p><p class="mt-2 whitespace-pre-line text-sm leading-6 text-slate-600">{{ $proforma->notes }}</p></div>@endif</div>
@endsection
