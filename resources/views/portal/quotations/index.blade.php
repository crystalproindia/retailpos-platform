@extends('layouts.portal') @section('title','Quotations') @section('content')
<div><h1 class="text-2xl font-semibold">Quotations</h1><p class="mt-2 text-sm text-slate-600">Commercial proposals available to your organization.</p></div><div class="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">@forelse($quotations as $quotation)<a href="{{ route('portal.quotations.show',$quotation) }}" class="flex items-center justify-between gap-4 border-b border-slate-100 p-5 transition hover:bg-slate-50 last:border-0"><div><p class="font-semibold">{{ $quotation->title }}</p><p class="mt-1 text-sm text-slate-500">{{ $quotation->quotation_number }} · Created {{ $quotation->created_at->format('d M Y') }}</p></div><div class="text-right"><p class="font-semibold">{{ $quotation->currency }} {{ number_format((float)$quotation->grand_total,2) }}</p><span class="mt-1 inline-block rounded-full bg-sky-100 px-2.5 py-1 text-xs font-semibold text-sky-800">{{ $quotation->status->label() }}</span></div></a>@empty<div class="p-12 text-center text-sm text-slate-500">No quotations are available yet.</div>@endforelse</div><div class="mt-5">{{ $quotations->links() }}</div>
@endsection
