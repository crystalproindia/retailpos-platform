@extends('layouts.portal') @section('title',$quotation->quotation_number) @section('content')
<a href="{{ route('portal.quotations.index') }}" class="text-sm font-semibold text-teal-700">Back to quotations</a><div class="mt-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><p class="text-sm text-slate-500">{{ $quotation->quotation_number }}</p><h1 class="mt-1 text-2xl font-semibold">{{ $quotation->title }}</h1><p class="mt-2 text-sm text-slate-600">Valid until {{ $quotation->valid_until?->format('d M Y') ?? 'Not specified' }}</p></div><div class="text-left sm:text-right"><span class="rounded-full bg-sky-100 px-2.5 py-1 text-xs font-semibold text-sky-800">{{ $quotation->status->label() }}</span><p class="mt-3 text-2xl font-semibold">{{ $quotation->currency }} {{ number_format((float)$quotation->grand_total,2) }}</p></div></div><div class="mt-7 overflow-hidden rounded-lg border border-slate-200"><table class="w-full text-sm"><thead class="bg-slate-50 text-left text-slate-500"><tr><th class="p-3 font-medium">Item</th><th class="p-3 text-right font-medium">Qty</th><th class="p-3 text-right font-medium">Total</th></tr></thead><tbody>@foreach($quotation->items as $item)<tr class="border-t border-slate-100"><td class="p-3"><p class="font-medium">{{ $item->name }}</p>@if($item->description)<p class="mt-1 text-xs text-slate-500">{{ $item->description }}</p>@endif</td><td class="p-3 text-right">{{ $item->quantity }}</td><td class="p-3 text-right">{{ $quotation->currency }} {{ number_format((float)$item->line_total,2) }}</td></tr>@endforeach</tbody></table></div>@if($quotation->notes)<div class="mt-6 rounded-lg bg-slate-50 p-4"><p class="text-sm font-semibold">Notes</p><p class="mt-2 whitespace-pre-line text-sm leading-6 text-slate-600">{{ $quotation->notes }}</p></div>@endif @if($quotation->terms_conditions)<div class="mt-4 rounded-lg bg-slate-50 p-4"><p class="text-sm font-semibold">Terms</p><p class="mt-2 whitespace-pre-line text-sm leading-6 text-slate-600">{{ $quotation->terms_conditions }}</p></div>@endif</div>
@endsection
