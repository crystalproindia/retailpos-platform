@extends('layouts.portal') @section('title','Request services') @section('content')
<section class="rounded-xl border border-slate-200 bg-white p-7 shadow-sm"><div class="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div><p class="text-sm font-semibold text-teal-700">CrystalPro services</p><h1 class="mt-1 text-2xl font-semibold">Need another service?</h1><p class="mt-2 max-w-2xl text-sm leading-6 text-slate-600">Share what your business needs next. We will create a dedicated request for our team without changing your current service or support tickets.</p></div><a href="{{ route('portal.services.request') }}" class="inline-flex w-fit rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white hover:bg-slate-800">Request a service</a></div></section><section class="mt-7"><h2 class="text-lg font-semibold">Your recent requests</h2><div class="mt-4 grid gap-4 md:grid-cols-2">@forelse($requests as $request)<article class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between gap-3"><div><p class="font-semibold">{{ $request->metadata['service_category'] ?? $request->title }}</p><p class="mt-2 text-sm leading-6 text-slate-600">{{ $request->description }}</p></div><span class="rounded-full bg-sky-100 px-2.5 py-1 text-xs font-semibold text-sky-800">{{ $request->status?->name }}</span></div><p class="mt-4 text-xs text-slate-500">Requested {{ $request->created_at->format('d M Y, h:i A') }}</p></article>@empty<div class="rounded-xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">You have not requested an additional service yet.</div>@endforelse</div></section>
@endsection
