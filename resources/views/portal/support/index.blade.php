@extends('layouts.portal') @section('title','Support') @section('content')
<div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 class="text-2xl font-semibold">Support</h1><p class="mt-2 text-sm text-slate-600">See your requests and customer-safe updates from the CrystalPro team.</p></div><a href="{{ route('portal.support.create') }}" class="inline-flex w-fit rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white hover:bg-slate-800">Create support ticket</a></div><div class="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">@forelse($tickets as $ticket)<a href="{{ route('portal.support.show',$ticket) }}" class="flex items-center justify-between gap-4 border-b border-slate-100 p-5 transition hover:bg-slate-50 last:border-0"><div><p class="text-xs font-semibold text-slate-500">{{ $ticket->ticket_number }}</p><p class="mt-1 font-semibold">{{ $ticket->subject }}</p><p class="mt-1 text-sm text-slate-500">{{ $ticket->category->label() }} · Updated {{ $ticket->updated_at->diffForHumans() }}</p></div><div class="text-right"><span class="inline-block rounded-full bg-sky-100 px-2.5 py-1 text-xs font-semibold text-sky-800">{{ $ticket->status->label() }}</span><p class="mt-2 text-xs text-slate-500">{{ $ticket->priority->label() }} priority</p></div></a>@empty<div class="p-12 text-center"><p class="font-semibold">No support tickets yet</p><p class="mt-2 text-sm text-slate-500">Create a ticket whenever your team needs a hand.</p></div>@endforelse</div><div class="mt-5">{{ $tickets->links() }}</div>
@endsection
