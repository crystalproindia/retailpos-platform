@extends('layouts.portal') @section('title',$ticket->ticket_number) @section('content')
<a href="{{ route('portal.support.index') }}" class="text-sm font-semibold text-teal-700">Back to support</a><div class="mt-4 grid gap-6 xl:grid-cols-[1fr_340px]"><section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><p class="text-sm text-slate-500">{{ $ticket->ticket_number }}</p><h1 class="mt-1 text-2xl font-semibold">{{ $ticket->subject }}</h1><p class="mt-2 text-sm text-slate-600">Opened {{ $ticket->created_at->format('d M Y, h:i A') }}</p></div><span class="h-fit rounded-full bg-sky-100 px-2.5 py-1 text-xs font-semibold text-sky-800">{{ $ticket->status->label() }}</span></div><div class="mt-7 space-y-4">@forelse($ticket->messages as $message)<article class="rounded-lg border border-slate-100 p-4"><p class="text-xs font-semibold text-teal-700">{{ $message->message_type === 'customer_request' || $message->customer_portal_user_id ? 'You' : 'CrystalPro support' }}</p><p class="mt-2 whitespace-pre-line text-sm leading-6 text-slate-700">{{ $message->message }}</p><p class="mt-3 text-xs text-slate-500">{{ $message->created_at->format('d M Y, h:i A') }}</p></article>@empty<p class="text-sm text-slate-500">No customer-safe messages yet.</p>@endforelse</div><form method="POST" action="{{ route('portal.support.replies.store',$ticket) }}" class="mt-7 border-t border-slate-100 pt-6">@csrf<label class="text-sm font-semibold">Add a reply</label><textarea name="message" rows="5" required class="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5 text-sm" placeholder="Share an update with our team."></textarea>@error('message')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror<button class="mt-3 rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white hover:bg-slate-800">Send reply</button></form></section><aside class="h-fit rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-semibold">Ticket details</h2><dl class="mt-4 space-y-3 text-sm"><div><dt class="text-slate-500">Category</dt><dd class="mt-1 font-medium">{{ $ticket->category->label() }}</dd></div><div><dt class="text-slate-500">Priority</dt><dd class="mt-1 font-medium">{{ $ticket->priority->label() }}</dd></div><div><dt class="text-slate-500">Last update</dt><dd class="mt-1 font-medium">{{ $ticket->updated_at->format('d M Y, h:i A') }}</dd></div></dl></aside></div>
@endsection
