<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow,noarchive"><title>{{ $invoice->invoice_number }} | RetailPOS</title>@vite(['resources/css/app.css'])</head><body class="min-h-screen bg-slate-100 text-slate-900"><main class="mx-auto max-w-4xl p-4 sm:p-8"><article class="overflow-hidden rounded-lg border bg-white shadow-sm"><header class="bg-slate-950 p-6 text-white"><div class="flex justify-between gap-4"><div><p class="text-sm font-semibold text-teal-300">RETAILPOS.BIZ</p><h1 class="mt-2 text-2xl font-semibold">Invoice {{ $invoice->invoice_number }}</h1><p class="mt-2 text-sm text-slate-300">{{ $invoice->billing_company ?: $invoice->billing_name }}</p></div><div class="text-right text-sm"><p>Due date</p><p class="mt-1 font-semibold">{{ $invoice->due_date?->format('d M Y') ?? 'Not specified' }}</p></div></div></header><div class="p-6"><div class="grid gap-4 sm:grid-cols-3"><div><p class="text-xs font-semibold uppercase text-slate-500">Total</p><p class="mt-1 text-xl font-semibold">{{ $invoice->currency }} {{ number_format((float)$invoice->grand_total,2) }}</p></div><div><p class="text-xs font-semibold uppercase text-slate-500">Paid</p><p class="mt-1 text-xl font-semibold text-teal-700">{{ $invoice->currency }} {{ number_format((float)$invoice->amount_paid,2) }}</p></div><div><p class="text-xs font-semibold uppercase text-slate-500">Balance due</p><p class="mt-1 text-xl font-semibold text-rose-700">{{ $invoice->currency }} {{ number_format((float)$invoice->balance_due,2) }}</p></div></div>@if($invoice->isOverdue())<p class="mt-5 rounded-lg bg-amber-50 p-3 text-sm font-medium text-amber-900">This invoice is overdue. Please contact RetailPOS if you need assistance.</p>@endif<table class="mt-6 min-w-full text-sm"><thead class="border-b text-left text-xs uppercase text-slate-500"><tr><th class="py-3">Item</th><th class="py-3 text-right">Qty</th><th class="py-3 text-right">Rate</th><th class="py-3 text-right">Total</th></tr></thead><tbody>@foreach($invoice->items as $item)<tr class="border-b"><td class="py-3"><p class="font-medium">{{ $item->name }}</p><p class="text-xs text-slate-500">{{ $item->description }}</p></td><td class="py-3 text-right">{{ $item->quantity }}</td><td class="py-3 text-right">{{ number_format((float)$item->unit_price,2) }}</td><td class="py-3 text-right font-semibold">{{ number_format((float)$item->line_total,2) }}</td></tr>@endforeach</tbody></table><a href="{{ route('invoices.public.pdf',$token) }}" class="mt-6 inline-flex rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Download invoice PDF</a>@if($invoice->payments->isNotEmpty())<section class="mt-6 border-t pt-5"><h2 class="font-semibold">Payments received</h2>@foreach($invoice->payments->where('status','!=','reversed') as $payment)<div class="mt-3 flex justify-between rounded-lg bg-slate-50 p-3 text-sm"><span>{{ $payment->receipt_number }} · {{ $payment->payment_date?->format('d M Y') }}</span><span><a class="font-semibold text-teal-700" href="{{ route('invoices.public.receipts.pdf',[$token,$payment]) }}">{{ $payment->currency }} {{ number_format((float)$payment->amount,2) }} receipt</a></span></div>@endforeach</section>@endif</div><footer class="border-t bg-slate-50 p-6 text-sm text-slate-600">India +91 8072682244 · Malaysia +60 104305163 · Singapore +65 92475024<br>info@retailpos.biz · global@retailpos.biz</footer></article></main></body></html>
